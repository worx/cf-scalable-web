# SPDX-License-Identifier: GPL-2.0-or-later
# Copyright (C) 2026 The Worx Company
# Author: Kurt Vanderwater <kurt@worxco.net>
#
# Environment configuration for cf-scalable-web
#
# Copy this file to .env and edit for your environment:
#   cp .env.example .env
#
# All values have sensible defaults in the Makefile. This file is
# only needed to override them (e.g., setting AWS_PROFILE for
# workstation use).
#
# NOTE: .env is gitignored -- it is never committed.

# -------------------------------------------------------------------------
# AWS Configuration
# -------------------------------------------------------------------------

# AWS CLI named profile (workstation only)
# Leave commented out on EC2 instances that use IAM instance profiles.
# The instance role provides credentials automatically via IMDS.
#AWS_PROFILE=ZI-Sandbox

# AWS region (default: us-east-1)
AWS_REGION=us-east-1

# Prevent AWS CLI from opening a pager for large outputs
AWS_PAGER=''

# Disable interactive auto-prompt (prevents hangs in scripts)
AWS_CLI_AUTO_PROMPT=off
