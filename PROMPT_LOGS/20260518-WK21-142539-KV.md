# AI Prompt Log

**Date**: 2026-05-18 14:25:39
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:25:39 - User Prompt

```
kick off the AMI bake
```

---

### 14:27:12 - Response

All three pipelines are baking, RecipeVersion 1.0.10. Historical builds take ~10-15 min from BUILDING → AVAILABLE.

**Estimated ready:** ~19:40 UTC (about 13-15 min from now).

**Once they're all AVAILABLE**, the cutover sequence is:

```bash
# From your Mac or deploy-host (Mac works since we have ZI-Sandbox creds):
cd ~/Work/dev/cf-scalable-drupal  # or your deploy-host clone
make update-ami-param ENV=sandbox PIPELINE=nginx
make update-ami-param ENV=sandbox PIPELINE=php74
make update-ami-param ENV=sandbox PIPELINE=php83
make deploy-compute ENV=sandbox

# Roll the fleet
for ASG in sandbox-nginx-asg sandbox-php74-asg sandbox-php83-asg; do
  aws autoscaling start-instance-refresh --auto-scaling-group-name $ASG \
    --strategy Rolling \
    --preferences '{"MinHealthyPercentage":50,"InstanceWarmup":120,"SkipMatching":true}'
done
```

While the AMIs bake (next 15 min), good time to **draft a couple of the P1 Make targets**. Want me to start on:

1. **`make build-amis` waiter** — modifies the existing target to poll until all pipelines hit AVAILABLE/FAILED before returning (eliminates the silent-race with `update-ami-param`)
2. **`make update-ami-params`** plural wrapper — calls the three per-pipeline ones
3. **`make check-drift`** — warns when local templates are newer than deployed stacks
4. **`make restart-php-fpm` + `make clear-drupal-cache`** — the two fleet/state helpers

Any preference, or want me to just pick the most impactful (#1 + #2 together, since they fix the exact race we keep hitting)? I'd say #1 + #2 in one commit, then #3 separately, then leave #4 for when you have a use case to drive it.

---

### 14:27:12 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
