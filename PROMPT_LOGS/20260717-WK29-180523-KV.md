# AI Prompt Log

**Date**: 2026-07-17 18:05:23
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 18:05:23 - User Prompt

```
I ran your backfill and then went back in and added a chown so that it was owned by www-data instead of root. Since we did a sudo on the whole thing, it was owned by root. Figured with the 644, I probably didn't need it, but just to be complete, I went ahead and did it. The URL has been successfully made, and I'm inside looking at their production data, so everything from that standpoint seems to be working fine. 
Yeah, Akama, I like your checks better than the.installed, though the information inside of the.installed is kind of nice to have, so maybe we'll keep it, but we'll check more legitimately instead of just relying on that brittleness.
```

---

### 18:05:54 - Response

Migration test **passes end-to-end.** Prod data serving on sandbox Drupal, admin login works, UI renders. That's the whole loop closed.

## Immediate next moves (your call)

**A. Capture the current known-good state**
```bash
make backup-deploy-host
```
This includes your just-placed `.installed` marker in the tarball so next instance replacement doesn't lose it.

**B. Cleanup the backup DB (or not)**
Kept for a bit:
```bash
psql-env sandbox -d postgres -c "\l" | grep drupal_backup
# still there, can psql into it for comparison
```
Drop when ready:
```bash
psql-env sandbox -d postgres -c \
  "DROP DATABASE drupal_backup_20260717_201352;"
```

## Follow-ups accumulated during today's test

I'll draft these as tracked items — need commits when we get to them:

1. **Structural `.installed` check** — swap the 4 sites (admin-login-url.sh, install-drupal-remote.sh, publish-drupal-vhost.sh, Makefile:verify-drupal) from `[ -f /var/www/drupal/.installed ]` to `[ -x /var/www/drupal/vendor/bin/drush ] && [ -f /var/www/drupal/web/sites/default/settings.php ]`. Keep the marker file — it's a nice metadata dump — but stop using its existence as the correctness gate.
2. **Backup `.installed` in BACKUP_PATHS** — add `/var/www/drupal/.installed` to the deploy-host backup so post-rebuild we still have the info. Also — you should own future markers as www-data so they match the ownership pattern (nit: fix install-drupal.sh:643 which currently just `chmod`s, doesn't `chown`).
3. **run-pgloader.sh Phase 6 verify query bug** — checks `public` schema, should check `zinew` (or derive from source MySQL DB name).
4. **`_ssm-run-deploy-host.sh` + Mac-side dispatch targets** (already in TODO.md) for run-overnight migrations.
5. **Makefile verify-drupal path bug** (`/var/www/$(ENV)/drupal/.installed` → `/var/www/drupal/.installed`) — becomes moot when we do #1, or fixed alongside.
6. **`migration/README.md`** — Makefile references but file doesn't exist.
7. **`docs/MIGRATION.md`** — rewrite for post-consolidation flow (current doc is stale, mentions deleted target names).
8. **Files / private restore path** — dump-files + dump-private stage to S3, but nothing pulls onto FSx. Blocks any test where images/downloads matter.
9. **`make db-backup` / `make db-restore` wrappers** — automate today's psql-env heredoc dance.

**Want me to memorize your preference for structural checks over marker files** as a project memory (like the DB rollback pattern one)? It's the same shape — a design principle you'd apply beyond just this one case.

---

### 18:05:54 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
