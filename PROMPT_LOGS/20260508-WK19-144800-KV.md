# AI Prompt Log

**Date**: 2026-05-08 14:48:00
**Week**: 19
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:48:00 - User Prompt

```
yea - anal-retentive that I am - the fix was good - now let's see it after a new deploy :) - the 4 stage redeploy is running...
```

---

### 14:48:26 - Response

Patience pays. While you're waiting, here's the post-deploy checklist for when SSH access is back:

## Post-deploy validation

```bash
# Find new instance ID, update SSH config + add your public key (one-time per new box)
INSTANCE_ID=$(AWS_PROFILE=ZI-Sandbox aws ec2 describe-instances \
  --filters "Name=tag:Name,Values=cf-deploy-host" "Name=instance-state-name,Values=running" \
  --query 'Reservations[].Instances[].InstanceId' --output text --region us-east-1)
echo "New instance: $INSTANCE_ID"

# SSM in (no SSH needed yet)
AWS_PROFILE=ZI-Sandbox aws ssm start-session --target "$INSTANCE_ID" --region us-east-1
```

On the deploy-host:

```bash
# Add your public key
mkdir -p ~/.ssh && chmod 700 ~/.ssh
cat >> ~/.ssh/authorized_keys <<'EOF'
<paste public key>
EOF
chmod 600 ~/.ssh/authorized_keys
exit
```

Update SSH config on Mac with new instance ID, then:

## Bedrock validation — should all pass with NO manual fixes

```bash
ssh deploy-host

# === bootstrap state ===
cat /etc/motd                                    # should exist (the bug we just fixed)
ls /etc/worxco/                                  # marker + envs/
head -3 /etc/worxco/envs/sandbox                 # should start with "export ..."
df -h | grep /var/www                            # FSx auto-mounted
grep "worxco-mount-env" /etc/fstab               # fstab entry written
grep "worxco-fsx-hosts" /etc/hosts               # /etc/hosts entry from refresh-env-config

# === Drupal install survives ===
ls /var/www/sandbox/drupal/.installed            # marker file from previous install
cat /var/www/sandbox/drupal/.installed           # see when it was installed
make verify-drupal ENV=sandbox                   # full health check

# === plain source works (no set -a needed) ===
source /etc/worxco/envs/sandbox
env | grep DRUPAL_DB_HOST                        # exported value visible
```

If all of those pass cleanly with **zero manual `sudo install` or `sudo refresh-env-config`** commands, you've proven the whole pipeline regenerates itself from a destroy/deploy. That's the bedrock validation.

Last thing — if you want browser proof too:
```bash
source /etc/worxco/envs/sandbox
export DRUPAL_DB_PASS="$(aws secretsmanager get-secret-value \
  --secret-id worxco/sandbox/drupal/db-password \
  --query SecretString --output text)"
tmux new-session -d -s drupal-cloud "cd /var/www/sandbox/drupal && vendor/bin/drush runserver 0.0.0.0:8080"

# On Mac: ssh -L 8080:localhost:8080 deploy-host
# Browser: http://localhost:8080 → admin / <password from secretsmanager>
```

Tell me how it goes. If clean — Phase C step 2 is *truly* validated and we're cleared for step 3.

---

### 14:48:26 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
