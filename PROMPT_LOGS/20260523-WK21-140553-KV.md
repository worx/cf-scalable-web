# AI Prompt Log

**Date**: 2026-05-23 14:05:53
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:05:53 - User Prompt

```
great - now, let's take a look at our TODO and see what is outstanding that we can discuss, prioritize, and execute
```

---

### 14:07:00 - Response

Here's a snapshot organized by what's actually live in the TODO vs. what got resolved this session.

## ✅ Resolved this session (TODO not yet updated)

These are checked-in but TODO.md is stale on them:

| Item | Status |
|------|--------|
| `build-amis-if-needed` (was P3) | ✅ commit 3dfbf44 |
| Route 53 DNS automation (was Phase 3 area) | ✅ publish-dns / unpublish-dns / smoke-test-public commits |
| use-env stale-NFS hardening | ✅ commit b79cdeb |
| destroy-all unmount-FSx step | ✅ commit e638d58 |
| nginx /health decoupled from Drupal install | ✅ commit 77e9eca |
| install-drupal separated from deploy-allX | ✅ commit c076078 |

I can update TODO.md to mark these done in a sweep commit — say the word.

## 🟥 Outstanding — Priority Order

### P0 — Architectural conversations still owed

- **Architectural review: build-order assumptions + multi-site future.** Two threads:
  - (a) When orchestration order is violated, worker scripts bail silently or run wrong. Should worker boxes self-validate prerequisites or should orchestration enforce strict order?
  - (b) Today's `env↔RDS` 1:1 assumption is baked into SSM paths, settings.php, secret paths. Won't survive the multi-site future.
  - **Next step:** read `docs/ARCHITECTURE.md` + `docs/plans/multi-tenancy.md`, then talk direction. No code yet.

### P1 — One real production blocker

- **Move Drupal `hash_salt` to SSM SecureString.** Currently on FSx only (`/var/www/drupal-private/salt.txt`). Every destroy-all + redeploy invalidates active sessions, password-reset links, CSRF tokens, "remember me" cookies across the entire user base. Tolerable in sandbox; **not** OK for production. Full design exists in `docs/memory/salt-persistence-design.md`. ~2 hours of work.

### Phase 3 — HTTPS (was "next on my list" before the deploy-debug detour)

Whole cluster of related work, currently scattered in TODO:
- ACM wildcard cert for `*.envs.zoning-info.com` (validates via DNS in the sub-zone you just delegated — free now that DNS plumbing works)
- ALB HTTPS listener (port 443) + optional HTTP→HTTPS redirect on port 80
- Drupal `$settings['reverse_proxy']` and `reverse_proxy_addresses` for X-Forwarded-Proto trust
- nginx pass-through of X-Forwarded-Proto to PHP-FPM
- `docs/SSL-MANAGEMENT.md`
- CertBot tasks in TODO are stale — we're using ACM, not CertBot. Worth cleaning up.

Estimated: ~half day of work, would naturally become `v0.2.0-https`.

### P2 — Documentation polish

- **`docs/OPERATIONS.md`** — already in progress; document the actual now-stabilized workflow (deploy-allX → install-drupal-full → smoke-test-public).
- **YAML frontmatter on all docs** for Obsidian + future doc-index/master-PDF builds.
- **`docs/SSL-MANAGEMENT.md`** — natural pair with Phase 3 work.
- `docs/READING-GUIDE.md` — defer until docs are more stable.
- `docs/master.md` + `make build-master` — design captured, low priority.

### P3 — Deferred (no immediate action expected)

- Drupal upgrade promotion workflow (Phase D — global TODO)
- Tighter Secrets Manager IAM (global TODO)
- Ship deploy-host bootstrap log to CloudWatch (global TODO)
- `make check-updates ENV=…` operator-facing target
- Multi-tenancy refactor (Phase E — design exists in `docs/plans/multi-tenancy.md`)
- Decide post-destroy-all residue strategy
- Ubuntu 26.04 LTS spike (gated on August 2026 .1 release)
- Drush site-aliases (gated on multi-tenancy)

## My recommendation for what to pick up next

1. **First (5 min):** Let me sweep the TODO.md update — mark this session's wins, prune stale CertBot items, tighten the language. Zero risk, repo hygiene.
2. **Then the choice:**
   - **Path A — HTTPS now.** Natural follow-on, was your stated next-up before the detour, easy with ACM since DNS plumbing is in place. Tag `v0.2.0-https` after.
   - **Path B — Salt persistence first.** Smaller chunk (~2 hours), but it's the one item with a real "this would bite prod" footprint, and design is already captured.
   - **Path C — Architectural conversation.** No code;

*[Response truncated at 4000 characters]*

---

### 14:07:00 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
