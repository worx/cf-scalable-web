# AI Prompt Log

**Date**: 2026-07-29 14:32:09
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:32:09 - User Prompt

```
not actually - RE: S3 - it showed here that it succeeded - even though it failed... I thought that was A: interesting -and- B: a bug :(
```

---

### 14:33:57 - Response

Confirmed the bug and the (misplaced) intent — line 224 even documents it:

> `# Note: every cmd has || true appended, so rc=0 even on stack-not-found.`
> `# Only non-zero exit here indicates a genuine make-target failure.`

The author's intent was **"catch stack-not-found (already destroyed) so we don't fail on re-runs."** But `|| true` also catches genuine failures like `DELETE_FAILED`, hiding them.

## Fix — three options, listed by scope

**Option A — Post-teardown verification (small addition, no existing behavior touched).** After all tracks finish, orchestrator queries CFN for each expected stack. Any that still exist → real failure, reported separately from track state. Additive, doesn't disturb the `|| true` swallowing pattern (which does handle the legit "already gone" case).

**Option B — Smarter check_completed (moderate).** Remove `|| true` from each cmd. Post-completion, on non-zero exit, do a describe-stacks check: if the stack doesn't exist → treat as success (was already gone); if it exists in `DELETE_FAILED` → failure. Requires mapping track → stack name.

**Option C — Fix the Makefile destroy-* targets (biggest).** Make each `destroy-*` target internally distinguish "stack doesn't exist" (exit 0) from "delete-stack failed" (exit non-zero). Then the orchestrator can remove `|| true` entirely.

## Recommendation

**Not tonight.** The bug doesn't block your rebuild — it just hides failures on the destroy side, and *this* destroy is verifiably complete (we manually confirmed no stacks remaining). Fix belongs on the follow-up backlog alongside:

- Auto-purge versioned S3 buckets on stack delete (cf-storage-s3.yaml Lambda custom resource)
- The `dispatch-clean-migration-baks.sh` improvements
- Age-based auto-prune option for `.BAK` dirs
- **This orchestrator false-success bug**

Option A is the least invasive fix if we want to add it later — a single new phase at the end of destroy-all-parallel that does `describe-stacks` for each track's expected stack and prints red errors for any survivors.

## For now — proceed with `make deploy-all`

Not blocking anything. Say the word and let me know when deploy-all is running / done.

---

### 14:33:57 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
