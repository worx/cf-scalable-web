# AI Prompt Log

**Date**: 2026-05-18 14:22:27
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:22:27 - User Prompt

```
OK, yes, I'm going to ask you to flesh it out and prioritize the to-dos, but I want to make a comment on your reorganization of the docs folder, specifically your maybe an index.md. I don't disagree with the index.md, but one of the things that I would like to add into the system is a master.md. I might be thinking about this wrong, so just use it as a general, not a specific directive.
In the master.md, I would like instructions that you can use to build a Master PDF. The master PDF would have all of our MD files aggregated together into a single hyperlinked PDF file built using our LaTeX plug-ins or whatever we call that LaTeX thing, our LaTeX skill, using that to build it out. Basically, it is turning all that documentation together into a book that is ordered appropriately:
1. Getting started and an introduction
2. After they've gotten that, the detailed documentation for all of the make stuff that we've got out there, which may or may not be documented anywhere except the makefile. It needs to be documented for an end-user book.
3. Our architectural stuff and all the rest of the things that we've got in the docs folder
I don't know the exact order of everything off the top of my head. This is just a thought that I want to make sure we capture so that we can have this conversation at a later time and come up with a build process to update the master.pdf from time to time as we make progress in our total project. Our.md files need to be updated pretty regularly. Any time we make a change, we need to be updating our documentation. The master.pdf really only needs to be updated when somebody wants to kind of get an overview, a look at the whole system.
It could even be part of the make. We could have a `make build-master` or something like that. Obviously, we can't run that on the deploy host because we don't have LaTeX installed, and the first thing that make would have to do is check that LaTeX is available and send a message to the end user if they don't have the appropriate tools available on their box. I might even take the master.pdf and gitignore it so that we can build it as many times as we want. Also, gitignore any of the rest of the intermediate LaTeX files so that we can build this anytime we want, but we're always building it from whatever's current. I hope that makes some sense.
```

---

### 14:23:13 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/TODO.md`

---

### 14:23:21 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/TODO.md`

---

### 14:23:25 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/TODO.md`

---

### 14:25:18 - Git Commit

**Commit:** `41f25d32fa4c`

**Message:**
```
docs(todo): codify lessons from 2026-05-13->2026-05-18 debug arc + master PDF design

Add a new "Operational Hardening" section to the project TODO with the
debug-session findings prioritized by "would this bite again":

P0 (regressions waiting to happen):
  - AMI bake-and-roll to 1.0.10 (start->restart fix not yet in any deployed AMI)
  - install-drupal.sh psql heredoc password mangling (latent — ExcludeChars
    masks it for now but a single character change reopens the trap)
  - deploy-host cutover to /var/www mount (commits in git, host not yet flipped)

P1 (operational tooling):
  - make check-drift (the cf-iam stack sat un-deployed for 6 days)
  - make build-amis wait for pipeline completion
  - make update-ami-params plural wrapper
  - make restart-php-fpm fleet-wide
  - make clear-drupal-cache (filesystem + DB cache tables)
  - OPcache reset story when settings.php changes

P2 (documentation):
  - docs/OPERATIONS.md runbooks
  - docs/master.md design (see below)

P3 (already captured elsewhere):
  - Drupal upgrade promotion (in global TODO)
  - Multi-tenancy (in docs/plans/multi-tenancy.md)

Also adds a design-captured section under Future Enhancements for the
master.pdf concept: a single hyperlinked PDF aggregating all *.md
files built via the existing LaTeX skill, driven by docs/master.md as
the manifest, exposed via `make build-master` with a local-LaTeX
pre-flight check. Open questions noted for the build session. PDF and
intermediate LaTeX artifacts will be .gitignore'd; always built fresh
from current sources, never committed.

Status footer updated: Phase 2 (compute) and Phase C (Drupal install)
both marked complete; SSL/Routing (Phase 3) explicitly deferred.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	TODO.md
```

---

### 14:25:23 - Response

Pushed `41f25d3`. TODO.md is now the canonical source for what's next, prioritized.

## Recommended order to actually tackle these

If you want to start knocking items off **today/this session**, my suggestion:

1. **P0 #1: AMI bake-and-roll to 1.0.10** (~30 min: ~15 min waiting for pipelines, ~10 min for refresh, ~5 min for verification). This is the biggest one — without it, your fleet is one instance cycle away from re-breaking. Pure "kick off and wait" work.

2. **While AMIs build** — P1 items don't require AWS time. Could draft any of:
   - `make check-drift` (~20 min, includes a script that diffs local mtimes vs deployed stack times)
   - `make build-amis` waiter + `make update-ami-params` plural (~20 min total, tightly related)
   - `make restart-php-fpm ENV=...` + `make clear-drupal-cache ENV=...` (~30 min, the two FSx-state busters)

3. **P0 #2: install-drupal.sh psql heredoc fix** (~15 min). Latent but worth closing while the context is fresh.

4. **P0 #3: deploy-host /var/www cutover** (~15 min, but riskier — touches your running deploy-host's mount state. Best done when you're ready to focus, not in parallel with anything else).

Want to kick off #1 (AMI bake) now so it runs in the background, and start drafting some of the P1 Makefile improvements while it bakes?

---

### 14:25:23 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
