# AI Prompt Log

**Date**: 2026-05-07 15:02:53
**Week**: 19
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 15:02:53 - User Prompt

```
Okay, this write-up you just provided desperately needs to be made a part of our documentation because I don't want to forget this. You're right, it is extremely valuable. So for now, let's get our documentation updated with this, and then we'll go on to phase C.
```

---

### 15:04:11 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/DEPLOY-HOST.md`

---

### 15:05:09 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/DEPLOY-HOST.md`

---

### 15:05:47 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/DEPLOY-HOST.md`

---

### 15:06:05 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/architecture-reference.md`

---

### 15:06:41 - Git Commit

**Commit:** `084bc16ea8d6`

**Message:**
```
docs(deploy-host): Document control plane vs data plane architecture

Captures the architectural insight from today's discussion: the
deploy host is the system's control plane, sharing FSx and RDS with
the data plane (PHP-FPM ASG) but with a very different network
posture. PHP-FPM is sealed from the internet by design; new Drupal
code, modules, and config changes enter the system only through the
deploy host.

This isn't an emergency-access pattern — it's the primary, intended
day-two operations story.

Updates to docs/DEPLOY-HOST.md:

  Overview / Mermaid diagram
    Refreshed to show the deployed state: VPC peering, FSx mount,
    helper commands, RDS/Valkey access. Replaces the old "Future:
    VPC Peering" placeholder with current reality.

  Architecture table
    Bumped t4g.micro -> t4g.small. Added rows for VPC peering, FSx
    access, DB access, cache access — the things the deploy host
    can actually do now after Phases A and B.

  VPC Peering section
    Updated from "Future" to "per-environment". Documents the FSx
    DNS caveat (doesn't resolve across peering — refresh-env-config
    maintains /etc/hosts entries instead).

  Control Plane vs Data Plane (new section)
    Side-by-side comparison: where each role lives, what it does,
    internet egress, FSx access, RDS access, AWS API access. Explains
    WHY the split matters (PHP-FPM sealed for security; deploy host
    is the audited entry point).

  Day-Two Operations (new section)
    Concrete recipes for: installing a module, database ops, cache
    ops, config management, drush runserver as a "preview through
    the deploy host" pattern with full SSH-tunnel-via-SSM
    instructions.

  Pre-Installed Tools (expanded)
    Three tables now: core ops, Drupal management toolchain
    (composer, drush, psql, sqlite, redis-cli, PHP+12 ext), helpers
    (info-env, show-env, refresh-env-config, mount-env, psql-env,
    valkey-env). Plus a new "State files" table covering marker,
    /etc/worxco/envs/*, /etc/hosts managed block, bootstrap log.

Updates to docs/memory/architecture-reference.md:

  Added "Control Plane vs Data Plane" section near the top so future
  Claude sessions land on the architectural model immediately. Points
  to the full DEPLOY-HOST.md sections for detail.

This closes the conceptual gap that was bugging Kurt — the
"oh, that's actually the *intended* design" moment is now captured
where future-us will find it.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	docs/DEPLOY-HOST.md
M	docs/memory/architecture-reference.md
```

---

### 15:06:54 - Response

Pushed (`084bc16`).

## Summary of what's in the docs now

**`docs/DEPLOY-HOST.md`** (now ~480 lines) covers:
- Updated Mermaid diagram showing the actual deployed architecture
- Architecture table reflecting t4g.small + peering + FSx mount + helpers
- **Control Plane vs Data Plane** section — the architectural insight, with side-by-side comparison and "Why this split matters" rationale
- **Day-Two Operations** section — concrete recipes for module installs, database ops, cache ops, config management, and drush runserver previewing
- Expanded **Pre-Installed Tools** with three tables (core ops, Drupal toolchain, helpers) plus state files

**`docs/memory/architecture-reference.md`** has a new "Control Plane vs Data Plane" section at the top so future Claude sessions land on the model immediately.

The "you've actually rediscovered the whole point" moment is now captured permanently. Future-you (and future-Claude) will find it.

---

Now ready for **Phase C** when you are. Where would you like to start?

1. **Continue iterating locally** — refine `install-drupal-local.sh`, add more validation, polish the install experience
2. **Build cloud variant** — port validated logic from local script to `install-drupal.sh` (RDS + FSx + dynamic settings.php)
3. **Build `cf-app-drupal.yaml`** — the minimal CFN stack holding Secrets Manager entries (admin pw, DB user pw) and SSM parameters

I'd suggest in that order: fully exercise local first, then build out the CFN scaffolding, then port to cloud — but you tell me.

---

### 15:06:54 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
