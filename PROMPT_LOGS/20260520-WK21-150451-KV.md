# AI Prompt Log

**Date**: 2026-05-20 15:04:51
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 15:04:51 - User Prompt

```
browser test passed.

1) Just a dumb question about the salt.txt file. Should that be stored on a disk somewhere that might get destroyed and rebuilt? Whether that disk is SSD or a rotating disk is irrelevant, but whether it's FSx or a deploy host is relevant, and both of them can get rebuilt. Should we be storing our salt in AWS config? 

2) I have looked at your make endpoints, and while I like what it's giving me, I would also like to know, for the purposes of GUI testing, a username and a password that'll let me get into it. Preferably the admin username and admin password.

Now, you and I both know we shouldn't be publishing passwords. We could publish the admin username if that's what we're using, and we could publish the AWS query that looks up the password. That would only work if I had the rights and permissions to execute it, so that would be a good thing to add to our make endpoints.
Just a thought...

3) We need some documentation now for end-user Drupal engineers, systems engineers, and so on. While you're building wonderful nginx, wonderful CSS, and so on, I need to know where each of those assets is stored so that if I need to make a change to it, I make the change in the right place. Preferably in a place that's got something to do with CloudFormation so that we've got infrastructure as code under git, and now I have an audit trail and a history.
```

---

### 15:06:19 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 15:07:30 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/salt-persistence-design.md`

---

### 15:07:44 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/TODO.md`

---

### 15:09:34 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/CUSTOMIZATION-GUIDE.md`

---

### 15:09:41 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/README.md`

---

### 15:10:00 - Git Commit

**Commit:** `75fe4750b057`

**Message:**
```
docs: customization guide + salt-persistence design + endpoints admin info

Three threads from the 2026-05-20 evening review:

1) docs/CUSTOMIZATION-GUIDE.md (new) — addresses the "where do I make
   this change?" question from Drupal engineers / sysadmins / new
   contributors. Maps every customizable surface (network, IAM, AMI
   contents, fleet boot behavior, Drupal codebase, settings.php,
   nginx vhost, PHP runtime, admin UI / themes, secrets) to the
   canonical source file + apply workflow + whether the change
   survives destroy-all. Includes a "where do things persist?" cross-
   cutting table at the bottom for the audit-trail question. README
   "Documentation map" gets a new "I want to customize / change
   something" section pointing at this doc.

2) docs/memory/salt-persistence-design.md (new) + TODO.md P1 entry:
   Drupal's hash_salt currently lives only on FSx at
   /var/www/drupal-private/salt.txt. FSx is destroyed by destroy-all,
   which regenerates salt, which invalidates every active session +
   password-reset link + CSRF token. Tolerable for sandbox; not OK
   for production. Proposed SSM SecureString design + ownership
   options (cf-app-drupal CFN-managed vs install-drupal.sh script-
   managed) + multi-tenancy interaction (per-site salt paths) + IAM
   requirements + implementation checklist. Not implementing now —
   capturing the design so when we DO implement we don't re-derive.

3) `make endpoints ENV=<env>` extended: new "Drupal admin login"
   block shows the /user/login URL, admin username (from
   /<env>/drupal/admin-username SSM), and a copy-paste-ready aws CLI
   command to fetch the admin password from Secrets Manager (which
   works only if the caller's IAM principal has GetSecretValue on
   that ARN — defense in depth, no password ever printed to terminal
   or logs).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	Makefile
M	README.md
M	TODO.md
A	docs/CUSTOMIZATION-GUIDE.md
A	docs/memory/salt-persistence-design.md
```

---

### 15:10:12 - Response

Pushed `e0d03e7..75fe475`. All three asks addressed:

**1. salt.txt persistence** — captured as P1 TODO + full design doc at `docs/memory/salt-persistence-design.md`. Diagnosed the problem (FSx-only → wiped on destroy-all → all sessions/tokens invalidated), proposed SSM SecureString as the right store, weighed cf-app-drupal CFN-ownership vs install-drupal.sh script-ownership (leaning script for simplicity), and wrote an implementation checklist for future-me. Not implemented yet — deliberate, since multi-tenancy will affect the per-site path structure.

**2. `make endpoints` admin block** — adds login URL, admin username, and the aws CLI fetch command for the password (never prints the password itself). Try it: `make endpoints ENV=sandbox`.

**3. `docs/CUSTOMIZATION-GUIDE.md`** — the "where do I edit this?" map. Covers infrastructure (CFN/AMI/network/IAM), Drupal application layer (codebase, settings.php, vhost, PHP runtime, admin UI, themes, secrets), and a cross-cutting "what survives destroy-all?" table for the audit-trail question. Honest about today's gaps (no per-site git repo for Drupal themes/modules — captured as the multi-tenancy direction). README's documentation map now has a new "I want to customize / change something" section pointing at it.

---

### 15:10:12 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
