# AI Prompt Log

**Date**: 2026-07-30 09:45:24
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 09:45:24 - User Prompt

```
the deploy-all should be done - what is a quick, command line, to check that all the CFN scripts finished without error?
```

---

### 09:46:21 - Response

**All 14 stacks are ✓ CREATE_COMPLETE or UPDATE_COMPLETE.** Second query returned `[]` — no failures, no rollbacks, nothing in progress. Clean.

Also visible:
- `migration-bucket` (2026-07-13, unchanged) — expected survivor
- `app-drupal` (2026-05-22, unchanged) — the intentional survivor we just documented tonight

## The command for your own use later

If you want just a pass/fail signal (empty = all good), one-liner:

```bash
aws --profile ZI-Sandbox cloudformation list-stacks \
  --query "StackSummaries[?contains(StackName, 'cf-scalable-web-sandbox') && StackStatus != 'DELETE_COMPLETE' && StackStatus != 'CREATE_COMPLETE' && StackStatus != 'UPDATE_COMPLETE']" \
  --output json --region us-east-1
```

Empty `[]` = all clean. Anything else = investigate.

## Next up

Ready for:
- `make install-drupal-full` (~5-7 min per docstring)
- Then the full migration (`make dump-all` from Mac + all the restores)

Or want to verify infrastructure works cleanly first (compute reachable, ALB responding, RDS accepting connections) before Drupal install?

---

### 09:46:21 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
