# AI Prompt Log

**Date**: 2026-05-18 17:16:54
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 17:16:54 - User Prompt

```
I realized that having the deploy host out there is good, and the secrets are wonderful. We did destroy FSX, and that's where all the actual deployed Drupal codebase is kept, so we're going to have to redeploy the codebase. We can reuse the passwords if you want to, but we have to redeploy the codebase. And of course we also have to read deploy into the database, so a build of the Drupal infrastructure. We may have those keys, but we still need the code and the database, and that all is going to be taken care of by the Composer processes that are part of this build. I just want to make sure we don't forget those tiny little details. Nuances everywhere.
```

---

### 17:17:06 - Response

Yep — those nuances are all covered by `install-drupal.sh` and therefore by Phase 7. Walking through the post-destroy-all state to confirm:

**Preserved** (not touched by destroy-all):
- Deploy-host VM + its git clone + SSH key in Secrets Manager
- `cf-app-drupal` stack → `worxco/sandbox/drupal/db-password` + `admin-password` secrets + `/sandbox/drupal/*` SSM params

**Destroyed** (recreated by deploy-allX phases 1-5):
- FSx → all `/var/www/drupal` code gone
- RDS → drupal database + schema gone, `.installed` marker gone with FSx

**What `install-drupal.sh` does in Phase 7** (i.e., what your nuances actually need):
1. Read existing Drupal DB password from the preserved secret → `ALTER USER drupal_user WITH PASSWORD '<existing>'` on the brand-new postgres (password reuse, no rotation)
2. `composer create-project drupal/recommended-project` into the brand-new `/var/www/drupal` on the brand-new FSx (this is the codebase redeploy)
3. `drush site:install` against the brand-new RDS → creates `users`, `node`, `config`, all the `cache_*` tables, the admin account using the preserved admin password (this is the database redeploy)
4. Writes `settings.php` (env-var-driven, points at Phase 5's RDS endpoint + Valkey)
5. Writes `.installed` marker

So Phase 7 is doing *real work* — ~3-5 min of composer + drush site:install — not just re-linking. The skip-if-installed guard in my wrapper only fires if you re-run deploy-allX twice in a row without a destroy in between (in which case the marker IS on FSx and skipping is correct).

One thing worth flagging: if you ever want to verify the secret-reuse vs fresh-generation logic, it's at `install-drupal.sh:140-180` — the "create-or-rotate" branch is the bit that handles "secret already exists, just sync postgres to it."

Want me to commit the deploy-allX extension now (so the version on the deploy-host's `git pull` in Phase 7 includes it), or hold for review?

---

### 17:17:06 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
