# AI Prompt Log

**Date**: 2026-07-31 12:53:21
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 12:53:21 - User Prompt

```
my deploy is finished - there was an error near the very beginning - but it kept running.... there is an error at the end
http 500

I have added the /etc/hosts record
I get a white screen
```

---

### 13:07:28 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 13:07:35 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 13:07:52 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/scripts/deploy-host/restore-codebase.sh`

---

### 13:08:52 - Response

PROMPT_LOG is already being written by a hook (found `20260731-WK31-125321-KV.md` captures this session). No manual log needed.

## Summary

**Site is up.** `HTTP 200`, Drupal 11 (Commerce 3) content rendering.

**Root cause:** stale PHP-FPM OPcache after `restore-codebase` atomically swapped the codebase on FSx. OPcache held pre-swap bytecode referencing `Drupal\Core\Runtime\DrupalRuntime`, but the swap replaced the composer classmap with prod's (no such class) → fatal on every request. `clear-drupal-cache` doesn't touch OPcache — only `restart-php-fpm` does.

**Immediate fix:** `make restart-php-fpm ENV=sandbox` — already run.

**Permanent fix (staged, awaiting your review):**

| File | Change |
|---|---|
| `migration/Makefile` | Adds Phase 9 (`restart-php-fpm`) after Phase 8 in `migrate-full-all`. Docstring updated. |
| `migration/scripts/deploy-host/restore-codebase.sh` | Replaces the meek "Recommend:" tail with a loud warning box explaining the OPcache problem and required follow-up. |

Suggested commit message (pipe via stdin per your convention):

```
fix(migration): restart-php-fpm as mandatory Phase 9 in migrate-full-all

After restore-codebase atomically swaps /var/www/drupal on FSx, every
PHP-FPM worker still holds pre-swap bytecode in OPcache. Bytecode for
files that no longer exist executes — and resolves class names against
the NEW composer classmap on the fresh vendor tree, fataling when the
old bytecode references classes the new classmap doesn't have.

Symptom (this session): fresh Drupal 11.3.13 install shipped
Drupal\Core\Runtime\DrupalRuntime via vendor/autoload_runtime.php.
Prod's tarball uses classic bootstrap (no runtime shim). After the
swap, every request → HTTP 500 with WSOD.

clear-drupal-cache is not sufficient — it only wipes the FSx compiled-
container dir + cache_* tables. Only systemctl restart php-fpm on every
compute box invalidates per-worker OPcache.

Belt & suspenders:
- migration/Makefile: migrate-full-all adds Phase 9 = restart-php-fpm
- restore-codebase.sh: standalone use now prints a prominent warning
  block naming both required follow-ups (clear-cache, restart-php-fpm)

Ready for review. When approved:
  git add migration/Makefile migration/scripts/deploy-host/restore-codebase.sh
  git commit -F -   # paste the message above
```

## Still on the table

Kurt mentioned "an error near the very beginning" of the deploy that didn't halt. I haven't looked at it yet — want me to pull the deploy-all-parallel log tail and identify it, or move on?

---

### 13:08:52 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
