# AI Prompt Log

**Date**: 2026-07-28 13:57:35
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 13:57:35 - User Prompt

```
from React2:

ubuntu@ip-172-31-23-46:/var/www/html$ ls -al
total 2521116
drwxr-xr-x 11 ubuntu www-data       4096 Jun  2 06:47 .
drwxr-xr-x  5 ubuntu www-data       4096 Dec 15  2025 ..
-rwxr-xr-x  1 ubuntu ubuntu          163 Jul 29  2025 ._zoning-2.0-ddev
drwxr-xr-x 10 ubuntu www-data       4096 Sep 19  2025 drupal
drwxrwxr-x  3 ubuntu ubuntu         4096 Oct  4  2021 drupalComposer2
drwxrwxr-x  4 ubuntu www-data       4096 May 26 15:11 entity_bundle_manager
drwxrwxr-x  7 ubuntu www-data       4096 Jun 17 16:35 filtered_entity_reference
-rw-r--r--  1 ubuntu www-data  133402703 May 13  2019 finding-next.tgz
drwxr-xr-x  9 ubuntu www-data       4096 Dec 15  2025 frontend
drwxr-xr-x  8 ubuntu www-data       4096 Feb  5  2021 frontend_old
-rw-r--r--  1 root   root            612 Jun  2 06:47 index.nginx-debian.html
drwxrwxr-x  7 ubuntu www-data       4096 Aug 11  2025 zinew-back
drwxrwxr-x  6 ubuntu www-data       4096 May  7 14:52 zinew_back2
drwxrwxr-x 16 ubuntu www-data       4096 Jul 17 20:14 zoning_info_platform
-rw-rw-r--  1 ubuntu ubuntu   2448155069 May 26 21:08 zoning_info_platform.sql
```

---

### 13:59:54 - Response

Great news — **FSx is mounted at `/var/www/`**, not just `/var/www/drupal/`. So placing the modules at `/var/www/entity_bundle_manager/` and `/var/www/filtered_entity_reference/` puts them on FSx, visible to every node. And composer's path config uses `symlink: true` — which works fine because both ends of the symlink live on the same FSx. Clean path forward.

## The ferry — React2 → S3 → deploy-host → FSx

Same architecture as `sync-s3-media`. Steps:

### Step 1 — On React2 (SSH from your Mac as you're already doing)

```bash
sudo tar czf /tmp/custom-modules.tgz -C /var/www/html \
  entity_bundle_manager filtered_entity_reference
ls -lh /tmp/custom-modules.tgz
```

### Step 2 — Get it into the sandbox migration bucket

Cleanest via your Mac (React2 probably doesn't have `ZI-Sandbox` AWS creds; your Mac does):

```bash
# From your Mac:
scp ubuntu@<react2>:/tmp/custom-modules.tgz /tmp/
aws --profile ZI-Sandbox s3 cp /tmp/custom-modules.tgz \
  s3://sandbox-migration-kv-worxco/uploads/custom-modules.tgz
```

### Step 3 — On deploy-host (I can fire this via SSM once the tarball is uploaded)

```bash
# Fetch from S3:
aws s3 cp s3://sandbox-migration-kv-worxco/uploads/custom-modules.tgz /tmp/

# Extract into /var/www/ (which is FSx):
sudo tar xzf /tmp/custom-modules.tgz -C /var/www/
sudo chown -R www-data:www-data /var/www/entity_bundle_manager /var/www/filtered_entity_reference
ls -la /var/www/ | grep -E "entity_bundle_manager|filtered_entity"

# Refresh composer autoloader (composer will now find and symlink the local packages):
cd /var/www/drupal
sudo -u www-data COMPOSER_HOME=/tmp/composer-home composer install --no-dev --optimize-autoloader

# Verify symlinks:
ls -la /var/www/drupal/vendor/zoning-info/

# Now the actual fix:
source /etc/worxco/envs/sandbox
sudo -E HOME=/root vendor/bin/drush cache:rebuild
sudo -E HOME=/root vendor/bin/drush en bca -y
sudo -E HOME=/root vendor/bin/drush cache:rebuild

# Verify bundle classes registered:
sudo -E HOME=/root vendor/bin/drush php:eval '
$b = \Drupal::service("entity_type.bundle.info")->getBundleInfo("zi_research_document");
foreach ($b as $bundle => $info) {
  echo $bundle . " => " . ($info["class"] ?? "(NONE)") . "\n";
}
'
```

## What I need from you

1. **Do steps 1 and 2** (React2 tar → Mac → sandbox S3). Report back when the tarball is at `s3://sandbox-migration-kv-worxco/uploads/custom-modules.tgz`.
2. Then I fire **step 3** via SSM and report the drush output.

Or if you'd rather do all three yourself (SSM into deploy-host, do steps 3 by hand), that works too.

One caveat on `composer install`: it may pull additional dependencies (updates to other packages in vendor/) if the sandbox codebase's composer.lock is stale. If it does a lot of work, that's fine — it's idempotent and won't break running Drupal. If it errors on a dep resolution issue, we handle that.

Let me know when the tarball is up.

---

### 13:59:54 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
