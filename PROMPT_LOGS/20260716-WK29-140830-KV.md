# AI Prompt Log

**Date**: 2026-07-16 14:08:30
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:08:30 - User Prompt

```
<task-notification>
<task-id>aae2652b583868389</task-id>
<tool-use-id>toolu_01LtReGwDkCqZode4NvYuF22</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-kurtvanderwater-Work-dev-cf-scalable-drupal/7b8a99bf-1d83-4398-b0c4-e6fb7f0e0db9/tasks/aae2652b583868389.output</output-file>
<status>completed</status>
<summary>Agent "Map migration + Drupal ops targets" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Here is the DB-only migration runbook map.

## A. Migration workflow — happy-path targets

`migration/Makefile` help output (lines 107-160). Phase groupings:
- **Bucket** (sandbox side, `ZI-Sandbox`): `deploy-bucket` / `destroy-bucket` — L166, L180
- **Secret** (prod, `ZoningInfoAdmin`): `deploy-secret`, `set-secret-password`, `destroy-secret` — L206, L239, L223 (SECRET_NAME=`cf-migration/prod-mysql-zinew`)
- **Jumpbox** (prod, `ZoningInfoAdmin`): `deploy-jumpbox` / `destroy-jumpbox` — L261, L295
- **SSM utilities**: `ssm-jumpbox`, `ssm-tunnel`, `jumpbox-pubkey` — L353, L366, L387
- **Dumps** (jumpbox via SSM send-command → `migration/scripts/_ssm-run-jumpbox.sh`): `dump-mysql`, `dump-codebase`, `dump-files`, `dump-private`, `dump-all` — L428-491
- **Restore/pgloader** (deploy-host, instance role): `restore-mysql` (L535), `run-pgloader` (L568)
- **Backup/restore jumpbox state**: `backup-jumpbox`, `restore-jumpbox`, `list-jumpbox-backups` — L502-533

Per-target FROM/PROFILE/env vars:
- `dump-mysql` — FROM Mac|DH, both profiles; env: `MIGRATION_BUCKET`, `SECRET_NAME`, `DUMP_S3_KEY` (defaults `sandbox-migration-kv-worxco` / `cf-migration/prod-mysql-zinew` / `dumps/zinew.sql`). Script header: `migration/scripts/jumpbox/dump-mysql.sh:1-49`.
- `restore-mysql` — FROM **deploy-host only**, instance role. Env: `MIGRATION_BUCKET DUMP_S3_KEY DUMP_LOCAL_PATH LOCAL_DB_NAME LOCAL_DB_USER LOCAL_DB_PASS FORCE_DOWNLOAD CONFIRMED DRY_RUN` — see `migration/scripts/deploy-host/restore-mysql.sh:60-65`. Sanitizes `utf8mb4_0900_ai_ci`, rebuilds worxco@127.0.0.1 with `mysql_native_password` (required by pgloader's qmynd driver).
- `run-pgloader` — FROM **deploy-host only**, instance role. Env: `MIGRATION_BUCKET ENV_FILE TEMPLATE_PATH RENDERED_PATH PGLOADER_HEAP_MB` — `migration/scripts/deploy-host/run-pgloader.sh:60-75`. Sources `/etc/worxco/envs/sandbox` for `DRUPAL_DB_*` + `SANDBOX_PW[_ENC]`. Template `migration/pgloader/zinew.load.tmpl` uses `include drop` — destructive rebuild of target public schema.

**`dump-all`** (L456-491) sequences the four jumpbox dumps only. **No `db-migrate-all` wrapper** sequences `dump-mysql → restore-mysql → run-pgloader`. That chain is operator-orchestrated.

## B. Preflight / verification helpers

- `migration/Makefile` has `.preflight` (L336) — only checks SSM plugin + ZoningInfoAdmin creds. No `make status`/`make verify` in migration/.
- Top-level `Makefile:565` `status`, `1247` `verify-database` (shows RDS `DBInstanceStatus` only — no data check), `1054` `verify-drupal` (drush status + user/node/config/session counts against `/var/www/$(ENV)/drupal`).
- No script under `scripts/deploy-host/` queries `config` or `key_value` tables or checks enabled modules on the scratch MariaDB. **Gap.**
- `scripts/deploy-host/psql-env` (L1-46) — sandbox RDS shell, resolves endpoint via SSM param `/sandbox/rds/endpoint`, master password from `worxco/sandbox/rds/master-password`. Also `valkey-env` sibling. **No `mysql-env` wrapper for the scratch MariaDB** — operators use `sudo mysql` (socket auth).

## C. Drupal cache + maintenance helpers

- `scripts/clear-drupal-cache.sh` — SSM-dispatched. Requires `/etc/worxco/current-env` == `$ENV` and `/var/www` mounted. Wipes `/var/www/drupal/web/sites/default/files/php/*` AND TRUNCATEs every `cache_%` table (L59-72). Wrapped by `Makefile:2659 clear-drupal-cache`.
- **No maintenance-mode helper.** No `drush sset system.maintenance_mode` anywhere. **Gap** — operator must `ssm-deploy-host` and run `sudo -E vendor/bin/drush sset system.maintenance_mode 1` manually.
- `scripts/admin-login-url.sh` (+ `Makefile:2711`) — one-time `drush user:login` URL via SSM. Auto-selects https://site-name vs http://alb-dns.
- Drush from deploy-host: `scripts/admin-login-url.sh:104-116` shows canonical invocation — `cd /var/www/drupal &amp;&amp; source /etc/worxco/envs/$ENV &amp;&amp; export DRUPAL_DB_PASS=$(aws secretsmanager get-secret-value ...) &amp;&amp; sudo -E HOME=/root vendor/bin/drush ...`. `verify-drupal` (Makefile:1054) does the same.

## D. Rollback / snapshot

- **No RDS-snapshot Makefile target** exists (`create-db-snapshot` search: 0 hits in scripts/Makefile).
- `cloudformation/cf-database.yaml`: `BackupRetentionDays` param default 14 (L88-93), but sandbox parameter file `cloudformation/parameters/sandbox.json:23` sets it to **1** — so PITR window is only 1 day. `DeletionPolicy: Snapshot`, `UpdateReplacePolicy: Snapshot` (L226-227), `CopyTagsToSnapshot: true`, `DeletionProtection: true` for sandbox. **Recommendation: operator should `aws rds create-db-snapshot` manually before `run-pgloader`.**

## E. Existing runbook docs

- **`migration/README.md` does NOT exist** despite `migration/Makefile:33,109` referencing it — the Makefile advertises a runbook file that was never created. **Gap.**
- `docs/MIGRATION.md` (481 lines, dated 2026-06-12) is **stale**: references old target names `deploy-migration-bucket` / `deploy-migration-jumpbox` / `destroy-migration-bucket` / `ssm-migration-jumpbox` / `migration-jumpbox-pubkey` (lines 69-70, 86, 107, 377-378) that no longer exist in the consolidated `migration/Makefile`. The doc also describes the pre-consolidation flow: live SSM tunnel + pgloader against `localhost:3306`, no restore-mysql phase. Post-consolidation flow (`dump-mysql` → `restore-mysql` → `run-pgloader`) is not documented anywhere — this is the "docs/MIGRATION.md rewrite" follow-up.
- Other docs: `docs/ARCHITECTURE.md`, `docs/DEPLOY-HOST.md`, `docs/SANDBOX-TESTING.md`, `docs/OPERATIONS.md`, `docs/SECRETS.md`, `docs/FSX-LAYOUT.md`, `docs/DNS-SETUP.md`, `docs/SSL-MANAGEMENT.md`, plus 15 files under `docs/memory/`. None cover DB-only restore.

## Gaps summary
1. No `db-migrate-all` wrapper — operator chains `dump-mysql`/`restore-mysql`/`run-pgloader` by hand.
2. `migration/README.md` referenced by Makefile but does not exist.
3. `docs/MIGRATION.md` is pre-consolidation and stale.
4. No maintenance-mode helper — must do manually via ssm-deploy-host + drush.
5. No `mysql-env` wrapper for the scratch MariaDB scratch DB.
6. No preflight that queries `config`/`key_value`/enabled modules on either scratch MariaDB or target Postgres.
7. No `make rds-snapshot` (or equivalent) target; sandbox `BackupRetentionDays=1` gives a very thin PITR safety net.
8. No `.preflight` target in migration/Makefile checks deploy-host readiness or that `restore-mysql` prereqs (mariadb-server, pgloader) are installed — those checks live inside the shell scripts, so failures show up only after invocation.

Plan mode is active — I have not written a plan file. Say the word and I'll persist this as `/Users/kurtvanderwater/.claude/plans/zesty-plotting-squid-agent-aae2652b583868389.md`, or refine sections first.</result>
<usage><subagent_tokens>104274</subagent_tokens><tool_uses>43</tool_uses><duration_ms>292457</duration_ms></usage>
</task-notification>
```

---

### 14:11:16 - File Modified (Write)

- `/Users/kurtvanderwater/.claude/plans/zesty-plotting-squid.md`

---

### 14:13:06 - Response

Explore agent digging into install-drupal + current sandbox state. Once it reports, I'll revise the plan to reflect:

- **Skip Phase 1** (dump already done, prod frozen — Zac on vacation)
- **Phase 0 A → DB-level rename** (your preference over AWS snapshot — simpler rollback since we can just rename back)
- **Whatever `install-drupal` needs to happen** for /var/www/drupal codebase to exist before pgloader loads DB
- **Phase 5/6 adjustments** if cache clear / smoke test paths depend on install-drupal having run

---

### 14:13:06 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
