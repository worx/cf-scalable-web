# AI Prompt Log

**Date**: 2026-07-20 14:17:01
**Week**: 30
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:17:01 - User Prompt

```
Group A. I think we need to go ahead and go forward with all of those items.

When you're looking at number two, the S3-hosted PDF media, I believe we're going to need to add in a backup step that would back up that S3 media to here. It might be a direct pull without an intermediate, which would probably be preferable. I don't know how that would happen with our jump box and deploy host, etc., but if there's a sync so that we can run it again five days later and only pick up the changes, that would be marvelous.

We need to find where in the Drupal code it points to the file system so that we can change its pointer to the new bucket. I do believe the bucket already exists, and all we need to do is populate the bucket and point to the bucket. I believe number two is really simple: how do we populate the bucket, and how do we update Drupal to point to the bucket? Not sure there's much more to that.

Let's get started on our entire Group A. There's not much more we need to think about as far as that's concerned, I don't believe.
```

---

### 14:19:34 - Response

Kurt, before I plow in, quick alignment on scope — Group A has some ordering nuances worth naming:

## The subtle dependencies

**Item 1 (Salt→SSM cleanup)** — this is the one to be careful with. Removing the file fallback is a code change, but it's only SAFE after every PHP-FPM instance has been rebuilt with the new `configure-php.sh` (env[DRUPAL_HASH_SALT] line). Your current sandbox has the SSM secret but PHP-FPM still reads via file fallback. If I strip fallback and you `git pull`, sandbox WSODs until PHP is refreshed. **Recommend: keep this open, do it when you're ready for a coordinated PHP-FPM refresh.**

**Item 4 (generalize `_ssm-run-deploy-host.sh`)** — pure cleanup. Currently zero pain because we've only got two top-level dispatchers (`dispatch-db-backup.sh`, `clear-drupal-cache.sh`) using the inline-base64 pattern. Item 2's S3-media sync is another one. **Recommend: do this LAST, or after we've seen the shape of what item 2 produces.**

**Items 2 and 3 are safe to do now** in either order.

## Item 2 — the reality check

Your ask has three parts:
1. **Populate sandbox bucket from prod** — needs a cross-account permission. Two options:
   - **Prod bucket policy grants sandbox account read** (one-time, small IaC change on prod side). Then sandbox pulls directly: `aws --profile ZI-Sandbox s3 sync s3://prod-bucket/ s3://sandbox-bucket/`. Incremental — subsequent syncs only transfer changed objects. This is what "direct pull without intermediate" means.
   - **Jumpbox as ferry** — prod jumpbox reads prod bucket, writes to sandbox bucket (same cross-account write mechanism used by dump-mysql etc.). Also incremental via aws s3 sync. Adds a hop, but no prod bucket policy changes needed.
2. **Update Drupal to point at sandbox bucket** — install-drupal.sh writes flysystem config into settings.php, env-var driven from `DRUPAL_MEDIA_BUCKET` populated by refresh-env-config from SSM `/${ENV}/s3/media-bucket`.
3. **PHP-FPM needs `env[DRUPAL_MEDIA_BUCKET]`** — configure-php.sh update.

**Question before I code**: Option (a) bucket policy vs (b) jumpbox ferry? The BUCKET policy is simpler ops-wise for you (no jumpbox routing), but requires you to add a policy statement on prod's `drupal-media` bucket allowing sandbox account. Ferry keeps prod's ACL surface unchanged. Both work. Which do you prefer?

## Proposed execution order

1. **Item 3 first** (small, self-contained) — restore-codebase.sh with PRESERVE_FROM_BAK for `web/sites/default/settings.php` + `.installed`
2. **Item 2 second** (larger, higher value) — pending your (a) vs (b) answer above
3. **Item 4 third** — cleanup refactor after we see how item 2's dispatcher shakes out
4. **Item 1 deferred** — safe to land only after operational PHP-FPM rollout

Sound right? Which option (a) vs (b) for item 2's sync?

---

### 14:19:34 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
