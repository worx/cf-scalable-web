# AI Prompt Log

**Date**: 2026-07-15 19:42:48
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 19:42:48 - User Prompt

```
RE: trade-off | Runtime cost... while I still believe we should install both baked-in, there is no reason to leave mariadb-server running except when we need it. So I would alter the service after the install so that mariadb-server is not running and taking up that memory except when we need it? that is something that can be checked by any migration scripts to turn it on. I would NOT worry about turning it off except for manually.... 100M is only about 5% of base memory and only a possible issue during pgloader execution.
Which - Tickled another memory. If I remember, we ended up altering this from a 2 GB swap file to a 6 GB swap file because during our first run we got some kind of out-of-memory indication. We added extra swap space and altered the default chunk sizes from 1,000 to 256 or something along those lines. You should be able to find that in your history as well. From a going-forward standpoint, I suspect we could probably happily run this on a total of 4 GB of swap instead of 2 + 4 GB. When we did run it the second time, it wasn't much of a load on the system at all and still ran pretty fast.
```

---

### 19:43:26 - Response

While that fork extracts the numbers, quick sketch of the plan you can push back on:

## Design decisions I'll bake in (say stop if any of these are wrong)

**mariadb-server post-install:**
```bash
apt-get install -y mariadb-server
systemctl disable --now mariadb   # off by default; migration scripts start it
```
Migration scripts already need to `systemctl start mariadb` before using it — I'll add a helper (`ensure_mariadb_running`) to `migration/scripts/_common.sh` so every entry point calls it.

**Swap: 2 GB → 4 GB single file** (replacing the current 2 GB block, not stacking):
```bash
fallocate -l 4G /swapfile
# same chmod/mkswap/swapon/fstab pattern
```
`vm.swappiness=10` already there — no change.

**session-manager-plugin** as its own step near AWS CLI install, ARM64 .deb.

**pgloader tuning:**
- SBCL `dynamic-space-size` — waiting on fork for exact number, will pass via env or command flag
- Batch/prefetch — waiting on fork for exact numbers, will land in the migration/*.load files (not bootstrap.sh — those files are already tracked)

---

### 19:43:26 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
