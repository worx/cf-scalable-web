# AI Prompt Log

**Date**: 2026-05-20 14:47:04
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:47:04 - User Prompt

```
http://sandbox-alb-1006926792.us-east-1.elb.amazonaws.com fails -- 404
```

---

### 14:50:40 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/install-drupal.sh`

---

### 14:50:50 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/publish-drupal-vhost.sh`

---

### 14:51:04 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/cf-compute-nginx.yaml`

---

### 14:52:06 - Git Commit

**Commit:** `2f304c7eb99f`

**Message:**
```
fix(nginx): drupal vhost is default_server + owns /health; kill rogue port-80 health-check

Bit us 2026-05-20: `make smoke-test-drupal` passed (HTTP 200 with
Host: drupal-sandbox.test) but opening the ALB DNS in a browser
returned HTTP 404. Two server blocks on port 80:

  - /etc/nginx/conf.d/health-check.conf (written by cf-compute-nginx
    UserData at runtime): `listen 80 default_server; server_name _;`
    with only a `/health` location returning 200.
  - /etc/nginx/shared/sites-enabled/drupal.conf (written by
    install-drupal.sh + publish-drupal-vhost.sh on FSx):
    `listen 80; server_name drupal-sandbox.test;` with the full
    Drupal config.

For Host: drupal-sandbox.test, nginx matched the Drupal vhost → 200.
For Host: <anything else>, nginx fell through to the default_server
(health-check.conf), which only had /health configured → 404 for `/`.

Three-file fix:

1. scripts/deploy-host/install-drupal.sh (vhost template)
   - `listen 80` → `listen 80 default_server` so Drupal handles any
     unknown Host header (e.g., the ALB DNS, EC2 internal hostnames,
     curl tests).
   - New `location = /health { access_log off; return 200 'OK'; ... }`
     block so the ALB target-group health probe still gets its 200
     (without needing a separate default_server vhost).

2. scripts/publish-drupal-vhost.sh (same template, surgical fixup path)
   - Same two changes — the inline vhost body written by the surgical
     SSM dispatch matches install-drupal.sh's template.

3. cloudformation/cf-compute-nginx.yaml UserData
   - Stop writing /etc/nginx/conf.d/health-check.conf (no longer
     needed; Drupal vhost owns /health).
   - Defensively `rm -f` it so a previous-AMI legacy version doesn't
     conflict with the new Drupal default_server (would cause nginx
     to refuse start with "duplicate default server" error).

Net behavior after this commit:

  - ALB target health checks (Host: <internal>, path: /health) → 200 ✓
  - Smoke test (Host: drupal-sandbox.test, path: /)               → 200 ✓
  - Browser to ALB DNS (Host: <alb-dns>, path: /)                  → 200 ✓
  - Browser to ALB DNS (Host: <alb-dns>, path: /health)            → 200 ✓
  - Any other Host (catches typos, scan attempts, etc.)            → 200 ✓
    (Drupal then handles its own trusted_host_patterns check)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	cloudformation/cf-compute-nginx.yaml
M	scripts/deploy-host/install-drupal.sh
M	scripts/publish-drupal-vhost.sh
```

---

### 14:52:14 - Response

**All three paths green:**
- `Host: drupal-sandbox.test` → HTTP 200 (smoke test)
- `Host: <ALB-DNS>` → HTTP 200 (browser direct)
- `GET /health` → HTTP 200 with body "OK" (ALB target-group probe)

Open `http://sandbox-alb-1006926792.us-east-1.elb.amazonaws.com/` in your browser — should serve the Drupal welcome page now. Pushed `35a7aa5..2f304c7`.

---

### 14:52:14 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
