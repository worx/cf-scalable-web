# AI Prompt Log

**Date**: 2026-07-28 16:11:02
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 16:11:02 - User Prompt

```
fire it
```

---

### 16:11:13 - Git Commit

**Commit:** `28b8ce606a28`

**Message:**
```
docs(memory): capture 2 live-PDF-generation routes as Lambda candidates

Two Drupal routes 504 on sandbox for the same architectural reason:
they do synchronous heavy document generation on PHP-FPM workers.
Both need to move to Lambda (or a background queue) rather than being
"fixed" by growing the PHP instances.

The routes:
- `/zoning-info/conformance-report/{id}/latex-preview` — LaTeX
  generation via `LatexGenerator->renderTemplate()`. Also has an
  independent bug (`report.content.use` unpopulated).
- `/print/pdf/commerce_order/{id}` — entity_print (8.x-2.18) with
  DomPDF engine. UI label "View PDF" is misleading — it's a live
  generate operation, not a file lookup. Distinct from the
  pre-existing S3-served PDFs under `/_flysystem/s3/...` (which
  work fine).

Both fail with 504 despite healthy ASG/ALB/FPM (verified 2026-07-28).
The memory file documents why growing PHP instances is the wrong
fix (masks the underlying "sync heavy work on a web worker" shape),
and captures the right shape (enqueue + Lambda + presigned S3 URL).

## Files Modified

- docs/memory/pdf-generation-lambda-candidates.md — new
- docs/memory/README.md — index entry for the new file

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

**Files:**
```
M	docs/memory/README.md
A	docs/memory/pdf-generation-lambda-candidates.md
```

---

### 16:11:23 - Response

Pushed as `28b8ce6`.

## Back to the validation question

Still on the table from earlier — how to validate tonight's migration-script refactor:

- **Option A** — Just re-run `dump-codebase` + `restore-codebase` on the current sandbox (30-45 min, exercises the two refactored scripts without infrastructure churn)
- **Option B** — Full `make destroy-all` → `make deploy-all` → migration (2-3 hours, tests everything end-to-end)
- **Option C** — Defer B to fresh-morning session

Given the 504 investigation ate some time and it's late — my recommendation stands: **Option A tonight, Option B tomorrow when fresh.**

But also fair to ask: does the S3 media test earlier tonight (which involved manually ferrying the sibling packages via tarball + composer install) count as "validated enough" that you'd rather just call it a day and validate later? The scripts we wrote automate what you did manually — you already know the shape works. Option A would confirm the automation matches your manual steps.

What's your call?

---

### 16:11:23 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
