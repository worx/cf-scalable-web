# AI Prompt Log

**Date**: 2026-07-31 15:20:59
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 15:20:59 - User Prompt

```
<task-notification>
<task-id>a75e45645baeadea2</task-id>
<tool-use-id>toolu_012shRAKb9vv8f31eGrCHt9Q</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-kurtvanderwater-Work-dev-cf-scalable-drupal/aa44cb94-bd56-4bb5-879c-ab8e62c78e31/tasks/a75e45645baeadea2.output</output-file>
<status>completed</status>
<summary>Agent "Audit hardcoded 'drupal' references" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result># "drupal" Hardcoding Audit — cf-scalable-drupal

209 hits in top-level `Makefile` alone, ~77 SSM/secret-path hits across scripts+CFN. Categorized below with representative file:line anchors.

---

## 1. Filesystem paths (three roots + one local variant)

Canonical roots, all site-scoped, hardcoded, no `SITE` interpolation anywhere:
- `INSTALL_DIR="/var/www/drupal"` — `scripts/deploy-host/install-drupal.sh:71`
- `PRIVATE_DIR="/var/www/drupal-private"` — `install-drupal.sh:72`, `install-drupal.sh:491` (settings.php `file_private_path`)
- `CONFIG_DIR="/var/www/drupal-config"` — `install-drupal.sh:73`, `install-drupal.sh:492`

Consumers of these string-literal paths: `Makefile:1092, 1109, 1124, 1128`; `scripts/admin-login-url.sh:111,119`; `scripts/clear-drupal-cache.sh:67`; `scripts/install-drupal-remote.sh:101`; `scripts/verify-drupal-installed.sh:73,78,83`; `scripts/publish-drupal-vhost.sh:63`; `scripts/deploy-host/remove-drupal.sh:31-33`; `scripts/deploy-host/refresh-env-config:94` (`DRUPAL_PATH=/var/www/drupal`); `image-builder/configs/configure-php.sh:56,111`; migration `restore-codebase.sh:65,92`, `restore-files.sh:65`, `restore-private.sh:36,61`. All (a) — site-scoped, need per-site parameterization.

Local-only variant `/var/www/local/drupal` — `install-drupal-local.sh:23`, `Makefile:898-935`. (c) tooling name, iteration playground.

---

## 2. Database name + schema

- Postgres DB `drupal` — `cf-database.yaml:66` (DBName param default), `parameters/*.json:18`, `Makefile:1106,1147,1164,1178` (default arg), `install-drupal.sh:80`, `scripts/deploy-host/psql-env:45`, `refresh-env-config:72`, `clear-drupal-cache.sh:72,76`. The RDS *cluster* is (b) shared; the *db name inside* is (a) — Kurt already parameterizes `DRUPAL_DB_NAME` via env var in settings.php:456.
- Schema `zinew` — `install-drupal.sh:461,469` (`'schema' =&gt; getenv('DRUPAL_DB_SCHEMA') ?: 'zinew'`). Migration uses `zinew` at `migration/pgloader/zinew.load.tmpl:53`, `migration/Makefile:114,618-620`, `_ssm-run-jumpbox.sh:83`, `jumpbox/dump-mysql.sh:68-69`, `deploy-host/restore-mysql.sh:61-63`, `run-pgloader.sh:259` (`schema '$MIGRATION_DB_NAME'`). (a) site-scoped — different Drupal site → different schema.
- DB user `drupal_user` — `cf-app-drupal.yaml:79`, `install-drupal.sh:81,265`, `refresh-env-config:73`, `remove-drupal.sh:70,97,104`. (a).

---

## 3. CFN stack + resource names

- Stack name `${STACK_PREFIX}-app-drupal` where `STACK_PREFIX := cf-scalable-web-$(ENV)` — `Makefile:63,94`. Template `cf-app-drupal.yaml`, params `app-drupal-$(ENV).json`.
- Cross-stack exports: `${EnvironmentName}-drupal-admin-password-arn` (`cf-app-drupal.yaml:270`), `${EnvironmentName}-drupal-db-password-arn` (`:280`). No `ImportValue` consumers found — secrets are accessed by ARN pattern via IAM (`cf-iam.yaml:179` uses wildcard `${SecretNamePrefix}/drupal/*`). (a) stack name is site-scoped; (c) template file could remain named as-is.
- S3 bucket `${EnvironmentName}-drupal-media-${BucketSuffix}` — `cf-storage-s3.yaml:96,119`; IAM wildcard `${EnvironmentName}-drupal-media-*` — `cf-iam.yaml:214-215`; settings.php fallback `${ENV}-drupal-media-kv-worxco` — `install-drupal.sh:514`, `inject-flysystem-config.sh:89`. (a).

---

## 4. Makefile targets (top-level Makefile)

Site-scoped (a) — need `SITE=`: `install-drupal`, `install-drupal-full`, `install-drupal-remote`, `remove-drupal`, `reinstall-drupal`, `verify-drupal`, `clear-drupal-cache`, `publish-drupal-vhost`, `smoke-test-drupal`, `admin-login-url`, `deploy-app-drupal`, `destroy-app-drupal`, `verify-app-drupal`, `show-installed`, `create-installed`. Migration Makefile: `restore-codebase`, `restore-files`, `restore-private`, `migrate-db-all` (all currently pin sandbox+drupal).

DB-lifecycle targets accept `DB=` already: `db-backup`, `db-restore`, `list-db-backups`, `delete-db-backup` — `Makefile:1147,1158,1164,1178`. (b) infrastructure-scoped.

Local: `install-drupal-local`, `remove-drupal-local`, `reinstall-drupal-local`, `verify-drupal-local`, `serve-drupal-local`, `stop-drupal-local-server` — (c).

---

## 5. SSM parameter hierarchy — always `/${env}/drupal/&lt;key&gt;`

Owned by `cf-app-drupal.yaml:176-263`: `/db-name`, `/db-user`, `/site-name`, `/install-path`, `/admin-username`, `/admin-email`. Read by: `install-drupal.sh:99-103`, `configure-php.sh:94-98`, `Makefile:477,504,510,2793,2823,2884,2887`, `publish-drupal-vhost.sh:61`, `publish-dns.sh:43`, `unpublish-dns.sh:37`. All (a) — needs to become `/${env}/sites/${site}/*` or similar.

---

## 6. Secrets Manager — always `worxco/${env}/drupal/&lt;key&gt;`

- `worxco/${env}/drupal/admin-password` — `cf-app-drupal.yaml:124` (CFN `DrupalAdminPasswordSecret`)
- `worxco/${env}/drupal/db-password` — `:153` (`DrupalDBPasswordSecret`), read by `configure-php.sh:101`, `refresh-env-config:41`, `install-drupal.sh:87,219-237`, `remove-drupal.sh:57`
- `worxco/${env}/drupal/hash-salt` — created by `install-drupal.sh:89,381-396` (NOT in cf-app-drupal — script-owned; noted at `cf-app-drupal.yaml:45`), read by `refresh-env-config:49`, `configure-php.sh:115`

IAM wildcard `${SecretNamePrefix}/drupal/*` — `cf-iam.yaml:179`. All (a).

---

## 7. nginx vhost + PHP-FPM

- Single vhost file: `/etc/nginx/shared/sites-enabled/drupal.conf` (FSx path `/fsx/nginx/sites-enabled/drupal.conf`)
- Written by `install-drupal.sh:660-726` AND (duplicated) by `scripts/publish-drupal-vhost.sh:79-136`
- Nginx include pattern is already glob: `nginx.conf:108` `include /etc/nginx/shared/sites-enabled/*.conf` — (b) infra-shared, ready for N vhosts
- Access/error logs hardcoded `drupal_access.log` / `drupal_error.log` — `install-drupal.sh:678-679`, `publish-drupal-vhost.sh:93-94`. (a).
- PHP-FPM pool config env vars are single-site: `configure-php.sh:157-164` writes `env[DRUPAL_DB_*]` / `env[DRUPAL_SITE_NAME]` globally into pool. (a) — needs per-pool or per-site injection.

---

## 8. Migration — dump/restore + S3 keys

Jumpbox `DRUPAL_ROOT="/var/www/html/zoning_info_platform"` — `dump-codebase.sh:74`, `dump-files.sh:62`, `dump-private.sh:60` (source-side, site-specific to client). S3 keys: `dumps/drupal-codebase.tar.gz`, `dumps/drupal-files.tar.gz`, `dumps/drupal-private.tar.gz`, `dumps/zinew.sql` — set at `dump-codebase.sh:71`, `dump-files.sh:59`, `dump-private.sh:57`, `dump-mysql.sh:69` and mirrored at `restore-*.sh` deploy-host side. (a) — each site needs its own dump-key namespace.

Migration prod secret: `cf-migration/prod-mysql-zinew` — `migration/Makefile:114`, `cf-migration-secret.yaml:48`, `parameters/migration-secret.json:3`. (a) client-scoped.

`migrate-db-all` pins `ENV=sandbox` throughout (`migration/Makefile:820-903`). (a).

---

## 9. Env vars + config files

Env file path: `/etc/worxco/envs/&lt;env&gt;` — sourced by `db-backup.sh:53`, `db-restore.sh:46`, `db-delete-backup.sh:61`, `init-fsx-layout.sh:70`, `admin-login-url.sh:120`, `bootstrap.sh:225-261`, `run-pgloader.sh` (implied via `refresh-env-config`), `install-drupal.sh:477` (comment). File is written by `refresh-env-config:70-94` — env-scoped, currently emits one flat `DRUPAL_*` set. (b) env-scoped file; (a) contents need to become per-site (or per-site sub-files like `/etc/worxco/envs/&lt;env&gt;/&lt;site&gt;`).

Env-var namespace: `DRUPAL_DB_HOST/PORT/NAME/USER/PASS/SCHEMA`, `DRUPAL_HASH_SALT`, `DRUPAL_SITE_NAME`, `DRUPAL_PATH` — hardcoded consumers in `settings.php` heredoc (`install-drupal.sh:454-533`), `configure-php.sh:157-164`, `refresh-env-config:70-94`, `run-pgloader.sh:212,235-250,276,322-324`, `remove-drupal.sh:67-71`, `Makefile:1105-1107`, `db-backup.sh`. (a) — needs a prefix scheme or per-site injection (multiple PHP-FPM pools each with their own env[] block).

---

## Cross-cutting notes

- **cf-app-drupal.yaml is entirely per-site content mis-scoped as per-env** — every resource in it becomes N resources under multi-site.
- **The `drupal_user` role and `drupal` DB name assume 1:1 with cluster**; multi-site inside one RDS cluster means N users + N DBs (or 1 DB + N schemas).
- **Two independent writers of `drupal.conf`** (`install-drupal.sh:660` and `publish-drupal-vhost.sh:79`) — any per-site refactor must update both, or consolidate.
- **`refresh-env-config` emits a single flat env-var set** — the mechanism by which PHP-FPM workers know about *the* drupal site. This is the highest-leverage single file for multi-site.
- **Naming choice tokens** (target names like `install-drupal`, script names like `clear-drupal-cache.sh`, template file `cf-app-drupal.yaml`) are (c) — the *tool* is Drupal-centric; the *artifact it operates on* is what's site-scoped.</result>
<usage><subagent_tokens>105062</subagent_tokens><tool_uses>38</tool_uses><duration_ms>220364</duration_ms></usage>
</task-notification>
```

---

### 15:23:24 - Response

Plan agent working on the design synthesis. Will need Kurt's input on 1–2 key decisions after it returns (backward-compat for the existing "drupal" site vs rename to "zoning-info" is one; possibly directory-layout preference is another). Waiting on the notification.

---

### 15:23:24 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
