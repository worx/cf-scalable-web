# AI Prompt Log

**Date**: 2026-07-10 10:02:54
**Week**: 28
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 10:02:54 - User Prompt

```
1 - interactive - yes - but overridable with --confirm (or whatever we are currently using as a flag for this)
2 - Secrets Manager for prod MySQL - yes
3 - logs - I would prefer that they end up on S3 if possible... otherwise /var/log/worxco-migration/*.log is acceptable
4 - executing an aws-cli based terminate for the 2 8.3 instances takes no time, the new instances are up in under 5 - but - you're --refresh flag gives me both options. Let's go with that.
5 - yes
```

---

### 10:03:28 - Response

Locked in. Quick recap so tomorrow starts clean:

## Locked decisions

| # | Decision | Implementation note |
|---|----------|---------------------|
| 1 | Interactive prompts by default, `CONFIRM=yes` overrides | Same pattern as your existing destructive targets — one shell helper (`_confirm.sh`) sourced by all scripts, checks env var, prompts if unset |
| 2 | Secrets Manager for prod MySQL password | Small addition tomorrow: `cf-migration-secret.yaml` in prod account, jumpbox IAM role gets `secretsmanager:GetSecretValue` scoped to that specific ARN |
| 3 | Logs to S3 (with local fallback) | Each script writes to `/var/log/worxco-migration/<script>-<timestamp>.log` locally *and* on completion uploads to `s3://<migration-bucket>/logs/YYYY-MM-DD/<script>-<timestamp>.log`. If S3 fails, local copy is the backup. |
| 4 | ASG instance refresh opt-in via `REFRESH=yes` | Default is the fast direct terminate (5 min); `REFRESH=yes` triggers proper AWS instance-refresh (5-10 min but cleaner) |
| 5 | `DRY_RUN=yes` mode | Every destructive command wrapped in a `run_or_echo` shell function that respects the flag |

## Implications for tomorrow's start

Two small additions to the "what to build first" order:

- **Step 1a (before skeleton):** Create shared shell helper `_common.sh` with the four idempotency helpers we now know we need: `confirm_or_exit`, `run_or_echo`, `log_and_upload`, `require_env`. Every script sources this. ~20 min.
- **Step 1b (before jumpbox scripts):** Add `cf-migration-secret.yaml` CFN template + `make deploy-migration-secret` target. Deploy it, populate the secret value manually via AWS console (one-time). ~20 min.

Everything else in the plan stays as written.

## One tiny follow-up you can chew on overnight (or ignore)

For the `CONFIRM=yes` flag pattern — the widely used Makefile idiom is `CONFIRM=yes` as an env var. But your existing Makefile might use a different convention. Would be worth a quick `grep -E 'CONFIRM|FORCE|YES|APPLY' Makefile` in the morning to match whatever pattern is already there. If nothing, `CONFIRM=yes` is clean.

## Go home

Big migration done. Plan is captured, decisions are locked, tomorrow's first two hours are laid out. Sleep well.

---

### 10:03:28 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
