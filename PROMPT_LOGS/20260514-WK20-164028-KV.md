# AI Prompt Log

**Date**: 2026-05-14 16:40:28
**Week**: 20
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 16:40:28 - User Prompt

```
all 4 terminations have finished 3 of 4 are 3/3 and the last one is initializing (83 on 1a)
```

---

### 16:42:32 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/image-builder/configs/configure-php.sh`

---

### 16:51:19 - Response

Even with **all marker blocks stripped and FPM restarted clean on all 4 boxes**, every curl still gives 502. That's a major signal — **the env vars and the `~` issue are NOT the actual cause of the 502s.** It's something else, something the marker-block work was masking.

At this point I think it's right to stop. We've been pulling on threads for hours and the answer is buried somewhere we haven't looked yet. **Documented state for tomorrow:**

**Confirmed-real bugs we've found tonight that need real fixes:**

1. **IAM stack drift** — cf-iam template change for `worxco/<env>/drupal/*` was never deployed; manual `make deploy-iam` fixed it. *Followup: stack-drift detection or pre-deploy check.*

2. **`~`/backtick/`*` in DB password** — cf-app-drupal `ExcludeCharacters` doesn't exclude them. *Followup: tighten ExcludeCharacters, rotate the secret, **and** update the postgres user's password to match.*

3. **`configure-php.sh` env values unquoted** — works for tame passwords, breaks the moment a metachar lands. *Followup: defensive quoting (we already patched this in the canonical script locally; needs S3 sync + AMI rebuild OR runtime push).*

4. **PHPInstanceRole has no read on `image-builder` bucket** — can't refresh `/opt/worxco/configure-php.sh` from S3 at runtime. *Followup: add image-builder bucket to S3Access, OR commit that scripts only ever update via AMI rebuilds.*

5. **NLB `preserve_client_ip`** — toggled, found not causal. Leave at original setting (or document the choice).

**The actual 502 mystery is STILL not solved.** Workers are alive, config-test passes (after quoting), TCP works at every layer — but FastCGI requests are closed mid-response. We never got a clean run of the Python FastCGI test on a fresh-clean box. That's the FIRST thing to try in the morning, with a properly-escaped Python script.

**Suggested order for tomorrow:**

1. **Fresh clean box** — terminate one PHP box, let ASG bring it up. Don't touch it.
2. **Local FastCGI poke** (working Python script with no null-byte issues) — does FPM respond to a valid request *from the same box*? If yes, the issue is in nginx→NLB→FPM path. If no, FPM/AMI itself is broken.
3. From the answer to #2, the diagnostic path forks cleanly.

You can either leave the ASGs running overnight (about $1/hour for this fleet) or scale them to zero with `make pause-compute ENV=sandbox` and resume with `make resume-compute ENV=sandbox` in the morning. Up to you.

Get some rest. Want me to draft commits for the four confirmed-real fixes (#1, #2, #3, #4) so they're queued up to deploy tomorrow morning before we resume debugging?

---

### 16:51:19 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
