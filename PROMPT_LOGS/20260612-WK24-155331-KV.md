# AI Prompt Log

**Date**: 2026-06-12 15:53:31
**Week**: 24
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 15:53:31 - User Prompt

```
commit and push
```

---

### 15:53:39 - Git Commit

**Commit:** `ba2f8c348411`

**Message:**
```
feat(migration): cross-account jumpbox + S3 ferry for prod → sandbox migration

Two CloudFormation stacks deployed to two different AWS accounts via
explicit --profile flags in dedicated Makefile targets:

  cloudformation/migration/cf-migration-bucket.yaml   (SANDBOX account)
    - S3 bucket for ferrying mysqldumps + Drupal codebase tarballs
    - Cross-account bucket policy: PutObject/ListBucket allowed from
      prod account 978068244875, restricted via aws:PrincipalArn
      condition to ONLY roles matching cf-migration-jumpbox-* in prod
    - 30-day lifecycle expiration on objects (it's temporary)
    - AES256 encryption, all-public-access-blocked

  cloudformation/migration/cf-migration-jumpbox.yaml  (PROD account)
    - t4g.micro Ubuntu 24.04 ARM64 in subnet-85e938dc
      (same subnet as React2 → free SSH via WebSSH SG's
       172.31.16.0/20 rule)
      (in vpc-7fbd291a → free MySQL access to d8demo-cluster via
       RDS SG's 172.31.0.0/16 rule)
    - IAM role: AmazonSSMManagedInstanceCore + explicit S3 perms
      scoped to the specific sandbox bucket ARN
    - SG with ZERO inbound rules — SSM Agent works over outbound TLS
    - KeyPairName=WorxZoning (operator-provided, already in prod
      account) for emergency direct SSH if SSM ever fails
    - UserData installs mysql-client-core-8.0, pigz, rsync, AWS CLI v2
    - 8 GB gp3 root volume

Zero modifications to prod security groups required. The existing
WebSSH (sg-7d55a019) and d8zdb (sg-69d41f18) SG rules already allow
the access patterns the jumpbox needs.

Makefile targets:
  make deploy-migration-bucket     - SANDBOX (--profile ZI-Sandbox)
  make destroy-migration-bucket    - SANDBOX
  make deploy-migration-jumpbox    - PROD (--profile ZoningInfoAdmin)
  make destroy-migration-jumpbox   - PROD
  make ssm-migration-jumpbox       - opens SSM session on jumpbox
  make migration-jumpbox-pubkey    - generates + prints ed25519 key
                                     for operator to add to React2's
                                     authorized_keys

Documentation:
  docs/MIGRATION.md - full operator runbook covering bucket+jumpbox
  deploy, pubkey authorization on React2, mysqldump→S3 streaming,
  codebase rsync+tar→S3 streaming, sandbox-side pgloader conversion,
  codebase deploy onto FSx, smoke tests, and cleanup. Includes the
  "things to watch for" list (mysqldump password handling, Drupal
  version drift, charset/collation, S3 lifecycle).

Source details (from operator):
  - Drupal codebase server: React2 (i-044329095f5c3a821)
  - Source RDS: d8demo-cluster.cluster-cmbywichztfj.us-east-1.rds.amazonaws.com
  - Source DB: zinew, user worxco, password operator-supplied at runtime

Lifecycle: both stacks are TEMPORARY. Destroy when migration is done.
Bucket policy's role-name pattern (cf-migration-jumpbox-*) means even
if the jumpbox is deleted, no other prod role can write to the bucket.

Files Modified:
- cloudformation/migration/cf-migration-bucket.yaml (created)
- cloudformation/migration/cf-migration-jumpbox.yaml (created)
- cloudformation/parameters/migration-bucket-sandbox.json (created)
- cloudformation/parameters/migration-jumpbox.json (created)
- Makefile (migration target group + help text + .PHONY)
- docs/MIGRATION.md (created)

Tests: cfn-lint clean on both templates; jq empty validates params;
       make -n parses clean on all 6 new targets.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	Makefile
A	cloudformation/migration/cf-migration-bucket.yaml
A	cloudformation/migration/cf-migration-jumpbox.yaml
A	cloudformation/parameters/migration-bucket-sandbox.json
A	cloudformation/parameters/migration-jumpbox.json
A	docs/MIGRATION.md
```

---

### 15:53:50 - Response

Pushed as `ba2f8c3`. The migration tooling is live in the repo.

## Next steps when you're ready

```bash
# 1. Sandbox-side bucket (~30 sec)
make deploy-migration-bucket

# 2. Prod-side jumpbox (~2 min total: CFN ~1 min + UserData ~2 min)
make deploy-migration-jumpbox

# 3. Get jumpbox SSH pubkey to authorize on React2
make migration-jumpbox-pubkey
# Copy that line; paste into React2's authorized_keys via your existing access

# 4. Verify connectivity from inside the jumpbox
make ssm-migration-jumpbox
# Once you're in the shell, run the three verification commands from
# docs/MIGRATION.md §3 (MySQL, SSH to React2, S3 write)
```

When you're set up and ready to run the actual dump, ping me and we'll go through it interactively — that's when the prod DB password gets prompted for, and we can adjust the `mysqldump` invocation if anything surprising surfaces (charset issues, big-table memory pressure, etc.).

**One thing to keep in mind:** the IAM role name is hard-coded as `cf-migration-jumpbox-role` (so the sandbox bucket policy can reference the pattern). If a previous attempt left a stale role with that name in prod, the stack create will fail with an "EntityAlreadyExists" error — let me know and we can clean up with `aws --profile ZoningInfoAdmin iam delete-role` (after detaching policies).

Standing by.

---

### 15:53:50 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
