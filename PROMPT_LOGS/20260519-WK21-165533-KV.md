# AI Prompt Log

**Date**: 2026-05-19 16:55:33
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 16:55:33 - User Prompt

```
➜  cf-scalable-drupal git:(main) ✗ make publish-drupal-vhost ENV=sandbox                                                          <aws:ZI-Sandbox> <region:us-east-1>
make clear-drupal-cache ENV=sandbox
make smoke-test-drupal ENV=sandbox

Deploy-host: i-0b1c7366e47abd72c
Publishing Drupal vhost for env=sandbox via SSM...
CommandId: ce9a06b3-9784-496a-9d9f-d3f73a912a70
Waiting. Success.

Site name: drupal-sandbox.test
Doc root:  /var/www/drupal/web
Writing:   /var/www/nginx/sites-enabled/drupal.conf
✓ Wrote vhost. Verify with:
  cat /var/www/nginx/sites-enabled/drupal.conf
-rw-r--r-- 1 root root 1144 May 19 21:53 /var/www/nginx/sites-enabled/drupal.conf


✓ Drupal vhost published. Reload the fleet: make reload-nginx ENV=sandbox
Deploy-host: i-0b1c7366e47abd72c
Clearing Drupal caches for env=sandbox...
CommandId: 1f2c96d8-73c2-417d-a58d-0bea78d21390
Waiting for completion.. Success.

=== 1. Wipe compiled-container cache on FSx ===
  cleared /var/www/drupal/web/sites/default/files/php/
=== 2. TRUNCATE every cache_* table in the drupal DB ===
  Truncating: cache_bootstrap, cache_container, cache_config, cache_default, cachetags, cache_discovery, cache_data, cache_entity
TRUNCATE TABLE
=== 3. Done ===
Drupal will rebuild its compiled container on the next request.
If PHP-FPM workers have stale OPcache, also run:
  make restart-php-fpm ENV=sandbox

Smoke-testing Drupal at ENV=sandbox
  ALB:  sandbox-alb-2011302727.us-east-1.elb.amazonaws.com
  Host: drupal-sandbox.test
  ✓ Drupal returned HTTP 200
<!DOCTYPE html>
<html lang="en" dir="ltr" style="--color--primary-hue:202;--color--primary-saturation:79%;--color--primary-lightness:50">
  <head>

---

Looks like it is working - whew! 
so, have we captured all the errors for the day, converted them into settings/configs/scripts/etc into our CloudFormation Stacks and associated files?
```

---

### 16:56:40 - Response

🎉 **HTTP 200!** End-to-end working Drupal on a fresh deploy-allX. First time today.

Honest accounting on what's codified vs not:

**Codified (committed + pushed, will work next time):**
1. ✓ `install-drupal-remote.sh` rewrite — wait for marker, drop 30-min timeout cap, no inline bootstrap (commit `e205e1d`)
2. ✓ composer needs HOME + COMPOSER_ALLOW_SUPERUSER + partial-install wipe (commit `04c84ab`)
3. ✓ drush URL-encoding removed — pass raw password (commit `1ec0de2`)
4. ✓ install-drupal.sh writes nginx vhost; new `reload-nginx`; auto-called from install-drupal-remote (commit `81b5ccd`)
5. ✓ `publish-drupal-vhost` fixup target for already-installed envs (commit `6f58235`)
6. ✓ settings.php paths — drop env prefix (commit `417f9f0`)
7. ✓ Validate-once + compress output (commit `803eaa2`)

**NOT codified — will recur on next destroy-all + deploy-allX:**

8. ✗ **`/fsx/nginx` doesn't exist when nginx instances boot.** `configure-nginx.sh` mount fails with `set -e`, script bails BEFORE writing `/etc/nginx/conf.d/upstream-php.conf`. We hand-mounted + hand-wrote the upstream config today. Will recur.

9. ✗ **PHP instances launch before RDS exists in Phase 5.** `configure-php.sh` reads `/sandbox/rds/endpoint` from SSM, doesn't find it (cf-database not deployed yet in Phase 4 order), skips writing the env[] block. We hand-ran configure-php.sh later. Will recur.

Both are **phase-ordering bugs in `deploy-allX`** and both are blockers — next clean run hits them again. The fixes are tightly related:

- **Reorder deploy-allX**: data layer (RDS + cache) and **FSx layout init** (mkdir `/fsx/nginx/sites-enabled` via deploy-host SSM) need to complete *before* `deploy-compute`. The AMI build phase (~20 min) is the long pole, so data-layer + FSx-init can run in parallel with it for free wall-clock.

Plus existing TODOs from earlier today (destroy-host peering dependency, build-amis-if-needed, admin SSH key registry) are captured in `TODO.md` but not implemented.

Want me to make the phase-ordering fix the next thing — that's the only remaining item that prevents `destroy-all && deploy-allX` from being a reliable green-field "single command from zero to serving Drupal"?

---

### 16:56:40 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
