# AI Prompt Log

**Date**: 2026-05-24 10:21:34
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 10:21:34 - User Prompt

```
started deploy-allXX - IB failed - This was run on my mac - so logs are here in /tmp/parallel-deploy.52457

[T+ 1m54s]  deploy-all-parallel  (1/14 done)
  > vpc VPC + subnets + security groups          S: 0m00s  E:  -    ( 1m54s)  running
  > iam IAM roles + instance profiles            S: 0m00s  E:  -    ( 1m54s)  running
  ✓ app app-drupal (SSM params + secrets)        S: 0m00s  E: 0m02s  ( 0m02s)
  ✗  ib image-builder stack + S3 configs         S: 0m00s  E: 0m02s  ( 0m02s)  exit=2 log:ib.log
  ! ami AMI bake (nginx/php74/php83)                                  (never started)
  ! str storage (FSx + S3 buckets)                                    (never started)
  ! per peering (deploy-host <-> workload)                            (never started)
  !  db database (RDS PostgreSQL)                                     (never started)
  ! cch cache (ElastiCache Valkey)                                    (never started)
  ! fsx init FSx layout (/fsx/www + /fsx/nginx)                       (never started)
  ! alb compute ALB + target group                                    (never started)
  ! nlb compute NLB + target groups                                   (never started)
  ! cnx nginx ASG + instances                                         (never started)
  ! cph PHP ASGs (74 + 83) + instances                                (never started)
```

---

### 10:23:14 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-all-parallel.py`

---

### 10:23:42 - Git Commit

**Commit:** `a1059db61d58`

**Message:**
```
fix(deploy-allXX): ib depends on str (S3 bucket SSM param)

Caught on Kurt's first deploy-allXX run: ib failed at T+0m02s with
"Image builder bucket not found in SSM (deploy storage stack first)".

Root cause: cf-image-builder.yaml itself has no !ImportValue references
— it uses the default VPC for builds, owns its own IAM role, S3
bucket name is parameterized. So at the CFN-template level, image-
builder is independent of storage.

BUT the deploy-image-builder + upload-build-configs make targets both
read /<env>/s3/image-builder-bucket from SSM as a precheck, and that
SSM parameter is written by cf-storage.yaml. So the operational dep
is: storage → image-builder, even though the CFN dep is "none."

Comment in the TRACKS entry now flags this as a deferred optimization:
splitting cf-storage into separate cf-storage-s3 (no VPC dep, fast)
and cf-storage-fsx (needs VPC) would let ib start in parallel with
vpc — saving ~5 min off the critical path. Not doing that today
because it's a stack refactor.

New critical path: vpc (2) → str (5) → ib (3) → ami (25) → cnx/cph
(3) = ~38 min. Was ~33 in the prior (incorrect) estimate. Still
~7-12 min faster than sequential deploy-allX (~45-50 min).

Recovery from Kurt's interrupted run: re-running `make deploy-allXX
ENV=sandbox` picks up where it left off — vpc/iam/app CFN stacks
already created (idempotent on retry); the rest deploy as designed.

Files Modified:
- scripts/deploy-all-parallel.py (ib's deps: [] → ["str"], with comment
  explaining why and what to refactor later)

Tests: python ast.parse passes.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	scripts/deploy-all-parallel.py
```

---

### 10:23:56 - Response

Pushed as `a1059db`. **Re-run picks up cleanly:**

```bash
git pull origin main
make deploy-allXX ENV=sandbox
```

What'll happen:
- `vpc`, `iam`, `app` — CFN sees existing stacks, idempotent no-op (returns in ~5-10s each)
- `str` — starts as soon as `vpc` confirms (now ~5 min after start), creates FSx + S3 buckets + writes the SSM bucket param
- `ib` — starts after `str` completes (~7-8 min after deploy start), now successful
- `ami` — starts after `ib` (~10-11 min), runs ~25 min
- Everything else — runs in parallel under the AMI shadow
- `cnx` + `cph` — start once AMIs ready

**Updated critical path: ~38 min** (down from sequential ~45-50, up from my prior optimistic ~33). The 5-min storage step is now on the critical path because image-builder operationally depends on the S3 bucket SSM parameter.

**Deferred future optimization** captured in the code comment: split `cf-storage.yaml` into `cf-storage-s3` (no VPC dep, ~30 sec) and `cf-storage-fsx` (needs VPC, ~5 min). Then `ib` could depend only on the fast S3 stack, recovering those 5 min. Stack refactor; not blocking; logged.

**Lesson from this run for the audit log:** my dependency analysis covered CFN-level imports (`!ImportValue`) but missed *operational* dependencies enforced by Makefile prechecks reading SSM. Worth being suspicious of any `make` target that reads SSM as a precondition — those are hidden deps on whichever target writes the SSM param. Already grepped and confirmed image-builder is the only target with this pattern; no other hidden surprises lurking.

Standing by for the re-run result.

---

### 10:23:56 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
