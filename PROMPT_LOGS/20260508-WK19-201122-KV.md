# AI Prompt Log

**Date**: 2026-05-08 20:11:22
**Week**: 19
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 20:11:22 - User Prompt

```
ok - on deploy-host I ran the git pull, then the 5 make commands.... all finished without error.
```

---

### 20:15:23 - Response

Confirmed — `build-amis` (line 2018) and `update-ami-param` (line 2030) interact badly:

- **`build-amis`**: just calls `aws imagebuilder start-image-pipeline-execution` and exits. No waiter.
- **`update-ami-param`**: filters by `state=available` and picks the most recent. While new AMIs are still `BUILDING`, this returns the *previous* build (the broken sandbox-4 ones tonight, or sandbox-3 morning ones — whichever was the last AVAILABLE).

So the sequence races: pipelines started but not done → SSM still points at old AMIs → compute ASG sees no template change → no rollover.

**Right now:** the three new pipelines are 3-4 min in. Historical builds took ~10 min each (parallel). They'll be AVAILABLE around 01:18 UTC.

**Path forward** — two-step:

```bash
# Watch the build (this will exit when all three reach AVAILABLE or one FAILS)
watch -n 30 'aws imagebuilder list-image-pipelines \
  --query "imagePipelineList[?contains(name,\`sandbox\`)].[name,status]" \
  --output table; aws ec2 describe-images --owners self \
  --filters "Name=tag:Environment,Values=sandbox" \
    "Name=creationDate,Values=2026-05-09*" \
  --query "Images[*].[Name,State,CreationDate]" --output table'

# When 3 fresh sandbox-N AMIs (with 2026-05-09 timestamps) appear AVAILABLE:
make deploy-ami-params ENV=sandbox    # this time SSM gets new AMI IDs
make deploy-compute ENV=sandbox       # ASG rolls onto new AMIs
```

Then re-run the SSM probe to confirm `/opt/worxco/configure-php.sh` is the new 172-line script and `pool.d/extra/` has the two `.conf` files.

**Side suggestion:** want me to fix `build-amis` to actually wait for the pipelines before returning? It's ~30 lines of bash polling `aws imagebuilder get-image --image-build-version-arn ...` until each is AVAILABLE/FAILED. Would prevent this race for good. Could split into separate commit either now or after we confirm tonight's deploy actually works end-to-end.

---

### 20:15:23 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
