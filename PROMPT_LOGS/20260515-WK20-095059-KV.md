# AI Prompt Log

**Date**: 2026-05-15 09:50:59
**Week**: 20
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 09:50:59 - User Prompt

```
It's a new day, so where did we leave things last night with all this unresolved stuff that we need to get fixed? Do we need to kill the whole system and rebuild from scratch? Is this something we can just rebuild the PHP servers? What do we need to do?
```

---

### 09:51:54 - Response

Good morning. Quick recap of where we left it:

**State of the world right now:**
- 4 commits pushed last night (in `db1c69c..fab608f`), nothing deployed yet.
- 4 PHP boxes are running with: stale AMI configure-php.sh (no quoting), the OLD DB password (`X~pz...` with the breaking `~`), and last night's ad-hoc fixes (stripped marker blocks, manually restarted FPM).
- Databases, cache, VPC, deploy-host, nginx — all healthy and untouched.
- **The 502 mystery is still unresolved.** Even after stripping marker blocks and restarting FPM, requests still 502'd. We never got a clean baseline.

**No, you do NOT need to rebuild the whole system.** Only the PHP fleet needs to be rolled. Everything else (RDS, FSx, Valkey, nginx, ALB, NLB, IAM stack itself) stays in place. The PHP AMI is the thing that changes; rolling the ASG instances picks it up.

**The deploy sequence** (~30-40 min total, mostly waiting on AMI builds):

```bash
# On deploy-host
cd ~/projects/cf-scalable-web
git pull   # pulls commits a8fba8d, 5073dc1, 526e376, fab608f

# 1. IAM — adds drupal/* secret read AND image-builder bucket read (safe, no instance restart)
make deploy-iam ENV=sandbox

# 2. cf-app-drupal — tightens ExcludeCharacters in the template
#    NOTE: this does NOT rotate the existing password automatically
make deploy-app-drupal ENV=sandbox

# 3. ROTATE the DB password manually (because UpdateReplacePolicy: Retain keeps the existing X~ value):
NEW_PASS=$(aws secretsmanager get-random-password --password-length 32 \
  --exclude-characters '"@/\\'"'"'#%&+:;=?[]~$`<>*|^!(){}' \
  --query RandomPassword --output text)
echo "New password: $NEW_PASS"
# Sanity check: should be 32 chars, all safe
echo "$NEW_PASS" | grep -oE '[~$`<>*|^!(){}]' && echo "BAD! has a metachar" || echo "OK clean"

aws secretsmanager update-secret \
  --secret-id worxco/sandbox/drupal/db-password \
  --secret-string "$NEW_PASS"

# 4. Update postgres user to match (use psql-env helper on deploy-host):
psql-env <<SQL
ALTER USER drupal_user WITH PASSWORD '$NEW_PASS';
SQL
unset NEW_PASS

# 5. Sync the new configure-php.sh to S3, register new components, rebuild PHP AMIs
make upload-build-configs ENV=sandbox
make deploy-image-builder ENV=sandbox
make build-ami-php74 ENV=sandbox
make build-ami-php83 ENV=sandbox

# 6. Wait ~10 min for both pipelines to reach AVAILABLE. Reuse the wait script:
/tmp/wait-amis.sh   # if it survived; otherwise re-create from yesterday's snippet

# 7. Push new AMI IDs to SSM, update launch templates, roll instances
make update-ami-param ENV=sandbox PIPELINE=php74
make update-ami-param ENV=sandbox PIPELINE=php83
make deploy-compute ENV=sandbox
for ASG in sandbox-php74-asg sandbox-php83-asg; do
  aws autoscaling start-instance-refresh --auto-scaling-group-name $ASG \
    --strategy Rolling \
    --preferences '{"MinHealthyPercentage":50,"InstanceWarmup":120,"SkipMatching":true}'
done
```

**After it finishes, test `_test.php` again.** Three outcomes:

1. **`PHP_OK` + env vars + `PG_OK`** — the `~` really was the whole story; debug yesterday made it look stuck because we never got a clean baseline. Move on to drush + browser-based Drupal testing.

2. **`PHP_OK` but `PG_FAIL`** — DB credentials issue. Re-verify the rotation + ALTER USER step.

3. **Still 502** — the FastCGI mystery is real. With a known-clean baseline (proper IAM, proper passwords, proper AMI, fresh boxes) we can debug it without the noise. The first thing I'd try then is the (properly-escaped) Python FastCGI probe directly from the PHP box.

Want me to walk you through step-by-step, or just stand by while you run it and you ping me at each gate?

---

### 09:51:54 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
