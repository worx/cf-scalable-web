# AI Prompt Log

**Date**: 2026-07-13 17:25:01
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 17:25:01 - User Prompt

```
Okay, so this is writing things to the S3 bucket. I peeked at the UI for that bucket and looked at management. There is a lifecycle rule there named ExpireOrchestrationScripts. It is set for 7 days. But that is where we are storing scripts we use like dump-mysql.sh and the rest of them. should they really dissapear after 7 days? Apparently, one of my brain cells is misfiring because I don't think that is right?
```

---

### 17:25:57 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/cloudformation/cf-migration-bucket.yaml`

---

### 17:26:17 - Response

Down to 4 rules. `scripts/` prefix now has no lifecycle rule — objects there persist indefinitely.

## Final lifecycle picture

```
s3://sandbox-migration-kv-worxco/
├── scripts/                                 ← NO EXPIRATION (needed by scheduler + UserData)
├── dumps/                                   ← 30 days (migration data ferry)
├── logs/                                    ← 30 days (audit trail)
└── config/
    ├── jumpbox-latest.tar.gz                ← NO EXPIRATION (latest pointer)
    └── jumpbox-archive/                     ← 90 days (state backup history)
```

Every automated dependency now has permanent access to what it needs.

## To apply the fix

```bash
cd migration
make deploy-bucket
```

Same as last time — in-place bucket update, no data touched, existing objects just get re-evaluated against the new rules (which for `scripts/` means "no expiration").

## Also worth flagging — the scripts still work TODAY

The scripts you uploaded during our session are still well within the 7-day window. Your first upload was earlier today (2026-07-13). So even without applying the fix immediately, the scheduler at 02:00 tomorrow morning will find its scripts intact. But applying the fix at your leisure means you don't have to think about it again.

## Nice catch

This is exactly the sort of thing that gets caught by "wait, why is that number what it is?" and NOT by testing (because the test period is shorter than the expiration). Lifecycle rules are one of those areas where the failure mode is delayed and silent — the worst kind. You just saved us from a nightly-backup failure some Tuesday in August when nobody's paying attention.

---

### 17:26:17 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
