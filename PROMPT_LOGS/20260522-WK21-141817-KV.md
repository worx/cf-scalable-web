# AI Prompt Log

**Date**: 2026-05-22 14:18:17
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:18:17 - User Prompt

```
➜  cf-scalable-drupal git:(main) ✗ IDS=$(aws --profile ZI-Sandbox autoscaling describe-auto-scaling-groups \                      <aws:ZI-Sandbox> <region:us-east-1>
  --auto-scaling-group-names sandbox-nginx-asg \
  --query 'AutoScalingGroups[0].Instances[?LifecycleState==`InService` && HealthStatus==`Healthy`].InstanceId' \
  --output text)
echo "Targeting: $IDS"
aws --profile ZI-Sandbox ssm send-command \
  --instance-ids $IDS \
  --document-name AWS-RunShellScript \
  --parameters 'commands=["sudo nginx -t","sudo nginx -s reload","systemctl is-active nginx"]'

Targeting: i-02130b4aa814a9c1f    i-0b59bdd31af2fa1a7

aws: [ERROR]: An error occurred (ValidationException) when calling the SendCommand operation: 1 validation error detected: Value '[i-02130b4aa814a9c1f    i-0b59bdd31af2fa1a7]' at 'instanceIds' failed to satisfy constraint: Member must satisfy constraint: [Member must have length less than or equal to 20, Member must have length greater than or equal to 10, Member must satisfy regular expression pattern: (^i-(\w{8}|\w{17})$)|(^mi-\w{17}$), Member must not be null]
```

---

### 14:19:03 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/shell-portability.md`

---

### 14:19:14 - Git Commit

**Commit:** `592a15e3c272`

**Message:**
```
docs(memory): expand shell-portability — word-splitting is the silent footgun

Repeated the same class of error within 24 hours: yesterday's incident
was \${V^^} (Bash-4 expansion → zsh "bad substitution"); today's was
\$IDS unquoted in send-command --instance-ids (bash word-splits, zsh
doesn't, AWS rejects the concatenated mega-string with a misleading
ValidationException).

Updates:
- New section "Word-splitting (the silent footgun)" — explains the
  failure mode, shows the failing pattern with both shells' behavior
  side-by-side, lists three fixes with array-form recommended.
- New section "Scripts vs paste-snippets — different rules" — clarifies
  that #!/bin/bash scripts can use bash-isms safely, but paste-in-zsh
  snippets cannot. Cites reload-nginx.sh / restart-php-fpm.sh as the
  scripts-OK case (today's underlying scripts were not buggy; the
  one-shot snippet sent to Kurt's terminal was).

Files Modified:
- docs/memory/shell-portability.md

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	docs/memory/shell-portability.md
```

---

### 14:19:19 - Response

Pushed as `592a15e`. Memory now has two concrete past incidents (`${V^^}` yesterday, unquoted `$IDS` today) as red flags I'll see at the top of future sessions.

**For right now:** paste the array-form snippet above — it'll succeed and your nginx fleet will reload, then `make smoke-test-drupal ENV=sandbox` should pass.

---

### 14:19:19 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
