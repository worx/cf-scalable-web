# AI Prompt Log

**Date**: 2026-05-19 17:08:46
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 17:08:46 - User Prompt

```
commit the TODO, then clean up env-test.php and start on the three doc files
```

---

### 17:09:46 - Git Commit

**Commit:** `365a77b74002`

**Message:**
```
docs(todo): capture 2026-05-19 phase-ordering + architectural-review items

Two new P0 entries from end-of-day conversation:

1. **deploy-allX phase-ordering bug**: nginx instances boot in Phase 4
   before /fsx/nginx exists (configure-nginx.sh bails silently on set -e,
   never writes upstream-php.conf). PHP instances boot in Phase 4 before
   RDS exists in Phase 5 (configure-php.sh skips the env[] block when
   /sandbox/rds/endpoint isn't in SSM). Both required manual SSM fixups
   today; both will recur on next destroy-all/deploy-allX. Proposed fix
   captured (init-fsx-layout target + reorder/parallelize) but deferred
   pending architectural review per item 2.

2. **Architectural review — build-order assumptions + multi-site future**:
   today's recurring bugs surfaced that the system assumes greenfield
   build order without enforcing it. Worker boxes silently mis-config when
   prerequisites aren't ready. Separately, the 1:1 env↔RDS coupling baked
   into settings.php, SSM param namespacing, and secret paths is not
   designed for the multi-site / multi-RDS future Kurt sketched. Next
   step: review docs/ARCHITECTURE.md + docs/plans/multi-tenancy.md before
   any more architecture-shaping code changes.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	TODO.md
```

---

### 17:10:47 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/OPERATIONS.md`

---

### 17:11:13 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/OPERATIONS.md`

---

### 17:11:27 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/OPERATIONS.md`

---

### 17:12:10 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/ARCHITECTURE.md`

---

### 17:12:34 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/ARCHITECTURE.md`

---

### 17:13:15 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/gotchas.md`

---

### 17:14:00 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/gotchas.md`

---

### 17:17:53 - Git Commit

**Commit:** `104a83d027cd`

**Message:**
```
docs: align OPERATIONS, ARCHITECTURE, gotchas with 2026-05-19 reality

The full-day deploy-allX debug arc surfaced a lot of new operational
knowledge and exposed gaps between docs and the running system. This
commit lands the alignment in three existing files plus three new
project memory files.

docs/OPERATIONS.md:
  - New rows in the quick-command-reference table: install-drupal-remote,
    publish-drupal-vhost, reload-nginx, smoke-test-drupal.
  - New procedure section: "Install Drupal end-to-end" — the cloud
    install path via SSM dispatch (composer + drush + vhost write).
  - New procedure section: "Publish or update the Drupal nginx vhost
    without reinstalling" — the surgical publish-drupal-vhost path.
  - New procedure section: "Reload nginx (after FSx vhost changes)" —
    explains the FSx → /etc/nginx/shared NFS mount and why running
    nginx needs a reload to pick up new vhosts.
  - New anti-pattern entries: don't run install-drupal directly from
    Mac (use -remote); don't expect today's deploy-allX to land Drupal
    cleanly on truly fresh accounts (P0 phase-ordering bugs); don't
    diagnose PHP-FPM env vars via /proc/$PID/environ.

docs/ARCHITECTURE.md:
  - Storage Architecture section rewritten:
    * Replaced stale Mermaid diagram (showed "sites/configs/ssl" subdirs
      that don't exist) with a current diagram showing the actual FSx
      mount topology (deploy-host/PHP at /var/www; nginx's additional
      sub-mount at /etc/nginx/shared).
    * New "Mount layout" table documenting where each host class mounts
      what.
    * Documented the no-env-prefix path layout (post commit ee319d5
      cutover) and which scripts create each directory.
    * Flagged the /fsx/nginx chicken-and-egg as a known build-order
      fragility, pointing at P0 in TODO.
  - New section "deploy-allX Phase Ordering — Current Assumptions":
    table of phases + which assumptions each bakes in, with explicit
    callout of the two phase-ordering P0 bugs.

docs/memory/gotchas.md (appended, no rewriting):
  - 4 new SSM gotchas: list-command-invocations 2500-char truncation;
    AWS-RunShellScript HOME unset; literal `(` in commands= array;
    multi-instance-id arg passing.
  - 7 new Drupal-install gotchas: drush URL-encoding mismatch; nginx
    vhost lives on FSx not in AMI; configure-nginx.sh silent set -e
    bail on missing /fsx/nginx; configure-php.sh skips env[] block on
    missing SSM param; settings.php paths cutover trap; PHP-FPM
    clear_env makes /proc/environ useless; install-drupal.sh marker
    short-circuit + surgical-Make-target workaround.

docs/memory/admin-access-policy.md (new): captures the SSM-only-ingress
  rule + the AWS-StartSSHSession proxy pattern that lets scp/sftp work
  without ever opening port 22. Explicitly addresses the "what this is
  NOT" framing so future sessions don't pattern-match "SSH keys" →
  "open port 22".

docs/memory/destroy-all-residue.md (new): per-service inventory of what
  survives destroy-all + three design options for handling the cleanup
  (clean-account target / destroy-allX split / burn-and-recreate
  account via Organizations). References the SSM "new experience"
  open questions.

docs/memory/ssm-new-experience-decision.md (new): records the
  deliberate decision to disable AWS SSM's "new experience" / Quick
  Setup / DHMC for this project, with reasoning + rollback playbook
  (so if AWS prompts again, future me knows to decline).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	docs/ARCHITECTURE.md
M	docs/OPERATIONS.md
A	docs/memory/admin-access-policy.md
A	docs/memory/destroy-all-residue.md
M	docs/memory/gotchas.md
A	docs/memory/ssm-new-experience-decision.md
```

---

### 17:18:01 - Response

Pushed `365a77b..104a83d` (+533/-16 across 6 files).

**All 5 tasks complete:**
- ✓ TODO updates committed (`365a77b`)
- ✓ env-test.php cleaned off FSx
- ✓ OPERATIONS.md updated (new procedures + anti-patterns)
- ✓ ARCHITECTURE.md updated (mount layout rewrite + phase-ordering table)
- ✓ gotchas.md appended (SSM + Drupal-install lessons)
- ✓ Plus 3 new memory files from earlier today bundled in

**Still uncommitted from end-of-day housekeeping** (separate from doc work, your call):
- 45 untracked `PROMPT_LOGS/*.md` from today + an updated 5-18 prompt log — the ISO 27001 audit trail catchup. Want me to bundle those as a separate commit, or leave for tomorrow?

---

### 17:18:01 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
