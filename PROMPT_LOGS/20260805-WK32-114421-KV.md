# AI Prompt Log

**Date**: 2026-08-05 11:44:21
**Week**: 32
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:44:21 - User Prompt

```
<task-notification>
<task-id>a18294662cdc6590a</task-id>
<tool-use-id>toolu_01W1AcTchJ7g7E3dKKmL56uC</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-kurtvanderwater-Work-dev-cf-scalable-drupal/aa44cb94-bd56-4bb5-879c-ab8e62c78e31/tasks/a18294662cdc6590a.output</output-file>
<status>completed</status>
<summary>Agent "Deploy-host build architecture" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>I have sufficient detail to produce the report. Let me now write the final findings.

---

# Deploy-Host Architecture Map (cf-scalable-web)

## 1. CFN Template — Deploy Host

**Primary template:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/cf-deploy-host.yaml`
**Params file:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/parameters/deploy-host.json` (only overrides `InstanceType`).

**Parameters** (cf-deploy-host.yaml:31-55):
- `InstanceType` default `t4g.small` (allowed: `t4g.nano|micro|small|medium` — Graviton only)
- `UbuntuAMI` SSM public parameter `/aws/service/canonical/ubuntu/server/24.04/stable/current/arm64/hvm/ebs-gp3/ami-id` (line 45)
- `DeployHostBackupsBucketName` default `sandbox-deploy-host-backups-kv-worxco` (line 55)

**EC2 Instance** (cf-deploy-host.yaml:134-149): default VPC, single SG, no KeyName (SSM only), single EBS `/dev/sda1` gp3 20 GiB encrypted (lines 142-148). No additional volumes.

**IAM** (cf-deploy-host.yaml:78-105):
- Role `${AWS::StackName}-role` assumed by `ec2.amazonaws.com`.
- Managed policies: `AmazonSSMManagedInstanceCore` + `AdministratorAccess` (lines 91, 95) — no inline policies at all on the instance role.
- Instance profile `${AWS::StackName}-profile`.

**SSM Session doc** (cf-deploy-host.yaml:114-128): account-wide `SSM-SessionManagerRunShell` document forcing `runAsDefaultUser: ubuntu` with `cd ~ &amp;&amp; exec bash -l`.

**Security Group** (cf-deploy-host.yaml:62-72): no ingress rules at all. Default egress open. Exported as `${StackName}-sg-id`.

**EventBridge Scheduler + IAM** (cf-deploy-host.yaml:347-408):
- `DeployHostSchedulerRole` with inline `SendCommandToDeployHost` policy (lines 358-369) — scoped to `ssm:SendCommand` against just this instance ARN + `AWS-RunShellScript` document.
- `DeployHostNightlyBackupSchedule` cron `0 2 * * ? *` in `America/Chicago` (line 383-384). Payload: `aws s3 cp` `_common.sh` + `backup-state.sh` from S3 then executes them (lines 400-406).

**No** Route53, CloudWatch alarms.

**Backups bucket template:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/cf-deploy-host-backups.yaml`. Bucket `sandbox-deploy-host-backups-kv-worxco` (line 54), AES256 (line 58), all public access blocked (lines 59-63), lifecycle rules: `logs/` 30d, `config/deploy-host-archive/` 90d, `config/deploy-host-latest.tar.gz` never expires (lines 68-96), abort incomplete MPU 1d.

**VPC Peering template:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/cf-deploy-peering.yaml`. Params `EnvironmentName`, `DeployHostStackName`, `DefaultVpcId`, `DefaultVpcCidr` (default `172.31.0.0/16`, line 56), `DefaultVpcRouteTableId`, `AZCount` (lines 37-68). Creates peering (lines 81-91), route from default→project (97-103), routes from project public + private-1/2/3 back (110-142). Cross-VPC SG ingress rules added to project SGs sourced from deploy-host SG: FSx 2049 (152-162), FSx portmapper 111 (165-175), RDS 5432 (178-188), ElastiCache Valkey 6379 (191-201). `DefaultVpcId`/`DefaultVpcRouteTableId` resolved at deploy time by Makefile:2138-2170.

## 2. Bootstrap / UserData

**UserData** (cf-deploy-host.yaml:149-324) is minimal: sets `AWS_DEFAULT_REGION`, tees to `/var/log/deploy-host-bootstrap.log`, allocates **4 GiB swap** at `/swapfile` with `vm.swappiness=10` (lines 183-189 — comment explicitly says "sized for pgloader migrations SBCL --dynamic-space-size 4096"), installs `git jq unzip python3 curl` + AWS CLI v2 aarch64 (lines 192-200), pulls GitHub deploy key from Secrets Manager `worxco/deploy-host/github-ssh-key` (lines 207-227), clones `git@github.com:worx/cf-scalable-web.git` into `/home/ubuntu/projects/` (line 234), then execs the heavy-lift script.

**Heavy-lift bootstrap:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/bootstrap.sh` (692 lines). Steps:
- apt update+upgrade (71-72)
- Core CLIs: `htop make plocate pv screen tmux tree vim zsh python3-pip python3-venv` (77-88)
- `/etc/profile.d/deploy-host-env.sh` writes `EDITOR/VISUAL=vim`, `AWS_PAGER=""`, `AWS_CLI_AUTO_PROMPT=off`, `AWS_DEFAULT_REGION` (99-105)
- `/etc/tmux.conf` with vi mode + 50k scrollback + env indicator (119-175)
- `chsh -s /bin/zsh ubuntu` and `/etc/zsh/zshrc.d/worxco-prompt.zsh` (188-266), bash→zsh auto-exec via `/etc/bash.bashrc` (311-319)
- `/etc/worxco/current-env` bootstrapped to `NONE` (321-326)
- Root password from `worxco/deploy-host/root-password` (358-369)
- cfn-lint venv at `/opt/cfn-lint` linked to `/usr/local/bin/cfn-lint` (374-376)
- Node 20 LTS (381-382) → `@anthropic-ai/claude-code` global npm (387)
- SSM agent snap (392-394)
- Big apt install: `mariadb-client mariadb-server nfs-common pgloader postgresql-client sqlite3 redis-tools php8.3-cli/common/curl/mbstring/xml/zip/gd/pgsql/sqlite3/intl/bcmath/opcache` (399-418). **mariadb disabled+stopped** immediately (428) — used only for pgloader scratch DB.
- `session-manager-plugin` .deb ARM64 (437-444; duplicated at 502-505)
- AWS RDS CA bundle into `/usr/local/share/ca-certificates/rds-ca.crt` + `update-ca-certificates` (456-459)
- `git config --global --add safe.directory /home/ubuntu/projects/cf-scalable-web` for root (476)
- Composer to `/usr/local/bin/composer` (482-484); Drush via composer global for ubuntu (490-495)
- Creates `/etc/worxco/`, `/etc/worxco/envs/`, marker file `/etc/worxco/deploy-host-marker` (510-513)
- Installs helper scripts to `/usr/local/bin/{info-env,show-env,psql-env,valkey-env}` + `/usr/local/sbin/{use-env,refresh-env-config}` + sudoers file `/etc/sudoers.d/worxco-refresh-env-config` (523-529)
- First `refresh-env-config sandbox staging production` (540)
- Auto-remount `/var/www` from FSx via `use-env $prev_env` if `/etc/worxco/current-env` != NONE (556-571)
- MOTD to `/etc/motd` + `/etc/profile.d/00-worxco-motd.sh` + `/etc/zsh/zshrc.d/00-motd.zsh` (576-662)
- Admin SSH keys synced from SSM registry `/worxco/admin/ssh-public-keys/*` via `scripts/deploy-host/sync-admin-ssh-keys.sh` (676-682)

**No new user accounts** — only `ubuntu` (default). No systemd units created beyond enabling snap ssm-agent.

## 3. Storage

- Root EBS: 20 GiB gp3, encrypted (cf-deploy-host.yaml:143-148)
- Swap: 4 GiB `/swapfile`, `swappiness=10`, persisted via `/etc/fstab` entry (cf-deploy-host.yaml:183-189). Header comment at line 13 is stale ("2GB swap"); actual current allocation is 4 GiB.
- No additional EBS volumes. `/var/www` is a runtime NFS mount from the environment's FSx (managed by `use-env`, not the CFN).

## 4. Backup / Restore

**Backup:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/backup-state.sh` (400 lines). Paths captured — enumerated in `BACKUP_PATHS[]` at lines 148-196:
- `/root/.ssh`, `/home/ubuntu/.ssh`, `/home/ubuntu/.gitconfig`
- `/root/.env`, `/root/bin`, `/home/ubuntu/.env`, `/home/ubuntu/bin`
- `/root/.aws/config`, `/home/ubuntu/.aws/config` (explicitly NOT credentials — see header 60-73)
- `/etc/worxco/current-env` (drives auto-remount on next boot)
- `.bashrc`, `.profile`, `.zsh_history`, `.mysql_history`, `.config/htop`, `.sudo_as_admin_successful`

Tar → `/tmp/deploy-host-&lt;UTC&gt;.tar.gz`, uploaded to `s3://&lt;bucket&gt;/config/deploy-host-archive/deploy-host-&lt;UTC&gt;.tar.gz` + copy to `config/deploy-host-latest.tar.gz` (backup-state.sh:328-355). Manifest embedded in archive (283-317).

**Restore:** `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/restore-state.sh` (430 lines). Seven-phase flow (30-40). Takes a pre-restore `/tmp/pre-restore-backup-&lt;UTC&gt;.tar.gz` snapshot, extracts `deploy-host-latest.tar.gz`, then writes credentials stub files at `/root/.aws/credentials` and `/home/ubuntu/.aws/credentials` pointing operator back to their Mac (145-149).

**Nightly automation:** EventBridge Scheduler in cf-deploy-host.yaml:376-408 (cron `0 2 * * ? *` `America/Chicago`) → `ssm:SendCommand` `AWS-RunShellScript` fetching+running `backup-state.sh` from S3.

**Auto-restore on first boot:** UserData cf-deploy-host.yaml:265-298 — `head-object` on `config/deploy-host-latest.tar.gz`; if present, fetch `_common.sh` + `restore-state.sh` from S3 and run with `CONFIRMED=yes`. Then re-invokes remount step (309-320).

## 5. Network Position

- Lives in **AWS default VPC** (typically `172.31.0.0/16`), NOT the project VPC. Comment at cf-deploy-host.yaml:12 confirms: "survives project VPC teardowns."
- Public IP for outbound only (line 15); no inbound (empty SG).
- Cross-VPC reachability to RDS/FSx/Valkey via `cf-deploy-peering.yaml`, deployed per-environment. Requires modifying the default VPC's main route table (`DefaultVpcRouteTableId` param) AND each project route table (public + private × AZ count). Peering SG-to-SG references only work post-peering (comment at cf-deploy-peering.yaml:146-149).

## 6. Directly Reusable vs. Must-Parameterize for migrate-host

**Directly clone / parameterize away:**
- Whole SSM Session Manager pattern (SessionPreferences doc is account-wide — should NOT be duplicated; already exists once).
- Empty-ingress SG pattern (cf-deploy-host.yaml:62-72).
- IAM role skeleton with `AmazonSSMManagedInstanceCore + AdministratorAccess` (78-98) — same trust policy applies; only rename.
- UbuntuAMI SSM public parameter reference (arm64 already correct for Graviton) (42-45).
- UserData scaffolding: swap allocation (183-189), GitHub deploy key from Secrets Manager (207-227), repo clone (229-238), handoff to a bootstrap script (240-249), auto-restore block (265-298), remount block (309-320). The Secrets Manager path and bucket names would be re-parameterized.
- Backup bucket pattern in cf-deploy-host-backups.yaml (whole file cloneable with a `migrate-host-backups` bucket name; lifecycle rules already right shape).
- EventBridge Scheduler + role pattern (347-408) — trivially retargetable.
- Peering stack cf-deploy-peering.yaml — the migrate-host would either share the same default-VPC peering (if hosted in default VPC too) OR need its own peering resource block.

**Must-parameterize differently:**
- `InstanceType` — current `AllowedValues` list only goes up to `t4g.medium` (36-41); a ~4 vCPU / 16 GiB Graviton (e.g., `m7g.xlarge`, `r7g.xlarge`, `c7g.2xlarge`) is not in the allowed list. Either widen the list or drop the constraint.
- Root EBS 20 GiB (145) — likely undersized for a migration workspace holding MySQL dumps + pgloader intermediates. Parameterize `VolumeSize`.
- Swap sized at 4 GiB (183) — with 16 GiB RAM available the /swapfile can shrink or the ratio should be reconsidered.
- Bootstrap script path is hardcoded `scripts/deploy-host/bootstrap.sh` (244); a migrate-host will want its own script.
- Backups bucket name default `sandbox-deploy-host-backups-kv-worxco` (55) — needs separate bucket.
- SSM secrets: `worxco/deploy-host/github-ssh-key` (208), `worxco/deploy-host/root-password` (bootstrap.sh:359) — either shared or migrate-host-specific paths.
- `/etc/worxco/deploy-host-marker` file (bootstrap.sh:512) is what Makefile targets (869, 873, 885, 895, 923) key off to distinguish deploy-host from other hosts — the migrate-host will need its own marker (`/etc/worxco/migrate-host-marker`) and matching Makefile guards.
- `use-env` FSx mount step (556-571) is deploy-host-specific to `/var/www`; migration workflow may or may not need it — decide per-environment.
- The nightly backup Schedule Name `${AWS::StackName}-nightly-backup` will cleanly differ if StackName differs; no cross-stack collision.
- Peering: deploy-host currently uses default VPC; if the migrate-host also lives there, ingress from its SG must be added to project FSx/RDS/Valkey SGs (parallel `SecurityGroupIngress` rules in cf-deploy-peering.yaml lines 152-201, or in a companion peering template).</result>
<usage><subagent_tokens>68114</subagent_tokens><tool_uses>15</tool_uses><duration_ms>151627</duration_ms></usage>
</task-notification>
```

---

### 11:44:38 - Response

Agent 1 back. Deploy-host architecture mapped — good pattern to clone (SSM Session Manager, empty-ingress SG, IAM role, backup-state.sh + restore-state.sh + EventBridge nightly, auto-restore on first boot, peering-template ingress rules for FSx/RDS/Valkey). Worth flagging early: deploy-host today uses `AdministratorAccess` (broad) + swap explicitly sized for `pgloader --dynamic-space-size 4096` — the exact constraint we're proposing to eliminate.

Still waiting on Agent 2 (MariaDB + pgloader tuning deep-dive) before I move to design.

---

### 11:44:38 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
