# AI Prompt Log

**Date**: 2026-05-22 14:35:53
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:35:53 - User Prompt

```
ok - so - All the fixes are applied, committed, and pushed. That's a question, and once I get the response to the positive on that, I will start a complete tear down and rebuild from scratch.
```

---

### 14:36:11 - Response

**Yes — all code/docs/config changes are applied, committed, and pushed.**

`git log origin/main..HEAD` is empty, which means local matches remote — nothing unpushed. Last commits on `main`:

```
77e9eca feat(nginx): decouple compute health from Drupal install state
592a15e docs(memory): expand shell-portability — word-splitting is the silent footgun
5f98485 fix(ssm-dispatch): target ASG view, not tag-filtered EC2 describe-instances
e2299c5 docs(logs): catch up prompt logs through 2026-05-22
af7b200 chore(params): point sandbox at sandbox.envs.zoning-info.com
```

The only "M" / "??" entries are **PROMPT_LOGS** that the auto-logger is writing in real-time as we have this conversation — not real outstanding work. Those will sweep up in the next prompt-log catch-up commit.

**Pre-rebuild reminders:**

- Route 53 sub-zone `envs.zoning-info.com` (`Z03149452QE09GQX23F48`) stays since it's not CFN-managed — good, you'll want it
- The A-record `sandbox.envs.zoning-info.com` → old ALB will become stale. You'll need to re-publish it pointing at the new ALB once Phase 3 completes (per `docs/DNS-SETUP.md`). If you skip that step, `smoke-test-public` will fail with a clear "Route 53 alias missing" message; `smoke-test-drupal` will still pass since it forges the Host header
- `RecipeVersion 1.0.11` will trigger fresh AMI bakes on all 3 pipelines (~25 min in parallel during Phase 2)
- nginx instances should now pass `/health` immediately on boot — no ASG churn during the install window
- If install-drupal fails for any reason, deploy-allX will now exit clean with "Infrastructure healthy" instead of red-screen-of-death

Cleared for teardown. Standing by.

---

### 14:36:11 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
