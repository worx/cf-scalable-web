# AI Prompt Log

**Date**: 2026-07-29 14:07:40
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:07:40 - User Prompt

```
great - so - I left the big backup, and manually killed the BAK folders related to the 2 modules.
Not comment that any of this should matter since my next step is...

Destroy all.

Then, rebuild the whole thing and see how fragil it is :)
```

---

### 14:08:36 - Response

Nice — that's the real validation. Let me flag what to watch for based on what we learned this session.

## Checkpoints and things that could bite

**AMI build (~25 min after CFN deploy):**
- `configure-php.sh` now bakes `env[AWS_S3_BUCKET]` into the PHP-FPM pool config. If that regresses at build time, PHP-FPM won't see the env var and `_flysystem/s3/...` requests fail. Verify with our probe-file trick post-deploy if suspicious.

**install-drupal-full (fresh Drupal):**
- `install-drupal.sh` now writes the flysystem_s3 block into settings.php from the template. Should Just Work — the whole point of the `inject-flysystem-config.sh` we wrote was to bridge the gap for the EXISTING install; a fresh install doesn't need it.

**Migration dump/restore:**
- **jq is on React2 now** (you installed it manually) — so auto-discovery for siblings should work. If React2 gets replaced or jq disappears, WARN logs will tell you.
- **jq must also be on the deploy-host** for restore-codebase Phase 1. If the deploy-host AMI ships with jq, no problem. If not, you'll see "Required tool 'jq' not found on PATH" and can `sudo apt install jq` to unblock.
- Multi-top-level tarball layout + type=project heuristic — both fixed tonight.

**Post-restore Drupal:**
- **`bca` module must be enabled** after migration. The migrated DB carries prod's `core.extension` which enables it — should be automatic. If bundle classes come up as "(NONE)" like they did last session, we'll need `drush en bca -y && drush cache:rebuild`.
- **s3:GetObjectAcl** — cf-iam.yaml has it now. `_flysystem/s3/…` should serve without 504-via-permission-error.

## Things that WON'T get fixed by rebuild

Just so no one expects miracles:

- **LaTeX preview** still 504 — Lambda work
- **`/print/pdf/commerce_order/…`** still 504 — Lambda work
- **4 unmapped bundle classes** still crash on report pages that reference them — Zac's code question

## Time expectation

Per the top-level CLAUDE.md: `make deploy-all ~50 min` + AMI build ~25 min + migration (~30-45 min if you dump+restore all four) = **realistically 2-3 hours end to end**. Plan accordingly.

## When to ping me

Interrupt the deploy for anything that:
- Errors before the deploy times out cleanly (looks like a hang, actually failing)
- Errors during `install-drupal-full` (that's where our new flysystem block gets tested)
- Something we discussed tonight fails to auto-recover

Everything else just log-inspect after it completes. Good luck — fingers crossed the fragility count is low.

---

### 14:08:36 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
