# AI Prompt Log

**Date**: 2026-07-13 17:36:29
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 17:36:29 - User Prompt

```
ok - So before we run that, I suspect we need to do a commit so that when I go to deploy a host, I do a `git pull` before I try any of this. These won't be here. They've all been created and exist on my Mac side, not on the deploy host side.
```

---

### 17:39:34 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/.git/COMMIT_MSG_DRAFT`

---

### 17:40:09 - Response

Ready for your review.

## Summary of what's staged

| Category | Count | Notes |
|----------|-------|-------|
| **Total files** | 115 | Everything legitimate from this session + prior |
| **Migration folder** (new files + renames) | ~15 | Whole `migration/` subfolder |
| **Parent Makefile** | 1 | Removed old migration section |
| **`docs/research/nodejs-tier.md`** | 1 | New folder + moved file |
| **PROMPT_LOGS** | ~97 | Multi-week catch-up (WK21 mod, WK24, WK28, WK29) |

## Intentionally skipped

```
.claude/files.zip     ← from earlier "leave alone" decision
tt.html               ← same
tt.pdf                ← same
```

If any of those turn out to be things you actually want captured, easy to `git add` before committing.

## Ready for review

- Commit message: `.git/COMMIT_MSG_DRAFT` (150 lines, but scannable — starts with a clean summary, then sections for each major change area)
- Everything staged is legitimate; nothing that shouldn't go in is going in

## When approved

```bash
git commit -F .git/COMMIT_MSG_DRAFT
git push origin main
```

Then on deploy-host:

```bash
cd ~/projects/cf-scalable-web
git pull origin main
cd migration
make dump-all CONFIRMED=yes | tee /tmp/dump-all-$(date +%Y%m%d).log
```

## Notes on running `make dump-all` from deploy-host

Two things to remember when you get there:

1. **AWS profiles must be present on deploy-host.** The `_ssm-run-jumpbox.sh` helper needs both `ZI-Sandbox` (for S3 uploads to sandbox) and `ZoningInfoAdmin` (for SSM send-command to prod jumpbox). If you set those up on deploy-host during the earlier phase of the migration, you're good; otherwise `aws sts get-caller-identity` per profile can verify quickly.

2. **session-manager-plugin.** Same story — needed on the host running the make target so it can invoke SSM. Deploy-host got this during the earlier migration work, but worth eyeball-confirming with `command -v session-manager-plugin`.

Review the commit message when you get a moment, and let me know if anything wants adjustment before you fire it.

---

### 17:40:09 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
