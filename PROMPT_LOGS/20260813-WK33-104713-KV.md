# AI Prompt Log

**Date**: 2026-08-13 10:47:13
**Week**: 33
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 10:47:13 - User Prompt

```
yup - row limit = 1000
```

---

### 10:47:26 - Response

Confirmed as the Drupal 11 default. `dblog_cron()` runs on every cron trigger (typically every 3 hours by default in Drupal) and trims the oldest rows so only the newest 1000 survive.

Which explains **why the older prod entries in your watchdog (wid 29587, 29589) show notices from paths that don't exist on sandbox** — those are historical prod log entries from when prod was on the `/var/www/html/zoning_info_platform/` layout. They got captured in the DB dump you migrated, and they'll organically age out of watchdog as sandbox generates 1000+ new entries and cron trims them.

**Practical effect:** you have ~980 watchdog rows above the historical prod cutoff so far (30571 - ~29590). Once you exceed 1000, cron starts trimming — the ancient prod entries will disappear naturally in the normal course of running sandbox. No action needed.

If you ever wanted an immediate purge:
```bash
sudo bash -c 'source /etc/worxco/envs/sandbox && cd /var/www/drupal && vendor/bin/drush ws --delete=all'
```

Otherwise, just let cron handle it.

---

### 10:47:26 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
