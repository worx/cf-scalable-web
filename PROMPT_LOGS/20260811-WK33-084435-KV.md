# AI Prompt Log

**Date**: 2026-08-11 08:44:35
**Week**: 33
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 08:44:35 - User Prompt

```
<task-notification>
<task-id>aa56a1f14c861f007</task-id>
<tool-use-id>toolu_01Y7qSTa786EPrm8hbEKopL6</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-kurtvanderwater-Work-dev-cf-scalable-drupal/aa44cb94-bd56-4bb5-879c-ab8e62c78e31/tasks/aa56a1f14c861f007.output</output-file>
<status>completed</status>
<summary>Agent "Investigate RDS + pg_dump patterns" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## 1. Current sandbox RDS config

Parameters live in `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/parameters/sandbox.json` (no `database-sandbox.json` exists — the DB params are folded into the per-env file). Sandbox values:
- **Instance class**: `db.t4g.micro` (lines 19)
- **Allocated storage**: 20 GiB, max autoscale 100 GiB (lines 20-21)
- **IOPS / throughput**: NOT parameterized. `cf-database.yaml` line 235 hardcodes `StorageType: gp3` and omits `Iops` / `StorageThroughput` entirely, so it runs on gp3 defaults (3000 IOPS, 125 MB/s).
- **Multi-AZ**: `EnableMultiAZ: "false"` (sandbox.json line 30); template default is `true`.
- **Deletion protection**: `true` (line 31); PG version 16.11.

**In-place updateability**: `DBInstanceClass`, `AllocatedStorage`, `MaxAllocatedStorage`, `MultiAZ`, and `BackupRetentionDays` are all CFN properties that update in place on RDS (no replacement; scale-up of storage is online, class change causes a brief reboot / Multi-AZ failover). `StorageType`/`Iops`/`Throughput` are not exposed as parameters — adding gp3 IOPS/throughput would require a small template edit but the underlying RDS modify is also online.

## 2. Existing pg_dump / pg_restore patterns

No code uses `pg_dump`, `pg_restore`, or `pg_basebackup`. Only textual mention is `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/FSX-LAYOUT.md` line 309 (descriptive: "Per-DB via `pg_dump`"). The actual migration path is MySQL-dump → local MariaDB restore → **pgloader** → RDS Postgres, orchestrated by:
- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/scripts/jumpbox/dump-mysql.sh`
- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/scripts/deploy-host/restore-mysql.sh`
- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/scripts/deploy-host/run-pgloader.sh`
- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/pgloader/zinew.load.tmpl`

No "local Postgres on a worker + dump/restore to RDS" pattern exists — that would be novel.

## 3. Current migrate-host resources

From `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/cf-migrate-host.yaml`:
- **Root EBS**: 40 GiB gp3, 10000 IOPS (param `RootVolumeIops` default), 500 MB/s throughput (bumped post-boot via `aws ec2 modify-volume` in UserData, lines 302-320, because CFN inline block-device mappings can't set gp3 throughput).
- Instance: `r7g.xlarge` (4 vCPU, 32 GiB).
- Design-doc disk math (lines 106-110): ~15 GiB peak used (5 OS + 3 staged dump + 6 imported `/var/lib/mysql` + temp), **~25 GiB slack**.

**Fits a local Postgres + 2.2 GB Drupal DB?** Yes, comfortably. Postgres binaries ≈ 200 MB. A 2.2 GB MySQL dump / 12M rows typically lands at 4-7 GB in Postgres with indexes/WAL. Even if MariaDB and Postgres coexist during migration, peak becomes ~22 GiB used vs. 40 GiB total → ~18 GiB free. Also, `RootVolumeSize` is a CFN parameter (MinValue 20, MaxValue 500), so bumping to 60-80 GiB for extra headroom is a one-line param change.</result>
<usage><subagent_tokens>49283</subagent_tokens><tool_uses>14</tool_uses><duration_ms>83700</duration_ms></usage>
</task-notification>
```

---

### 08:51:13 - File Modified (Write)

- `/Users/kurtvanderwater/.claude/plans/zesty-plotting-squid.md`

---

### 08:55:14 - Response

Deploy-all started at 8:36 CT — ETA ~10:00 CT for full-stack ready, then install-drupal-full + migration follows. I'll be here for the timing report + implementation trigger.

---

### 08:55:14 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
