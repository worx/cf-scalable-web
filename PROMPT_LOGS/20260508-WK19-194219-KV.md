# AI Prompt Log

**Date**: 2026-05-08 19:42:19
**Week**: 19
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 19:42:19 - User Prompt

```
run both checks
```

---

### 19:45:59 - Response

**Confirmed STALE:** PHP boxes are running 1.0.4 code.
- `configure-php.sh` = 51 lines (old version, no Drupal env block)
- `/etc/php/8.3/fpm/pool.d/extra/` — does not exist
- `www.conf` has NO `include=` directive
- `php.ini` has the OLD plain-tcp Valkey session line (broken — can't talk to TLS+AUTH cluster)

So Drupal would not boot through these instances even if you tried — `getenv('DRUPAL_DB_HOST')` returns false, settings.php falls back to `localhost`, DB connection fails.

**The fix:** move the configure-php.sh body out of the YAML's inline `Data` field. Same pattern we already use for `www.conf` / `php.ini` / `extensions.txt` — script lives in `image-builder/configs/`, gets uploaded by `sync-configs`, AMI build downloads it. The component's `Data` field shrinks to a thin wrapper that does `aws s3 cp` + `sed s/__PHP_VER__/8.3/g` + `chmod 755`.

That gets us under the 16k limit AND makes the script easier to iterate on (edit one file instead of two YAML heredocs).

**Plan:**
1. Extract `configure-php.sh` → `image-builder/configs/configure-php.sh` (single file, `__PHP_VER__` placeholder)
2. Slim down `PhpFpm74InstallComponent` and `PhpFpm83InstallComponent` `Data` fields — replace ~200-line heredoc with ~10-line download+sed
3. Mirror in the reference component copies
4. Update `sync-configs` Make target to upload the new file
5. Bump `RecipeVersion` 1.0.6 → 1.0.7
6. Lesson learned: **don't dismiss W1031** — for ImageBuilder Components, the long-Fn-Sub warning is load-bearing. Will save this as a gotcha.

Re-deploy chain after the fix is the same as before (sync-configs → deploy-image-builder → build-amis → deploy-ami-params → deploy-compute).

Want me to do the refactor now? (UPDATE_ROLLBACK_COMPLETE is a normal terminal state — the stack can be re-updated as soon as the template is fixed.)

---

### 19:45:59 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
