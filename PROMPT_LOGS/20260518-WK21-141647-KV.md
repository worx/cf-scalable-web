# AI Prompt Log

**Date**: 2026-05-18 14:16:47
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:16:47 - User Prompt

```
Yeah, your assessment is correct. If the prompt logs are going to make it just too damn noisy, then let's leave that out. Though my understanding is that it should show up as a folder that I can hit a twisty on and go look at. I don't know. I don't use Obsidian much. I, frankly, don't plan to use this because for me IntelliJ lets me not only view any of my markdown files but edit them as well. As other people get to this, I don't want to force them all to have to use an editor just to look at documentation. By doing it this way, they've got a built-in Obsidian vault that they can gain access to.

Having said that, you'll find that there's a folder out there that's the Obsidian minimal template. I pulled the.obsidian from it. I don't need it any longer. There's no reason to pollute our git repositories with it, so before we do any committing, let's delete that folder.
```

---

### 14:17:27 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/.obsidian/app.json`

---

### 14:17:49 - Git Commit

**Commit:** `fa947f9d8f78`

**Message:**
```
chore(obsidian): set up project-level Obsidian vault for markdown browsing

The project doubles as an Obsidian vault so contributors can browse and
read the markdown docs (README, TODO, docs/*.md) without standing up
their own editor. This is for VIEWING — anyone who wants to edit can
use whichever tool they prefer (IntelliJ, VS Code, etc.).

Vault config (.obsidian/):
  app.json — userIgnoreFilters hides the top-level code directories
    (cloudformation/, scripts/, image-builder/, deploy-host/, tests/,
    PROMPT_LOGS/) so the file explorer shows only docs/ and root *.md.
    PROMPT_LOGS is excluded because 475 auto-generated audit-trail
    files would dominate the tree. showUnsupportedFiles is false so
    non-markdown extensions (yaml, sh, json, etc.) stay hidden too.
  core-plugins.json — sensible defaults (graph and daily-notes off,
    everything else on).
  appearance.json, templates.json, hotkeys.json, community-plugins.json,
    snippets/ — Obsidian defaults; included so a fresh clone opens
    consistently across machines.

.gitignore additions:
  - .obsidian/workspace.json, workspace-mobile.json, cache/ — volatile
    UI state that's per-user, not per-project (where panes were docked,
    last-viewed file, etc.). Excluded so each developer's local layout
    doesn't churn the repo.
  - .git/, node_modules/, vendor/, dist/, build/ — common engineering
    noise. Not currently in this repo but safe defensive additions.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	.gitignore
A	.obsidian/app.json
A	.obsidian/appearance.json
A	.obsidian/community-plugins.json
A	.obsidian/core-plugins.json
A	.obsidian/hotkeys.json
A	.obsidian/templates.json
```

---

### 14:17:56 - Response

Pushed `fa947f9`. Obsidian vault is wired up + the template scratch dir is gone.

**One thing to note about `.gitignore`:** the new additions include `.git/` — which is harmless (git ignores its own `.git/` regardless) but slightly redundant. The `node_modules/`, `vendor/`, `dist/`, `build/` entries are defensive — none of those dirs exist in the project today, but they protect against future contamination if anyone ever runs `npm install`, `composer install` from a non-Docker context, etc. I'd leave them as-is.

Now back to actual project work. You asked earlier about codifying lessons learned into a prioritized to-do list. **Want me to do that next?** I'd put together something like:

| Priority | Item | Why |
|---|---|---|
| HIGH | Bake-and-roll AMIs to 1.0.10 (start→restart fix) | Otherwise next instance cycle re-breaks Drupal |
| HIGH | install-drupal.sh psql-heredoc password mangling | Will bite again next install if password has shell metachars |
| MED | `make check-drift` target | The "template edited but not deployed" trap bit us repeatedly |
| MED | `make build-amis` should wait for pipelines | The "deploy-ami-params races AMI build" trap |
| MED | OPcache reset hook somewhere | Anytime settings.php changes on FSx |
| LOW | The `use-env` cutover on deploy-host | We committed the code but you still need to run `git pull`, `sudo bash bootstrap.sh`, unmount `/var/www/sandbox`, etc. |
| LOW | Re-organize `docs/` for the Obsidian vault story | Maybe an `index.md` linking everything together |

Want me to flesh that out as a real prioritized list in a doc/TODO update? Or jump straight into one of the items?

---

### 14:17:56 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
