# AI Prompt Log

**Date**: 2026-06-12 16:38:11
**Week**: 24
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 16:38:11 - User Prompt

```
update docs/MIGRATION.md with the corrected approach
As much as I'd love to explore your option B and learn more about the intricacies or simplicities of doing such a thing, I believe I'm going to go with your vote of option A. Since I have the same, and even if we forgot to delete that key (as shameful as that might be), the deploy host is extraordinarily fluid and temporary anyway and it would not survive a destroy and deploy phase, so I'm not terribly worried about it. I'm the only one that's got access to it, and we're only accessing it via SSM, so we don't even have somebody trying to break into it. I think A is the simplest of the options, and I just don't see much of a problem with it at all. Hopefully you agree with me.
```

---

### 16:40:26 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/MIGRATION.md`

---

### 16:40:54 - Git Commit

**Commit:** `ba78d69d3209`

**Message:**
```
docs(migration): replace mysqldump→S3→reload with live pgloader via SSM tunnel

Operator (Kurt) caught that the v1 runbook had pgloader reading from a
mysqldump file via a fabricated `--type mysql-file` flag that doesn't
exist. pgloader's MySQL source mode requires a live MySQL connection
(`mysql://...` URL) — there's no native dump-file mode.

Revised architecture:

  PROD:    cf-migration-jumpbox (SSM-only, in subnet-85e938dc)
              │
              │ SSM port-forward via ZoningInfoAdmin profile
              ▼
  SANDBOX: deploy-host:3306 ──┐
                              ├─ pgloader live-converts both sides
           sandbox RDS:5432 ──┘    (sandbox RDS via native VPC access)

Database now uses live conversion through an SSM tunnel terminating on
localhost:3306 of the deploy-host. No mysqldump intermediate. pgloader
runs on the deploy-host (where it's already installed). Codebase
ferry via S3 is unchanged — that part of the v1 runbook was right.

Required prerequisite added: install session-manager-plugin on the
deploy-host (needed to ORIGINATE SSM sessions; the SSM Agent already
running there is for RECEIVING them — different binaries).

Credentials approach documented as Option A (operator's choice):
  - Copy ZoningInfoAdmin profile config + credentials to deploy-host's
    ~/.aws/ directory for the duration of migration work
  - Acceptable risk: deploy-host is itself ephemeral (won't survive a
    destroy-allXX cycle), SSM is the only access path, operator is
    sole user
  - Cleanup step: rm ~/.aws/credentials when migration is done

New pgloader load-file recipe with Drupal-aware CASTs:
  - TINYINT(1) → BOOLEAN (Drupal true/false flags)
  - DATETIME → TIMESTAMPTZ (PostgreSQL timezone-aware variant)
  - zero-date sentinel ('0000-00-00') → NULL (MySQL allows, PG doesn't)

Reordered step numbers; renumbered "Things to watch for" with new
items relevant to the live-pgloader path:
  - pgloader memory pressure / batch rows tuning
  - SSM session idle timeout (and how pgloader's re-runs are clean)
  - Password handling in /tmp/zinew.load (chmod 600 + shred after)

Files Modified:
- docs/MIGRATION.md (substantial rewrite — same scope, corrected DB
  transport)

The CFN templates (cf-migration-bucket.yaml, cf-migration-jumpbox.yaml)
need NO changes — they were already designed correctly. The bucket is
still used for codebase ferry; the jumpbox is still the SSM relay
point; both stacks' roles still scope correctly. Only the operator-
facing procedure changed.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	docs/MIGRATION.md
```

---

### 16:41:05 - Response

Pushed as `ba78d69`. **Doc is now correct.** Notable changes:

| Was | Now |
|-----|-----|
| Step 4 mysqldump → S3 | Steps 4-5: install session-manager-plugin + copy `ZoningInfoAdmin` profile to deploy-host, then open SSM port-forwarding tunnel |
| Step 7 pgloader file-based (fictional `--type mysql-file`) | Step 6 live pgloader with Drupal-aware CAST recipe (TINYINT(1)→BOOLEAN, DATETIME→TIMESTAMPTZ, zero-dates→NULL) |
| Mysqldump password handling concerns | Replaced with: pgloader load-file password ops (`chmod 600`, `shred -u` after run) |
| Mysqldump intermediate file | Gone entirely |

**CFN templates didn't change** — they were architecturally correct as designed (bucket for codebase ferry, jumpbox as SSM relay). Only the operator-facing procedure was wrong.

## Where you are right now

You're already through doc-procedure step 3 (jumpbox verified). Next operator actions per the corrected doc:

1. **Step 4** — SSM into the sandbox deploy-host, install `session-manager-plugin` (Ubuntu ARM64 .deb), copy your `ZoningInfoAdmin` profile config + credentials, verify `aws --profile ZoningInfoAdmin sts get-caller-identity` returns prod account
2. **Step 5** — open the SSM port-forwarding tunnel from deploy-host
3. **Step 6** — run pgloader

The doc has the exact commands; ping me when you're at step 6 and we'll watch the first pgloader run together to see what the casts and warnings look like.

---

### 16:41:05 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
