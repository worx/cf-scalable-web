# AI Prompt Log

**Date**: 2026-07-29 14:13:26
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:13:26 - User Prompt

```
make destroy-all is now running - I'll let you know when it is done - BTW - on the jumpbox - hopefully, that is NOT part of destroy-all ??
```

---

### 14:14:24 - Response

**Jumpbox is safe.** `destroy-all` only touches sandbox-account (`cf-scalable-web-*` stacks in `ZI-Sandbox`). The jumpbox lives in the **prod account** as `cf-migration-jumpbox` and requires `ZoningInfoAdmin` credentials — completely outside destroy-all's scope.

The scripts confirm it:
- `scripts/destroy-all-parallel.py` only references `cf-scalable-web` — no `cf-migration-*` stacks, no `ZoningInfoAdmin` profile
- The three prod-account resources (jumpbox EC2, migration bucket-policy source, migration secret) all live in their own separate stack lineage and would need explicit `make destroy-jumpbox` / `destroy-secret` calls from `migration/Makefile` to touch

So after destroy-all finishes:
- Sandbox: **fully wiped** — RDS, FSx, ElastiCache, IAM, VPC, S3, all compute
- Jumpbox in prod: **untouched** — SSH keys, IAM role, bucket cross-account grants, secret — all still there
- React2 in prod: **untouched** — same reasoning
- The `jq` you installed on React2: still there (that install was to prod-account React2, which we never touch)

Rebuild sequence will be able to skip the whole "deploy the jumpbox in prod" phase and go straight to migration once sandbox is back up.

Ping when destroy-all finishes and you're ready to fire deploy-all.

---

### 14:14:24 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
