# AI Prompt Log

**Date**: 2026-05-15 11:39:48
**Week**: 20
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:39:48 - User Prompt

```
draft the follow-up commits

I think there is sufficient fragility in having different mount paths for deploy and production. We want to make sure that our production uses the same mount paths as deploy does. Yes, we do want to maintain an opportunity to have multi environments running. Let's just update everything so that our PHP boxes and our Nginx boxes and our Op Caches and our Database Caches and our Drupal Caches and all the rest of the damn things are all looking at what will essentially be, in this case, var/www/sandbox/Drupal, etc. 

I think your number three idea to do an opcache reset at various times is an excellent idea. You might want to go back and review, since I keep all of it, even if you don't review all of the conversations from the last couple of days. Find all the errors, and let's build a quick to-do list so we can knock these things out and destroy everything and build from scratch and make sure we've got it done. By everything, I only mean the everything we need to do, which might be everything. Who knows?
```

---

### 11:42:04 - Response

Looking back over the last two days, here's what I'm tracking. I'll structure this as **(A) Issues found**, **(B) Source-code fixes to commit**, **(C) Tear-down + rebuild sequence**.

## A. Issues found (2026-05-13 → 2026-05-15)

| # | Issue | Where it bit | Severity |
|---|---|---|---|
| 1 | `listen.allowed_clients = any` in www.conf rejects every FastCGI request | PHP-FPM ✅ fixed in `26c4bde` | CRITICAL |
| 2 | `install-drupal.sh` psql heredoc shell-interpolates DB password — backtick/`$` mangle it | Drupal can't connect to DB | CRITICAL |
| 3 | cf-app-drupal `ExcludeCharacters` allows shell metachars in generated passwords | Feeds bug #2 | CRITICAL — ✅ fixed in `a8fba8d` |
| 4 | `configure-php.sh` writes env values unquoted into www.conf | Breaks FPM config parse on bad passwords | HIGH — ✅ fixed in `5073dc1` |
| 5 | Mount path asymmetry: deploy-host `/var/www/$ENV/`, PHP/nginx `/var/www/` | Cached compiled container has wrong app_root | HIGH |
| 6 | settings.php paths use `/var/www/$_env/...` — only resolves on deploy-host | Missing hash_salt under FPM | HIGH (today's manual fix in-place) |
| 7 | `salt.txt` mode 0600 owned by ubuntu — unreadable by www-data | Missing hash_salt | HIGH (today's manual fix) |
| 8 | OPcache caches settings.php — won't see edits without FPM restart | Today's manual settings.php fix didn't take | MEDIUM |
| 9 | Drupal cache_container in DB held stale app_root | Cleared filesystem cache wasn't enough | MEDIUM |
| 10 | `/fsx/nginx/` didn't exist; configure-nginx.sh's mount silently failed | nginx had no vhosts; `||echo WARNING` antipattern | HIGH |
| 11 | PHPInstanceRole missing S3 read on image-builder bucket | Can't pull updated configure-php.sh at runtime | MEDIUM — ✅ fixed in `526e376` |
| 12 | `cf-iam` stack drift — template edits don't auto-deploy | `worxco/<env>/drupal/*` secret read denied for 6 days | HIGH (lurking) |
| 13 | `make build-amis` doesn't wait for pipelines | Subsequent `update-ami-param` finds OLD AMIs | MEDIUM |
| 14 | `make deploy-ami-params` doesn't exist — real target is `update-ami-param PIPELINE=…` (called 3x) | Confusing API surface | LOW |
| 15 | AWS::ImageBuilder::Component `Data` 16k limit — cfn-lint W1031 is load-bearing | Earlier in session, ✅ documented | DONE |
| 16 | `||echo WARNING` antipattern in boot scripts swallows real failures | All over configure-*.sh | MEDIUM |
| 17 | install-drupal.sh creates `drupal-private/` and files as `ubuntu:ubuntu 0600/0700` | PHP-FPM (www-data) can't read | HIGH |
| 18 | drupal.conf hardcoded to `/var/www/drupal/web` — won't work with `/var/www/$ENV/` mount | After mount change | HIGH (needs templating) |

## B. Source-code fixes to commit (proposed)

I'd group as **5 commits**:

**Commit 1 — `fix(boot): mount FSx at /var/www/$ENV across the fleet`**
- `image-builder/configs/configure-php.sh`: mount target `/var/www/$ENV` (mkdir -p first); fail loudly if mount fails (drop `||echo WARNING`, replace with `||{ echo FATAL; exit 1; }`)
- `image-builder/configs/configure-nginx.sh`: same; also fail loudly on `/fsx/nginx` mount failure
- `image-builder/configs/nginx/nginx.conf`: change `include /etc/nginx/shared/...` to a path under `/var/www/$ENV/nginx/...` — but since this is baked at AMI build, simpler: keep `/etc/nginx/shared` as a mount target but mount it from `/fsx/nginx/` (no per-env subdir since FSx is per-env already)
- Bump `RecipeVersion` to 1.0.9

**Commit 2 — `fix(install-drupal): use mount-agnostic paths, fix permissions, fix psql heredoc`**
- `scripts/deploy-host/install-drupal.sh`:
  - settings.php paths: keep `dirname(__DIR__, 4) . '/drupal-private/...'` (mount-agnostic and works both ways)
  - After install: `chgrp -R www-data drupal-private drupal-config`, `chmod 0640 salt.txt`, `chmod 0750` on dirs
  - Change the psql heredoc that creates the DB user to use `psql -c` with proper escaping (avoids shell expansion of `$` and backticks in passwords)
  - Add an `opcache_reset()` call at the end via `drush eval 'opcache

*[Response truncated at 4000 characters]*

---

### 11:42:04 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
