# AI Prompt Log

**Date**: 2026-07-20 14:33:13
**Week**: 30
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:33:13 - User Prompt

```
Okay, based on the salt issue in item 1, let's ignore it for the time being. I agree. We need to defer it. 
Let's go with B on item two. Let's go with B, the jump box ferry
```

---

### 14:35:01 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/scripts/deploy-host/restore-codebase.sh`

---

### 14:35:28 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 14:35:38 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 14:35:43 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 14:35:48 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 14:35:52 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 14:36:19 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/.git/COMMIT_MSG_DRAFT`

---

### 14:36:34 - Response

Item 3 staged. Ready:

```bash
git commit -F .git/COMMIT_MSG_DRAFT
git push
```

**Say the word and I'll immediately move on to Item 2 (S3-hosted PDF media via jumpbox ferry).** Rough shape for that:

1. New `migration/scripts/jumpbox/dump-s3-media.sh` — runs on prod jumpbox, does `aws s3 sync <prod-drupal-media> <sandbox-migration-bucket>/media-snapshot/` (uses jumpbox's cross-account write to sandbox migration bucket, same mechanism as dump-mysql/dump-files). Incremental — only changed objects transfer on subsequent runs.

2. New `migration/scripts/deploy-host/restore-s3-media.sh` — runs on sandbox deploy-host, does `aws s3 sync <sandbox-migration-bucket>/media-snapshot/ <sandbox-drupal-media-bucket>/`. Also incremental.

3. `make dump-s3-media` + `make restore-s3-media` + `make dispatch-restore-s3-media`.

4. Drupal side — `install-drupal.sh` writes flysystem config block into settings.php pointing at `getenv('DRUPAL_MEDIA_BUCKET')`.

5. `refresh-env-config` — fetch bucket name from SSM `/${ENV}/s3/media-bucket`, export `DRUPAL_MEDIA_BUCKET`.

6. `configure-php.sh` — add `env[DRUPAL_MEDIA_BUCKET]` line to PHP-FPM pool config.

Two things I'll need from you when we get there:
- **Prod drupal-media bucket NAME** (probably `production-drupal-media-<suffix>` — or is it different?). We can look it up from prod-side CFN if easier.
- **Confirmation that prod jumpbox already has cross-account write to sandbox migration bucket.** (It must — dump-mysql does the same thing today.)

Commit first, then let's go.

---

### 14:36:34 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
