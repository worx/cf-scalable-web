# AI Prompt Log

**Date**: 2026-07-20 10:21:31
**Week**: 30
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 10:21:31 - User Prompt

```
I think we should do A - yes! but, Let's take the time and get SALT into Secrets Manage ASAP - so, I guess I am saying both A & B
```

---

### 10:23:54 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/verify-drupal-installed.sh`

---

### 10:24:13 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 10:24:30 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/migration/Makefile`

---

### 10:25:05 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/.git/COMMIT_MSG_DRAFT`

---

### 10:25:17 - Response

Staged. Ready:

```bash
git commit -F .git/COMMIT_MSG_DRAFT
git push
```

## After push, we tackle Option B

That's the substantive one. Rough shape:

1. **`install-drupal.sh`** — generate salt to Secrets Manager (`worxco/$ENV/drupal/hash-salt`). If existing salt.txt on disk detected during rerun, migrate its contents into SSM as a one-time upgrade.
2. **`refresh-env-config`** — fetch salt from SSM, export `DRUPAL_HASH_SALT` in `/etc/worxco/envs/$ENV`.
3. **`settings.php` template** — `$settings['hash_salt'] = getenv('DRUPAL_HASH_SALT');`
4. **PHP-FPM pool config** — include `DRUPAL_HASH_SALT` in `env[]` directives so workers see it.
5. **`restore-private.sh`** — drop salt.txt from `PRESERVE_FROM_BAK` default (it's not there anymore). Keep the mechanism for future files.
6. **docs/memory/salt-persistence-design.md** — update to note implementation.
7. **Prereq for existing sandbox**: rerun `make install-drupal` (or a one-time helper) to migrate existing salt into SSM.

Bigger commit set. Say the word after commit + push and I'll swing at it.

---

### 10:25:17 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
