# AI Prompt Log

**Date**: 2026-07-21 16:01:53
**Week**: 30
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 16:01:53 - User Prompt

```
sandbox env mounted - "mount | grep drupal" - df shows /var/www and /etc/nginx/shared - both on FSX mount points... there is no 'drupal' to grep in mount - might be better to search for (grep) fsx or /fsx/www to validate.

next steps...
1) confirmed bucket in env
2) afraid we need to do the long term - my only 'knowledge' to reset PHP is to push it over (terminate) and let a new copy come up. If we need code on that box to find the S3 stuff, then we should probably re-build it. I don't know who to SSM into those boxes (I guess I could go to UI and find instanceID for each) Assuming I have the InstanceID - what command(s)/processes do I need to run on the PHP boxes?
```

---

### 16:03:13 - Response

Everything's in place. Here's the full picture:

## Path A: Long-term, correct — full rundown

Your commit `4959433` added the `env[AWS_S3_BUCKET]` write to `image-builder/configs/configure-php.sh`. That file needs to reach S3 → Image Builder → new AMI → new instance. Roughly ~30 min end to end. Steps:

```bash
# From your Mac (all one env — sandbox):

# 1. Push the updated configure-php.sh into the image-builder S3 bucket.
#    Fast (a few seconds). Image Builder reads from there at build time.
make upload-build-configs ENV=sandbox

# 2. Trigger all 3 Image Builder pipelines (nginx, php74, php83) and
#    WAIT (~25 min per docstring — actually parallel, so ~25 min total).
make build-amis ENV=sandbox

# 3. Register the new AMI IDs into SSM so CloudFormation can pick them up.
make update-ami-params ENV=sandbox

# 4. Re-deploy the PHP compute stack — the launch template picks up the
#    new AMI ID from SSM. `ForceUpdateToken=$(date +%s)` makes CFN see
#    the change even if nothing else moved.
make deploy-compute-php ENV=sandbox

# 5. Cycle the instances. Your instinct — terminate and let ASG relaunch —
#    IS correct at THIS point, because the launch template now points at
#    the new AMI. Two ways:
#    5a. Terminate manually (UI or CLI) — ASG replaces from new AMI:
#          aws autoscaling describe-auto-scaling-groups \
#            --auto-scaling-group-names sandbox-php83-asg sandbox-php74-asg \
#            --query "AutoScalingGroups[].Instances[].InstanceId" --output text
#          aws ec2 terminate-instances --instance-ids <IDS...>
#    5b. Trigger a proper instance refresh (rolling, respects health checks):
#          aws autoscaling start-instance-refresh \
#            --auto-scaling-group-name sandbox-php83-asg
#          aws autoscaling start-instance-refresh \
#            --auto-scaling-group-name sandbox-php74-asg

# 6. Wait ~5-10 min for new instances to come up healthy, then verify.
```

## SSM into a PHP box to verify

To find instance IDs — no need for the UI:

```bash
aws --profile ZI-Sandbox autoscaling describe-auto-scaling-groups \
  --auto-scaling-group-names sandbox-php83-asg sandbox-php74-asg \
  --query "AutoScalingGroups[].Instances[?LifecycleState=='InService'].{ASG:AutoScalingGroupName,Id:InstanceId}" \
  --output table --region us-east-1
```

Then SSM in:
```bash
aws --profile ZI-Sandbox ssm start-session --target i-XXXXXXXXXXXXX --region us-east-1
# once in:
sudo su -
grep AWS_S3_BUCKET /etc/php/*/fpm/pool.d/www.conf
# Expected: env[AWS_S3_BUCKET]    = "sandbox-drupal-media-kv-worxco"
sudo -u www-data php -r 'echo getenv("AWS_S3_BUCKET") . PHP_EOL;'
# Expected: sandbox-drupal-media-kv-worxco
```

## Suggested order right now

Start Step 1 + Step 2 now — they can run in the background while you focus on something else for 25 min. Report back when `make build-amis` completes and we'll fire steps 3–6.

Want me to walk you through anything else before you kick it off, or just fire steps 1 and 2 now?

---

### 16:03:13 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
