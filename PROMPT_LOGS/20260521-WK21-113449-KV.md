# AI Prompt Log

**Date**: 2026-05-21 11:34:50
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:34:50 - User Prompt

```
Yeah, okay, so I did the make deploy all X, and you know that phase two really needs a little bit of fiddling because it's back out there as part of the image builder rebuilding the AMIs again. <sigh>
```

---

### 11:37:01 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/check-ami-currency.sh`

---

### 11:37:10 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 11:37:17 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 11:39:17 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 11:39:39 - Response

Wired up. Summary of what landed:

**`scripts/check-ami-currency.sh`** (new, executable)
- Reads `RecipeVersion` from `cloudformation/parameters/image-builder-$ENV.json`
- For each of nginx/php74/php83: reads the AMI ID from SSM `/$ENV/ami/<pipeline>`, fetches that AMI's `Ec2ImageBuilderArn` tag, parses the version segment
- Exits 0 if all 3 match; exits 1 if any are stale, missing, or untagged
- Prints a per-pipeline status table

**`Makefile`**
- New target `build-amis-if-needed` — runs the check; on match prints "✓ skipping" + the force-rebuild hint; on mismatch calls existing `build-amis`
- Phase 2 of `deploy-allX` now calls `build-amis-if-needed` instead of `build-amis`
- `build-amis` itself is unchanged — still the force-rebuild path (`make build-amis ENV=...` always rebuilds), and per-pipeline `build-ami-nginx/php74/php83` are also unchanged
- Added the new target to `.PHONY`

**Design notes (for future you):**
- This is the coarse-grained version: if ANY one pipeline is stale, all 3 rebuild. The fine-grained "rebuild only the stale ones" version would split `build-amis-async` to be selective. Not worth the complexity yet — the common case is "all 3 current after a fresh `make build-amis`" or "all 3 stale after a recipe version bump."
- "Untagged" AMIs (manually imported, etc.) are treated as needing rebuild — we can't verify currency, so be conservative.
- The "~25 min long pole" heartbeat in Phase 2 prose is now optimistic in the other direction when AMIs skip — left alone for now.

Won't help the current bake (already in flight). Next deploy-allX will skip Phase 2's AMI track when nothing's changed.

---

### 11:39:39 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
