# AI Prompt Log

**Date**: 2026-07-31 10:43:37
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 10:43:37 - User Prompt

```
ok - the destroy-all is rerunning.

for the deploy-all next up
What would be a good 'script' or list of commands that will
Deploy-all
build-drupal
(don't) do a new dump - we will use the wednesday dumps
full migration (DB, codebase, files, private - which is still empty)
-any steps I'm not thinking of-
```

---

### 10:44:29 - Response

Here's the clean sequence — everything else is now automated by tonight's fixes:

## The whole thing, start to finish

```bash
# ============================================================
# From your Mac (or deploy-host — either works for deploy-all)
# ============================================================
cd ~/projects/cf-scalable-web  # or wherever your Mac copy is
git pull    # make sure you're on the latest (fff916c)

# 1. Deploy all infrastructure — ~50 min
#    The new `dns` track fires ~15 min in (right after ALB is up).
#    By the time everything else is done, DNS negative-cache is long expired.
make deploy-all

# ============================================================
# Then move to the deploy-host for the migration
# ============================================================
make ssm-deploy-host       # or however you SSM in

# Inside the SSM session:
cd ~/projects/cf-scalable-web
git pull    # latest fixes need to be here for migrate-full-all
cd migration

# 2. Full migration in one shot — ~90-150 min
#    Uses the S3 dumps from Wednesday (2026-07-29) automatically.
#    AUTO=yes triggers install-drupal on the fresh env before migrating.
make migrate-full-all AUTO=yes

# ============================================================
# When it's done, smoke test
# ============================================================
# Front page:
open https://sandbox.envs.zoning-info.com/     # from Mac

# Admin login (one-time URL):
make admin-login-url ENV=sandbox
```

## What migrate-full-all AUTO=yes now does automatically (nothing to remember)

| Phase | Action |
|-------|--------|
| **-2** | `refresh-env-config sandbox` — rewrites `/etc/worxco/envs/sandbox` from fresh SSM values ← from tonight's fix (922ae5e) |
| **-1** | preflight → detects no Drupal → auto-runs `install-drupal` → NOW self-sufficient (installs + publishes vhost + reloads nginx) ← 4c6106e |
| **0** | db-backup — no-op on fresh install |
| **1** | restore-mysql (S3 dump → local MariaDB scratch, ~5 min) |
| **2** | pgloader (MariaDB → RDS PostgreSQL, ~1.5 hr) ← HOME fix from 8d51ff3 |
| **3** | clear-drupal-cache |
| **4** | sync-s3-media (prod media bucket → sandbox, ~30-60 sec incremental) |
| **5** | restore-files (3.9 GB → FSx, ~5-10 min) |
| **6** | restore-private (546 B → FSx, ~2 sec) |
| **7** | restore-codebase (700 MB tarball, sibling packages, ~5 min) |
| **8** | final clear-drupal-cache |

## Things you WON'T have to do this time (thanks to tonight's fixes)

- ❌ Manual `refresh-env-config sandbox`
- ❌ Manual `make publish-dns` after everything's up
- ❌ Manual `make reload-nginx` because install-drupal didn't
- ❌ Manual DNS cache flush / 24-hour wait
- ❌ Manual purge of S3 versions before destroy
- ❌ Manual sibling package tarballing to React2

## Time expectation

- **deploy-all:** ~50 min
- **migrate-full-all:** ~2 hours (pgloader dominates)
- **Total wall-clock:** ~2.5-3 hours

## Interruption safety

Both are walk-away-safe. `deploy-all-parallel.py` polls in-place and prints state. `migrate-full-all` uses SSM dispatch, so the actual work continues even if your SSM session drops. You can `make ssm-deploy-host` back in and re-tail logs anytime.

## Fingers crossed on the destroy first

Once the current destroy-all-parallel completes (with the s3 fix), the deploy-all is next. Ping me at any phase completion or if something errors.

---

### 10:44:29 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
