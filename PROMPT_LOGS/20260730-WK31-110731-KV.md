# AI Prompt Log

**Date**: 2026-07-30 11:07:31
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:07:31 - User Prompt

```
 [notice] Performed install task: install_install_profile
 [notice] Performed install task: install_configure_form
 [notice] Performed install task: install_finished
 [success] Installation complete. (Admin)

============================================
STEP: Replace settings.php with env-var-driven config
============================================

============================================
STEP: Export env vars settings.php reads (for the rest of this script)
============================================

============================================
STEP: Make Drupal-writable directories accessible to PHP-FPM (www-data)
============================================
[16:02:13]   permissions set on /var/www/drupal/web/sites/default/files (root:www-data, g+rwX)
[16:02:13]   permissions set on /var/www/drupal-private (root:www-data, g+rwX)
[16:02:13]   permissions set on /var/www/drupal-config (root:www-data, g+rwX)

============================================
STEP: Uninstall the Update Manager module (architecturally unreachable)
============================================
[16:02:13] Uninstalling Update Manager module (telemetry to updates.drupal.org is unreachable from compute fleet)
 [success] Successfully uninstalled: update

============================================
STEP: Publish Drupal nginx vhost to FSx (read by all nginx instances)
============================================
[16:02:21]   wrote /etc/nginx/shared/sites-enabled/drupal.conf

============================================
STEP: Verify install via drush
============================================
Drupal version   : 11.4.4         
DB driver        : pgsql          
Database         : Connected      
Drupal bootstrap : Successful     
Site URI         : http://default 
PHP version      : 8.3.6          

============================================
STEP: Drop install marker
============================================
scripts/deploy-host/install-drupal.sh: line 717: ./write-install-marker.sh: No such file or directory
make[2]: *** [Makefile:1039: install-drupal] Error 127
AUTO install-drupal failed. Check its output above; probably need manual intervention.
make[1]: *** [Makefile:769: migrate-db-all] Error 1
make: *** [Makefile:862: migrate-full-all] Error 2
```

---

### 11:09:23 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/install-drupal.sh`

---

### 11:09:33 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/install-drupal.sh`

---

### 11:10:13 - Git Commit

**Commit:** `42907b351779`

**Message:**
```
fix(install-drupal): resolve SCRIPT_DIR before any cd

Bug caught 2026-07-30 during the FIRST successful Phase -1 run
(sudo -E fix from a1a85bc worked). Install-drupal.sh got through
composer install, drush site:install, settings.php write, permissions,
Update module uninstall, drupal.conf vhost publish, and drush verify.
Then failed on the very last step:

    scripts/deploy-host/install-drupal.sh: line 717: ./write-install-marker.sh: No such file or directory
    make[2]: *** [Makefile:1039: install-drupal] Error 127

## Cause

Line 717 was invoking a sibling script via:

    "$(dirname "$(readlink -f "$0")")/write-install-marker.sh" "$ENV"

Which is CORRECT if cwd is where "$0" is invocable relative to.
Problem: install-drupal.sh does `cd $DRUPAL_ROOT` earlier (for drush
site:install and subsequent drush calls) and never cds back. By line
717, cwd is /var/www/drupal/. "$0" is the RELATIVE path given at
invocation (`scripts/deploy-host/install-drupal.sh`), and there's
no scripts/deploy-host/ subdirectory inside /var/www/drupal/. So
`readlink -f "$0"` returns empty. Then `dirname ""` returns `.`.
Result: `./write-install-marker.sh` → 127 (file not found in cwd).

## Fix

Capture SCRIPT_DIR at the top of the file (before any cd) using
${BASH_SOURCE[0]} — the always-correct bash-idiomatic pattern:

    SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

Then reference sibling scripts as "$SCRIPT_DIR/…". Immune to any
future cd's in the script.

Added an explanatory comment block above the SCRIPT_DIR line
documenting the exact bug this pattern avoids, so it doesn't get
"cleaned up" back to the broken form by future refactoring.

## What happened to sandbox

install-drupal.sh completed almost all its work before line 717
failed. State on FSx:
- /var/www/drupal/ — full composer install + drush-installed DB
  tables in RDS + written settings.php + published vhost + working
  drush verify
- /var/www/drupal-private/salt.txt — written with correct ownership
- .installed marker — NOT written (line 717 was going to do that)

Recovery on the botched sandbox for next migrate-full-all run:
    make remove-drupal ENV=sandbox
    git pull
    cd migration
    make migrate-full-all AUTO=yes

Or if operator wants to save time: skip remove-drupal, just re-run
migrate-full-all with SKIP_PREFLIGHT=yes and manually write the
install marker via `make create-installed ENV=sandbox` (which uses
the same write-install-marker.sh via an absolute path).

## Files Modified

- scripts/deploy-host/install-drupal.sh — capture SCRIPT_DIR at top,
  use it at line 734 (was 717) for the write-install-marker call

Tests: bash -n clean.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

**Files:**
```
M	PROMPT_LOGS/20260730-WK31-105036-KV.md
A	PROMPT_LOGS/20260730-WK31-105546-KV.md
A	PROMPT_LOGS/20260730-WK31-105818-KV.md
A	PROMPT_LOGS/20260730-WK31-110731-KV.md
M	scripts/deploy-host/install-drupal.sh
```

---

### 11:10:29 - Response

Pushed as `42907b3`.

## Recovery — two paths, pick whichever

**Path A — nuke and re-fire (clean):**
```bash
cd ~/projects/cf-scalable-web
git pull
make remove-drupal ENV=sandbox
cd migration
make migrate-full-all AUTO=yes
```

**Path B — save the mostly-done install (faster if you're impatient):**
```bash
cd ~/projects/cf-scalable-web
git pull
sudo make create-installed ENV=sandbox   # writes the marker via write-install-marker.sh (absolute path)
cd migration
make migrate-full-all SKIP_PREFLIGHT=yes   # skip re-install; go straight to DB migration
```

Path B skips ~10-15 min of composer install + drush site:install rerun. But it assumes the install-drupal-completed-except-for-marker state is safe (which it is — everything else worked).

## Rationale for Path B being viable

The install produced:
- ✅ Composer install (vendor/ populated)
- ✅ drush site:install (fresh Drupal tables in `zinew` schema)
- ✅ settings.php written (env-var driven, flysystem_s3, etc.)
- ✅ salt.txt written with correct ownership (sudo -E worked)
- ✅ Permissions on files/, drupal-private/, drupal-config/
- ✅ Update module uninstalled
- ✅ drupal.conf vhost published to FSx
- ✅ drush verify passed — Drupal fully operational
- ❌ .installed marker not written (line 717 failure)

The marker is metadata. Nothing structural is missing.

Your call. Path B is faster; Path A is a cleaner test of the full end-to-end.

---

### 11:10:29 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
