# AI Prompt Log

**Date**: 2026-07-20 11:52:54
**Week**: 30
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:52:54 - User Prompt

```
Let's review all the TODOS for this project. I know we have discussed NodeJS, LaTeX, and others.... Let's get an abbreviated list for review and then we can assign some priorities
```

---

### 11:54:25 - Response

Compiled an abbreviated view. Cross-referenced against what's actually shipped so we can retire items too.

## Recently completed today (should get retired from TODO.md)

| # | From `~/.claude/TODO.md` | Completed by |
|---|-----|------|
| ✅ | `_ssm-run-deploy-host.sh` + Mac-side dispatch targets | commit `e1300e9`, `0286ef7` |
| ✅ | Install session-manager-plugin on deploy host | commit `9630bac` (in bootstrap.sh) |
| ✅ | Install database + cache CLI tools on deploy host | commit `9630bac` (pgloader, mariadb-client, plocate, htop, pv) |

## Still open — grouped

### Migration/testing (finish what we started)

| Item | Notes |
|------|-------|
| Salt → SSM transition cleanup | Drop file fallback in settings.php + `PRESERVE_FROM_BAK` default once every env verified running on env-var path |
| `restore-codebase` target (Drupal code from prod) | dump-codebase exists; no restore counterpart. Enables full parity migration |
| S3-hosted PDF media (flysystem_s3) | Sandbox needs bucket population + config to serve prod PDF links (deferred earlier) |
| Generalize `_ssm-run-deploy-host.sh` for top-level scripts | Currently scoped to migration/scripts/ only |
| `make refresh-deploy-host-scripts` target | Reinstalls copied scripts to /usr/local/sbin/ without a full bootstrap re-run |
| `make check-drift` for CFN template staleness | From `docs/memory/gotchas.md` — warns when local template is newer than deployed stack |

### Infrastructure / IaC hardening

| Item | Notes |
|------|-------|
| Account-ID-last-4 suffix on S3 bucket names | Pre-commercial-release; commercial multi-tenant safety |
| Tighter Secrets Manager IAM (least privilege) | Currently broad; scope down per-role |
| Ship deploy-host bootstrap log to CloudWatch | Post-mortem visibility for boot issues |
| Auto-load user public key on deploy-host from Secrets Manager | Currently manual/session-based |
| Full SES email stack with multi-domain support | Placeholder in configure-php; needs actual multi-domain |
| Weekly Image Builder schedule + 7-day instance lifecycle | Immutable-infra rotation |
| Auto-delete RDS snapshots on `destroy-database` | Cleanup ergonomics |
| VPC peering: deploy-host to project VPC | Currently isolated; peering would simplify some things |

### Makefile / tooling

| Item | Notes |
|------|-------|
| Recursive dependency resolution for deploy targets | `make deploy-compute` should chain prereqs |
| `make update-versions` — fetch latest AWS service versions | AMI/component version bumps |
| Fix `make test` RDS Multi-AZ failure | Existing bug |
| Fix `make ssm-report` default profile error | Existing bug |
| Fix `ssm-audit-params` exit code when optional params missing | Existing bug |
| SSM session connect tooling (list + interactive picker) | Nice-to-have UX |

### Major design work (Phase D+)

| Item | Notes |
|------|-------|
| **Drupal upgrade + promote infrastructure (Phase D)** | The BIG one — `composer.lock` + config sync + `make upgrade-drupal ENV=…` + `make promote-drupal FROM=… TO=…` |
| **Multi-tenancy refactor (Phase E)** | Per `docs/plans/multi-tenancy.md` — deferred; requires site-repo template pattern first |

### Docs / research

| Item | Notes |
|------|-------|
| NodeJS tier proposal | `docs/research/nodejs-tier.md` — extension idea, currently just design |
| LaTeX → Lambda PDF pipeline | `docs/research/latex-lambda.md` — related to docs auto-generation |
| Generate printable book from project documentation | The "final deliverable" — pandoc + latexmk pipeline to a single bound PDF |
| Rewrite `MIGRATION.md` as `MIGRATION-RUNBOOK.pdf` via LaTeX | Regenerate cleanly from `docs/MIGRATION.md`; throw away `tt.pdf`/`tt.html` |

## Sanity checks

- **Nothing on this list is a live-fire bug blocking prod.** Some are minor bugs; most are enhancements or design initiatives.
- **The recently-completed migration work exposed opportunities** (drift check, script sync helper) that surface now that we're actively using the pipeline.
- **Phase D an

*[Response truncated at 4000 characters]*

---

### 11:54:25 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
