# AI Prompt Log

**Date**: 2026-05-08 15:11:14
**Week**: 19
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 15:11:14 - User Prompt

```
Option A — clean install validation - seems the most prudent to me.
```

---

### 15:11:54 - Response

Here's the clean sequence for the deploy-host. All commands work from there (instance role provides credentials):

```bash
# Get the latest code
cd ~/projects/cf-scalable-web && git pull
make install-helpers     # belt-and-suspenders

# Step 1: Tear down the existing Drupal install
make remove-drupal ENV=sandbox

# Step 2: Force-delete the auto-created secrets (no 30-day wait)
aws secretsmanager delete-secret \
  --secret-id worxco/sandbox/drupal/admin-password \
  --force-delete-without-recovery \
  --region us-east-1
aws secretsmanager delete-secret \
  --secret-id worxco/sandbox/drupal/db-password \
  --force-delete-without-recovery \
  --region us-east-1

# Step 3: Deploy the cf-app-drupal stack
make deploy-app-drupal ENV=sandbox

# Step 4: Verify what the stack owns
make verify-app-drupal ENV=sandbox

# Step 5: Reinstall — install-drupal reads stack-owned secrets/params
make install-drupal ENV=sandbox

# Step 6: Verify the install
make verify-drupal ENV=sandbox
```

## What to watch for

In step 5, the install log should show:
- `Resolving Drupal DB user password (Secrets Manager: worxco/sandbox/drupal/db-password)...`
- **`Using existing password from Secrets Manager`** ← this confirms it's reading what the stack created (not generating)
- Same for the admin password

If you see "Secret not found — generating new password" in step 5, the stack didn't actually create them — we'd debug.

## After install succeeds

Browser test to confirm everything threaded together:

```bash
source /etc/worxco/envs/sandbox
export DRUPAL_DB_PASS="$(aws secretsmanager get-secret-value \
  --secret-id worxco/sandbox/drupal/db-password \
  --query SecretString --output text)"

# Use tmux this time so the server survives detach
tmux new-session -d -s drupal-cloud \
  "cd /var/www/sandbox/drupal && vendor/bin/drush runserver 0.0.0.0:8080"

# Get the admin password to log in
echo "Admin password: $(aws secretsmanager get-secret-value \
  --secret-id worxco/sandbox/drupal/admin-password \
  --query SecretString --output text)"
```

Then on Mac: `ssh -L 8080:localhost:8080 deploy-host` → `http://localhost:8080` → `admin` + that password.

If the site loads with stack-managed credentials end-to-end, **Phase C step 3 is validated**. The only Phase C piece left after that is PHP-FPM boot script wiring (so ALB traffic hits the real infrastructure path). That's a smaller scoped change.

Run through the sequence and tell me where it lands.

---

### 15:11:54 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
