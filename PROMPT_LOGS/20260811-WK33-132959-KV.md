# AI Prompt Log

**Date**: 2026-08-11 13:29:59
**Week**: 33
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 13:29:59 - User Prompt

```
I am watching htop on migrate-host - very little activity - watching r7g.xlarge RDS instance - very little activity.
and - it just died - failure to access /var/www - not sure why pgloader needed access to /var/www.....
color me confused
```

---

### 13:31:27 - Response

Real bug on my side, and it's actually two of them stacked. Let me explain:

**Bug 1 (immediate cause):** `restore-mysql.sh` defaults `DUMP_LOCAL_PATH` to `/var/www/mysql/zinew.sql` — that's the FSx path on deploy-host. Migrate-host has no `/var/www`. Phase 2 marker-guarded the SQL bulk-load wrap but missed guarding this path default. My miss.

**Bug 2 (design gap I should have caught):** `HOST_STACK=cf-migrate-host` propagates through **all** of migrate-full-all, including the file/private/codebase restore steps in Phases 5-7. Those write to `/var/www/...` which is ONLY on deploy-host (FSx mount). Even if bug 1 were fixed, phases 5-7 would still crash on migrate-host. `migrate-full-all-boosted` was over-scoped — the RDS boost only helps pgloader, and full-all's file syncs shouldn't cross the HOST_STACK line.

**Current state to check:**

Confirm the RDS revert trap fired (should be back at db.t4g.micro) and clean up SSM param if not:
```bash
scripts/rds-migration-tuning.sh sandbox revert   # no-op if trap already ran; harmless
# Sanity check current class:
aws --profile ZI-Sandbox rds describe-db-instances \
  --db-instance-identifier sandbox-postgres \
  --query 'DBInstances[0].DBInstanceClass' --output text --region us-east-1
# Expect: db.t4g.micro
```

**Two paths from here — which fits your energy level:**

**A) Call it for the night, fix both properly tomorrow.**
- `make destroy-migrate-host CONFIRMED=yes` (auto-backup fires)
- Then `make destroy-all` if you want the full teardown
- I write correct fixes tomorrow: marker-guard DUMP_LOCAL_PATH + scope HOST_STACK to db-only

**B) One more shot tonight with a narrower target.**
- I push a fix for bug 1 (marker-guard restore-mysql.sh, ~5 min my end)
- You use `migrate-db-all-boosted` (NOT `migrate-full-all-boosted`) — bypasses bug 2 by only running Phases 1-3 (restore-mysql + pgloader + cache-clear, all DB stuff, no file syncs)
- We get the pgloader-with-boost timing number we've been chasing
- Full-all fix comes tomorrow

Path B gets you the answer to the "does the RDS boost work end-to-end?" question tonight. Just narrower — no file restores exercised. Which do you prefer?

---

### 13:31:27 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
