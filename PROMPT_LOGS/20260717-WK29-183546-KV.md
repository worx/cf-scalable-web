# AI Prompt Log

**Date**: 2026-07-17 18:35:46
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 18:35:46 - User Prompt

```
yes memorize the structural checks preference

I think our next step is to automate this whole process so that it works cradle to grave and end to end, without you giving me steps to run and me running steps. I mean, outside of you giving me the step to do a make migrate DB and have it go out and do the whole thing: the backup, the local build, and the actual copy across 

The drop database on the backup is probably a good idea, but I think the RDS has a minimum disk size. As long as we're not exceeding that minimum disk size, having one, two, or 78 databases doesn't really change pricing. It's the size of the databases that will change the pricing. I'll probably drop that. There's no reason to keep it at this point since I'm in a working system right now. 

And let's make sure that we run through all those follow-ups before we try to completely automate everything, as I think they are intimately involved in making that whole thing work.
```

---

### 18:36:35 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/structural-checks-over-markers.md`

---

### 18:36:52 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/.git/COMMIT_MSG_DRAFT`

---

### 18:37:11 - Response

Memory staged. Ready when approved.

## Follow-up execution order (proposed)

I'd sequence them roughly by dependency/risk, small to big:

**Quick wins first (unblocks or reduces friction, no structural change):**
1. **Structural `.installed` checks** — 4 file edits, mechanical, unblocks the pattern for future rebuilds. Same commit as the memory. (Once this lands, we can stop backfilling markers by hand forever.)
2. **run-pgloader.sh Phase 6 verify query bug** — one-line: `public` → `zinew`. Removes the misleading warning.
3. **Add `/var/www/drupal/.installed` to BACKUP_PATHS** + fix `install-drupal.sh:643` to chown www-data. Two-line edits, closes the gap on future instance replacements losing the marker.
4. **Makefile verify-drupal path bug** — either fix in place (`/var/www/$(ENV)/drupal` → `/var/www/drupal`) or drop the whole check and swap to the structural pattern. Prefer the latter — same as #1.

**Documentation catchup (unblocks understanding, small):**

5. **`migration/README.md`** — write the actual runbook based on today's test. What each phase does, when to use which env override, how to read logs.
6. **`docs/MIGRATION.md`** — rewrite for post-consolidation flow. Delete or heavily revise the stale content.

**Medium — new capability, still bounded:**

7. **`make db-backup ENV=…` / `make db-restore ENV=…`** wrappers — encapsulate today's rename+create+cleanup dance behind two make targets. This is the primitive `make migrate-db` will call under the hood.
8. **Files / private restore path** — dump-files + dump-private are done, we need `restore-files` and `restore-private` on the FSx side. Non-trivial (untar into `/var/www/drupal/web/sites/default/files/` and `/var/www/drupal-private/` respectively, with atomic swap for zero-downtime).

**Big — the automation payoff:**

9. **`_ssm-run-deploy-host.sh` + Mac-side dispatch targets** — mirrors `_ssm-run-jumpbox.sh`. `make dispatch-restore-mysql`, `make dispatch-run-pgloader`, `make dispatch-clear-drupal-cache`.
10. **`make migrate-db` uber-target** — chains dispatch-dump-mysql → dispatch-restore-mysql → dispatch-run-pgloader → dispatch-clear-drupal-cache, each in its own SSM send-command, polling for success before the next fires. Kickable from Mac, walk-away safe. Optionally: `make migrate-full` also chains files/private restore.

## Approach

I'd suggest each item is its own commit — small, reviewable, revertable. #1 pairs well with the memory commit if you want to bundle. Otherwise commit the memory alone first.

**Commit + push the memory now, then start on #1?**

---

### 18:37:11 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
