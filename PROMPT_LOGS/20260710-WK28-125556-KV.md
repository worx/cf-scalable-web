# AI Prompt Log

**Date**: 2026-07-10 12:55:56
**Week**: 28
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 12:55:56 - User Prompt

```
I believe we are ready for 1B. However, I think we should take into account the documentation that's going to go with this and replace the tt.html and tt.pdf with repository-managed documentation. I doubt that you're going to forget it, but I just managed to take a look at a git status and saw that the tt files are still out there in the untracked. I just wanted to make sure that we remembered that they need to go away and that we need documentation to cover what we're doing.
```

---

### 12:57:24 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/migration/cf-migration-secret.yaml`

---

### 12:57:29 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/parameters/migration-secret.json`

---

### 12:57:42 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/migration/cf-migration-secret.yaml`

---

### 12:57:52 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/migration/cf-migration-jumpbox.yaml`

---

### 12:58:00 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/migration/cf-migration-jumpbox.yaml`

---

### 12:58:09 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/cloudformation/parameters/migration-jumpbox.json`

---

### 12:58:42 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 12:59:11 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 12:59:27 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 12:59:36 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 12:59:49 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 13:00:22 - Response

Step 1B complete. Summary of what changed:

## Files created

| File | Purpose |
|------|---------|
| `cloudformation/migration/cf-migration-secret.yaml` | Secrets Manager entry with placeholder password value |
| `cloudformation/parameters/migration-secret.json` | Default connection metadata (host, port, db, user) |

## Files modified

| File | Change |
|------|--------|
| `cloudformation/migration/cf-migration-jumpbox.yaml` | Added `MigrationSecretArn` parameter + IAM policy allowing `secretsmanager:GetSecretValue` scoped to that ARN |
| `cloudformation/parameters/migration-jumpbox.json` | Added `MigrationSecretArn` with wildcard fallback |
| `Makefile` | +37 lines: variables, three new targets, help text, `.PHONY` entries |

## Three new targets

```
make deploy-migration-secret          # Creates the CFN stack with placeholder pw
make set-migration-secret-password    # Prompts (-s), preserves other fields via jq, put-secret-value
make destroy-migration-secret         # Standard CONFIRMED=yes teardown pattern
```

Plus `deploy-migration-jumpbox` now looks up the real ARN from the deployed secret stack via `describe-stacks`, so the wildcard in the parameter file only serves as a fallback if the lookup fails.

## Verification

- ✅ JSON: both parameter files pass `jq empty`
- ✅ YAML: basic structure valid; would defer full `cfn-lint` to when it's installed
- ✅ `make -n` on all four targets renders clean shell commands
- ✅ `make help` shows all three new targets in the migration block
- ✅ Convention: `CONFIRMED=yes` skip pattern matches existing targets (verified against ~30 similar blocks)

## Operator flow (order of operations)

Everything's wired for a clean, ordered rollout:

```
make deploy-migration-bucket           # Sandbox side (existing)
make deploy-migration-secret           # Prod side, placeholder pw
make set-migration-secret-password     # Real pw goes in
make deploy-migration-jumpbox          # Uses real secret ARN in IAM
```

If someone runs `deploy-migration-jumpbox` without first deploying the secret, they get a clear error with the exact next command to run.

---

**Ready for Step 1C — the migration.mk skeleton?**

That's where we sketch every migration target as `@echo "TODO"` stubs so the `include` mechanism works and `make help` picks them up cleanly. Once we have that scaffold, we can build `restore-mysql.sh` (Step 2) directly against real Makefile plumbing instead of a moving target.

---

### 13:00:22 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
