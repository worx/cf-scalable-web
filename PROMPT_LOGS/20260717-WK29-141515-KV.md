# AI Prompt Log

**Date**: 2026-07-17 14:15:15
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:15:15 - User Prompt

```
<task-notification>
<task-id>ad2bfc7003a305eb9</task-id>
<tool-use-id>toolu_01XeVqY4cy642WSx9cntC1Uv</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-kurtvanderwater-Work-dev-cf-scalable-drupal/7b8a99bf-1d83-4398-b0c4-e6fb7f0e0db9/tasks/ad2bfc7003a305eb9.output</output-file>
<status>completed</status>
<summary>Agent "Understand install-drupal + current state" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>Findings from read-only recon. Critical bug flagged in #2/#3.

**1. What `make install-drupal ENV=sandbox` does** (target at Makefile:1028-1039, wraps `scripts/deploy-host/install-drupal.sh`, must run on deploy-host per line 1029 marker check):

Both (a) and (b). Full sequence in `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/install-drupal.sh`:
- Pre-flight: deploy-host marker, `/var/www` mounted, active env matches (lines 105-128), PHP extensions, RDS TCP reachable (170-175)
- Fetch/create Secrets Manager secrets `worxco/$ENV/drupal/{db-password,admin-password}` (180-243)
- As RDS master `dbadmin`, CREATE/ALTER `drupal_user` role, own `drupal` DB + `public` schema (246-291)
- Wipe `/var/www/drupal` if non-empty-no-marker (301-304)
- `composer create-project drupal/recommended-project` into FSx `/var/www/drupal` (319-325), then `composer require drush/drush` (330-331)
- `drush site:install standard` — creates tables + admin user in RDS DB `drupal` (352-371)
- Rewrite settings.php env-var-driven (374-449), uninstall Update module (494-530), publish nginx vhost to `/etc/nginx/shared/sites-enabled/drupal.conf` (532-615), write `/var/www/drupal/.installed` marker (627-643)

Duration: header says `~3-5 min` (Makefile:1028); the remote-dispatch wrapper `scripts/install-drupal-remote.sh:118` budgets 45 min wall for cold-bootstrap-plus-install; the two composer steps dominate. **Realistic: 5-10 min against a warm deploy-host.**

**2. What `verify-drupal` checks (CRITICAL BUG)**

Makefile:1058 checks `/var/www/$(ENV)/drupal/.installed` — WITH env in the path. But `install-drupal.sh:56,59` writes marker to `/var/www/drupal/.installed` — NO env in path. Comment at install-drupal.sh:53-55 explicitly says "post-cutover commit ee319d5" moved to env-less path. **`verify-drupal` was not updated for that cutover.** Same bug in Makefile:1074, 1078, 1081 (`cd /var/www/$(ENV)/drupal`). Same shape in `remove-drupal.sh` uses correct env-less path (INSTALL_DIR=`/var/www/drupal`, line 31).

Meaning: your "Drupal not installed" message from `make verify-drupal ENV=sandbox` is **inconclusive** — Drupal may actually be installed. To confirm directly, on deploy-host: `ls -la /var/www/drupal/.installed`.

**3. Current sandbox state — verification you can run**

`/var/www/drupal` existence: install-drupal.sh:310 `sudo mkdir -p` creates it, so if install ran the dir exists. Definitive check: `ssh deploy-host 'sudo use-env sandbox &amp;&amp; ls /var/www/drupal/.installed &amp;&amp; cat /var/www/drupal/.installed'`.

RDS state: connect via `psql-env sandbox -d drupal -c '\dt public.*'` and `-c '\dn'` (list schemas). If install-drupal completed, DB `drupal` contains ~90+ Drupal core tables in `public`.

**4. Runbook impact**

- `clear-drupal-cache.sh`: script (lines 54-72) will hard-fail if `/var/www` not a mountpoint, and quietly do nothing if `cache_*` tables absent ("no cache_* tables found" line 71). Safe.
- pgloader against empty sandbox: safe. Template `migration/pgloader/zinew.load.tmpl:48-50` only references `${DRUPAL_DB_NAME}` (resolves to `drupal`, the DB name — install-drupal.sh:65, cf-database.yaml:63-66); no Drupal-codebase cross-checks. `include drop` (line 52) is destructive per table but only touches tables pgloader itself creates.
- Re-running install-drupal after pgloader: install-drupal.sh:107-115 **hard-refuses** if `.installed` marker present. If marker absent but `/var/www/drupal` has files, lines 301-304 wipe the directory. It does NOT inspect RDS before running `drush site:install`, which drops-and-recreates Drupal tables. **`drush site:install` on a pgloader'd DB would clobber loaded prod data.** Use `make reinstall-drupal` only intentionally.

**5. DB vs schema**

- Postgres DB name is `drupal` — install-drupal.sh:65, refresh-env-config:57, cf-database.yaml:63-66, cf-app-drupal.yaml:165.
- `zinew` is a **schema** inside DB `drupal`, referenced from legacy `settings.php` (`'database'=&gt;'zinew'` in PROMPT_LOGS/20260709-...:54 is the OLD prod MySQL DB name; the migration plan is `'schema'=&gt;'zinew'` per PROMPT_LOGS/20260715-WK29-202654:92).
- pgloader template does NOT set a target schema. Two conflicting signals in the template comments themselves: line 24-25 says "pgloader writes to public schema"; but `run-pgloader.sh:294` verifies `table_schema='public'`. Assumption flag: **pgloader's actual write schema is not verified in code I can read here** — pgloader's default when converting from MySQL is to create a same-named schema (`zinew`), which contradicts the verify query at line 294. This is worth testing before relying on it.
- Safer rollback: **schema-level rename** `ALTER SCHEMA zinew RENAME TO zinew_backup_YYYYMMDD; CREATE SCHEMA zinew;` — surgical, preserves the `drupal` DB (which holds Drupal's core tables in `public`), and pgloader's next run rebuilds `zinew`. Database-level rename (`ALTER DATABASE drupal RENAME…`) would break everything: RDS endpoint SSM param, settings.php `DRUPAL_DB_NAME=drupal`, drupal_user grants, and would require `drush site:install` again.

**Assumptions flagged**
- Pgloader target-schema behavior (public vs zinew) unverified from code — check with `\dn` after a run.
- Duration estimate is based on script headers and prior prompt logs; sandbox-specific timing not directly observed.
- I did not create a plan file; if you need one persisted for later, I can output the content and you (or the parent agent) can save it.</result>
<usage><subagent_tokens>67458</subagent_tokens><tool_uses>20</tool_uses><duration_ms>133869</duration_ms></usage>
</task-notification>
```

---

### 14:17:20 - File Modified (Write)

- `/Users/kurtvanderwater/.claude/plans/zesty-plotting-squid.md`

---

### 14:32:33 - File Modified (Write)

- `/Users/kurtvanderwater/.claude/plans/zesty-plotting-squid.md`

---

### 14:46:57 - File Modified (Write)

- `/Users/kurtvanderwater/.claude/projects/-Users-kurtvanderwater-Work-dev-cf-scalable-drupal/memory/feedback-db-rollback-pattern.md`

---

### 14:47:38 - Response

Ready when you kick off Phase 0. Quick offer as a follow-up (not blocking): the DB-level rename pattern you described (rename current → create fresh → operate → drop or rename back — metadata-only, no I/O, sub-second, application-transparent once repopulated) is worth a note in `docs/memory/` so future me doesn't propose the AWS-snapshot rathole again. Say the word if you want me to draft that as a topical memory file (e.g. `docs/memory/db-rollback-pattern.md`) — separate commit, after we know the test worked.

---

### 14:47:38 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
