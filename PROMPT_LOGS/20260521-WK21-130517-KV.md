# AI Prompt Log

**Date**: 2026-05-21 13:05:17
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 13:05:17 - User Prompt

```
separate commits, go ahead
```

---

### 13:05:36 - Git Commit

**Commit:** `bf6187198bc3`

**Message:**
```
fix(install-drupal): export DRUPAL_DB_* env vars right after settings.php swap

The Update Manager uninstall step was failing with "Command pm:uninstall
was not found. Drush was unable to query the database." That misleading
error was drush's way of saying it couldn't bootstrap the DB connection.

Root cause: after `step "Replace settings.php with env-var-driven config"`,
settings.php reads DB credentials via getenv(). But the script only
exported DRUPAL_DB_HOST/PORT/NAME/USER/PASS in the verify step further
down — leaving every drush call in between (including pm:uninstall) with
no way to bootstrap. Earlier drush calls (site:install) worked because
they bypassed settings.php via the --db-url= flag.

Also dropped the `sudo -E HOME=/root` from the pm:uninstall — the script
already runs as root via SSM dispatch, and every other drush call in this
script runs without sudo. The inconsistency was the giveaway that
something was off about that call.

Files Modified:
- scripts/deploy-host/install-drupal.sh

Tests: re-run `make install-drupal-remote ENV=sandbox` after commit + push
Docs: n/a (script comments updated inline)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	scripts/deploy-host/install-drupal.sh
```

---

### 13:05:46 - Git Commit

**Commit:** `c81159131dde`

**Message:**
```
docs(memory): test environment design discussion

Captures the design discussion from 2026-05-21 for standing up a `test` env
alongside `sandbox`. Treats `test` as a prod-migration rehearsal environment
rather than "a second sandbox" — decisions are biased toward "test should
teach us how prod will behave," even when that costs slightly more.

Key decisions documented:
- Dual-AZ everywhere (matches prod). The ~$31/mo saving from single-AZ
  persistence isn't worth losing failover/routing/ASG rehearsal fidelity.
- Cost savings come from aggressive stop/start: pause-compute + RDS stop +
  optionally destroy-cache overnight. FSx and ALB stay running.
- MySQL → PostgreSQL migration via pgloader from the deploy-host (not DMS).
- Multiple PHP versions can coexist via the existing port-routing design.
- Gating question for next steps: prod's current Drupal + PHP version.

Files Modified:
- docs/memory/test-environment-design.md (created)

Tests: n/a (design memo)
Docs: this IS the doc

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
A	docs/memory/test-environment-design.md
```

---

### 13:05:54 - Response

Both committed and pushed.

- `bf61871` — the install-drupal fix
- `c811591` — the test-environment design memo

Deploy-host's next `git fetch` (which happens automatically as step 2 of `install-drupal-remote`) will pick up `bf61871`. Go ahead and re-run:

```bash
make install-drupal-remote ENV=sandbox
```

Standing by for the result.

---

### 13:05:54 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
