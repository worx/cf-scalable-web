# AI Prompt Log

**Date**: 2026-07-13 11:59:11
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:59:11 - User Prompt

```
run A first
```

---

### 12:01:18 - Response

**Two-part diagnosis:**

1. **`cf-migration-secret` stack doesn't exist** — you haven't run `make deploy-secret` yet, so:
   - The secret we're trying to fetch (`cf-migration/prod-mysql-zinew`) doesn't exist
   - The jumpbox role doesn't have permission for a secret that doesn't exist
   - AWS's error message is slightly misleading ("not authorized") — really it's both

2. **Jumpbox needs redeployment** — the currently-deployed jumpbox role has only `CrossAccountMigrationBucketWrite`. Our `cf-migration-jumpbox.yaml` template now includes `ReadMigrationSecret` (scoped to the secret ARN), but that's only in the template file, not in the deployed stack. The current EC2 instance is running with the old IAM policies.

## Fix — three commands in order

```bash
cd migration

# 1. Create the secret with a placeholder password
make deploy-secret

# 2. Populate the real prod MySQL password
make set-secret-password
# (prompts you for the worxco@zinew password securely)

# 3. Update the jumpbox IAM role to include ReadMigrationSecret
make deploy-jumpbox
# (updates the CFN stack; IAM changes take effect immediately, no EC2 reboot needed)
```

Then rerun the dry-run and Phase 2 should sail through:

```bash
make dump-mysql CONFIRMED=yes DRY_RUN=yes
```

## About the running EC2 instance

You don't need to destroy/recreate the jumpbox. IAM role policy changes take effect immediately on the next API call from the instance — `make deploy-jumpbox` will just update the stack in place (CFN detects the IAM change, updates the role in AWS, the instance profile still points at the same role, and the next `secretsmanager:GetSecretValue` from the instance succeeds).

## Also — the log made it to S3 cleanly

Note this line in the output:
```
Log uploaded: s3://sandbox-migration-kv-worxco/logs/2026-07-13/dump-mysql-20260713-170001Z.log
```

`log_upload_and_exit` fired even though the script errored, so the full trace is durably on S3 for post-mortem. That's the intended design — errors don't lose visibility.

Want to run the three fix commands now?

---

### 12:01:18 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
