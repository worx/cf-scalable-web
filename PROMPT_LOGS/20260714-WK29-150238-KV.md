# AI Prompt Log

**Date**: 2026-07-14 15:02:38
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 15:02:38 - User Prompt

```
I checked a few things:
- make list-jumpbox-backups - there is a new one timestamped at 2:00AM CST
- make ssm-deploy-host - working
- sandbox-migration-kv-worxco/dumps -- all 4 files, expected sizes, first at 23:14, last at 23:19 - much faster than I expected - NOT complaining :)
- sandbox-migration-kv-worxco/logs -- only log entry was from backjup-state - no logs for the dumps....
- left over dumps in root of sandbox-migration-kv-worxco -- removed

did we finish the state protection for deploy-host yet? or is that next on our list??
```

---

### 15:04:16 - Response

**All 4 dump logs are there** — you were looking at `logs/2026-07-14/` (today's UTC date, which is when the nightly backup logged), but the dumps ran during **07-13 CST evening** and went to `logs/2026-07-13/`.

## What actually landed last night

**In `logs/2026-07-13/`** (from your evening runs):
```
dump-mysql-20260713-231415Z.log     — mysql at 23:14:15 UTC (18:14 CDT)
dump-codebase-20260713-231513Z.log  — codebase at 23:15:13 UTC
dump-files-20260713-231732Z.log     — files at 23:17:32 UTC
dump-private-20260713-231903Z.log   — private at 23:19:03 UTC
```

Also visible: your dry-run logs from earlier in the day, your manual backup, the auto-restore run when the jumpbox got replaced.

**In `logs/2026-07-14/`** (today's UTC date):
```
backup-state-20260714-070035Z.log   — nightly scheduler at 07:00:35 UTC (02:00 CST)
```

**The 5-minute total is real.** Your dump-all completed in ~5 wall-clock minutes because:
- mysqldump streams; no local staging
- pigz + parallel S3 multipart upload
- Your prod DB (2.3 GB uncompressed) is well within streaming range
- Codebase compresses well, files are already mostly-compressed
- Private is essentially instant

The 30-45 min estimate was conservative — real-world networks are usually faster than pessimistic projections.

## Slight gotcha to remember for future audits

`logs/YYYY-MM-DD/` uses **UTC date** (from `date -u +%Y-%m-%d` in `_common.sh`'s `log_upload_and_exit`). CDT evening runs after 19:00 will land in the next day's UTC folder. Not a bug — just a "did I look in the right folder" issue.

If it bugs you, one fix: change the log_upload path to use local time. Trade-off is timestamp-vs-folder mismatch would look confusing in a different way (log named `181432Z` in a folder called `2026-07-13` where Z-time actually indicates 07-14).

## Nightly scheduler proof

Perfect end-to-end proof that:
- EventBridge Scheduler fired at 02:00 America/Chicago ✓
- Scheduler execution role could `ssm:SendCommand` to jumpbox ✓
- Jumpbox downloaded scripts from S3 successfully ✓
- backup-state.sh ran, tar'd state, uploaded to S3 ✓
- Log_upload_and_exit shipped its log ✓

Full lifecycle validated.

## `make ssm-deploy-host` ✓

Nice — confirms yesterday's commit push landed cleanly on deploy-host.

## Deploy-host state protection — no, NEXT on the list

We didn't do it yet. It was #1 on the follow-up list I noted last night. Ready to start now if you want:

**Proposed scope (per your design from yesterday):**

```
Deploy-host state protection:
├── S3 backup (like jumpbox pattern):
│   ├── /root/.ssh
│   ├── /root/.env
│   ├── /root/bin
│   ├── /root/.aws/config          ← config only, NOT credentials
│   ├── /home/ubuntu/.ssh
│   ├── /home/ubuntu/.env
│   ├── /home/ubuntu/bin
│   └── /home/ubuntu/.aws/config
│
└── Post-restore stub creation:
    ├── /root/.aws/credentials     ← stub: "See Mac for real credentials"
    └── /home/ubuntu/.aws/credentials
```

**Not backed up (as you decided):**
- `/etc/worxco/envs/*` — regenerated by `refresh-env-config` on boot ✓
- Git repo, FSx mounts, MariaDB — handled by deploy-host CFN or persistent storage
- Real `.aws/credentials` — sensitive, kept only on Mac; stub file tells operator where to find them

**Deliverables:**
- `scripts/deploy-host/backup-state.sh` (mirrors jumpbox pattern)
- `scripts/deploy-host/restore-state.sh` (mirrors jumpbox pattern with pre-restore /tmp snapshot + stub-file creation)
- `make backup-deploy-host` / `make restore-deploy-host` / `make list-deploy-host-backups` (in main Makefile since deploy-host is not migration-specific)
- Nightly EventBridge Scheduler for deploy-host too (same 02:00 America/Chicago)
- UserData auto-restore in `cf-deploy-host.yaml`
- Pre-destroy backup step in `destroy-deploy-host`

**Where do the make targets go?** Deploy-host isn't migration-specific — it's a core part of the parent project. So these targets belong in the main `Makefile`, NOT in `migration/Makefile`. That matches where `deploy-deploy-host` and `v

*[Response truncated at 4000 characters]*

---

### 15:04:16 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
