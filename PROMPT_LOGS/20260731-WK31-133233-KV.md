# AI Prompt Log

**Date**: 2026-07-31 13:32:33
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 13:32:33 - User Prompt

```
I looked at the zinew.sql file - there are LOTS of records - the client customized everything in this system around Zoning requirements. Failing to use 'node' and the related 'node-*' tables is not, in my mind, any kind of a problem.
```

---

### 13:39:05 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/deploy-host/install-drupal.sh`

---

### 13:42:42 - Response

Updated commit message covering all four fixes:

```
fix(migration): schema=zinew in settings.php + Phase 9 restart-php-fpm
              + quiet AUTO=yes preflight + loud warning after restore-codebase

Root cause of sandbox HTTP 500 + WSOD chain — settings.php was missing
'schema' => 'zinew'. Drupal's pgsql driver hardcodes 'public.<table>'
in introspection queries (drush cr, install-check, cache-container
rebuild) — resolves fine for ordinary queries via PostgreSQL search_path,
but fails for anything schema-qualified. Symptoms:
  - /admin → 302 → /core/install.php (Drupal thought site not installed)
  - drush cr fatal: relation "public.search_dataset" does not exist
  - Container caches partially corrupt → partial page renders

Fix in scripts/deploy-host/install-drupal.sh: 'schema' key in the
settings.php DB config, env-driven with a 'zinew' default matching
pgloader's current output. Set DRUPAL_DB_SCHEMA in Secrets Manager
if pgloader's target ever changes.

Additional migrate-full-all improvements from this session:

1. Phase 9 restart-php-fpm (MANDATORY, was missing).
   restore-codebase atomically swaps /var/www/drupal on FSx, but
   PHP-FPM workers still hold pre-swap bytecode in OPcache. Bytecode
   for files that no longer exist executes and resolves classes
   against the NEW composer classmap, fataling when the old bytecode
   references a class the new classmap lacks. clear-drupal-cache does
   NOT cover OPcache — only restart-php-fpm does.

2. restore-codebase.sh: loud warning block at end.
   Standalone runs (not via migrate-full-all) still need manual
   clear-cache + restart-php-fpm. Prior "Recommend:" footer was easy
   to miss; now a boxed warning explicitly names both required
   commands and their order.

3. migrate-db-all: quiet AUTO=yes preflight false-alarms.
   Phase -1 verify-drupal-installed is expected to fail on a fresh
   AUTO=yes deploy — the AUTO branch installs then re-verifies. But
   the raw FAIL block looked alarming in walk-away logs. Now under
   AUTO=yes, suppresses the FAIL block, prints "First-time install
   detected", runs install-drupal, then re-verifies with output
   visible so a second-time failure IS surfaced.

Files:
- scripts/deploy-host/install-drupal.sh
- migration/Makefile
- migration/scripts/deploy-host/restore-codebase.sh

Ready for review. When approved:
  git add scripts/deploy-host/install-drupal.sh \
          migration/Makefile \
          migration/scripts/deploy-host/restore-codebase.sh
  git commit -F -   # paste the message above
```

Reload sandbox and let me know what you see. If prod's `/` shows something specific beyond "Test", tell me what and I'll trace how it gets there.

---

### 13:42:42 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
