# AI Prompt Log

**Date**: 2026-05-19 16:25:57
**Week**: 21
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 16:25:57 - User Prompt

```
Smoke-testing Drupal at ENV=sandbox
  ALB:  sandbox-alb-2011302727.us-east-1.elb.amazonaws.com
  Host: drupal-sandbox.test
  ✗ Drupal returned HTTP 404
<html>
<head><title>404 Not Found</title></head>
<body>
<center><h1>404 Not Found</h1></center>
<hr><center>nginx</center>
</body>
</html>
make: *** [smoke-test-drupal] Error 1
```

---

### 16:26:50 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/scripts/publish-drupal-vhost.sh`

---

### 16:26:58 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/Makefile`

---

### 16:27:25 - Git Commit

**Commit:** `6f58235687d4`

**Message:**
```
feat(make): publish-drupal-vhost — write vhost without re-installing Drupal

After the previous fix added vhost-writing to install-drupal.sh, we hit a
chicken-and-egg: Drupal is already installed (.installed marker present
on FSx), so install-drupal.sh aborts at the marker check before reaching
the new vhost-writing step. Re-running install-drupal won't write the
vhost without first wiping the install (overkill).

This adds a surgical fixup:

  make publish-drupal-vhost ENV=<env>

SSM-dispatches a script to the deploy-host that writes ONLY the nginx
vhost to /var/www/nginx/sites-enabled/drupal.conf, sourcing the site name
from SSM. Reads SITE_NAME from /<env>/drupal/site-name.

The on-disk vhost body matches what install-drupal.sh now writes, so
running this once retroactively fixes any environment where install
preceded the vhost-writing logic. Future fresh installs use install-drupal.sh's
own step and don't need this.

Workflow now:
  make publish-drupal-vhost ENV=sandbox   # write vhost
  make reload-nginx ENV=sandbox           # fleet picks it up
  make smoke-test-drupal ENV=sandbox      # should return 200

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
```

**Files:**
```
M	Makefile
A	scripts/publish-drupal-vhost.sh
```

---

### 16:27:32 - Response

Pushed `81b5ccd..6f58235`. Three-step recovery from your Mac:

```bash
make publish-drupal-vhost ENV=sandbox    # writes vhost to FSx (no reinstall)
make reload-nginx ENV=sandbox            # fleet picks it up
make smoke-test-drupal ENV=sandbox       # should return 200
```

The chicken-and-egg: Drupal is already installed (`.installed` marker on FSx from the earlier successful run), so install-drupal-remote can't re-write the vhost — install-drupal.sh's marker check aborts at line 107 before reaching the new vhost step. The new `publish-drupal-vhost` target bypasses that check and writes ONLY the vhost.

---

### 16:27:32 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
