# AI Prompt Log

**Date**: 2026-05-08 11:12:01
**Week**: 19
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 11:12:01 - User Prompt

```
1 - I was not aware that CodeCommit has been deprecated. Already we're working inside a client's AWS account. The ZI-Sandbox is working in Zoning's account, not in the Works company's account. I don't know that he'll have access to CodeCommit at all.
Long term, if we use this in other clients, we need a solution that'll work across the board. My knee-jerk is to run our own GitLab instance for these kinds of things. Part of the reasoning is security. Part of the reasoning is centralized billing.
Not all customers are going to have the same feelings as I do on this, so we may end up needing to support Bitbucket, GitLab, GitHub, and others. I do think we should be able to offer, as an option, a localized Git repository.
Long ago I used GitLab. It was open source. I don't know if that's still true or not. See what kind of can of worms we opened with this? 
2 - You are correct that we need to specify the site as well as the environment. That is not something we need to run off and do at the moment. It is definitely a to-do/reminder/feature upgrade that we want to add a little bit later. 
3 - this is a good set of questions, but let me clarify how I perceive this at the moment.
From a multi-tenancy standpoint, this tooling will be used by at least two different client types that I'm aware of:
1. I am a hosting site. I have a lot of clients, and I host for them, which is why I need multi-tenancy and why it's already in production.
2. Customers who want complete control over their entire infrastructure. Limiting them to a single website when many large corporations own multiple websites doesn't make a lot of sense.
In both scenarios, you will quickly determine that there is a centralized IT skill, and that the websites are for end users to use. At no point do I ever want to have to give a client access to the underlying PHP, Drupal, database, and all those things. They can go through the front end and change it by adding new content, adding new media, and things of that nature. They cannot change it by adding a new module. If they want new modules installed, they go through us. In the corporate setting, if somebody needs new modules, they are going to go through IT. Hopefully that will help clarify this a little bit.
Summary: right now, I agree. Now is not the time to refactor any of this. Let's take what we've been talking about and table it for a later point in time. 
So yes, let’s write up a multi-tenancy doc for later review and refactoring. That sounds good to me.
```

---

### 11:14:39 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/plans/multi-tenancy.md`

---

### 11:15:03 - Git Commit

**Commit:** `ca6045c59b61`

**Message:**
```
docs(plans): Add multi-tenancy plan and operational model

Captures the architectural decisions and constraints from today's
discussion (2026-05-08) about evolving from single-site-per-env to
true multi-tenant Drupal hosting.

  Two deployment models:
    A. Managed Hosting (worxco) — many clients per AWS account
    B. Self-Managed (corporate IT) — many sites per AWS account
    Both supported by the same infrastructure code.

  Three roles, three channels:
    End users — Drupal admin UI for content/media (site only)
    IT operators — deploy-host SSM, drush, composer (all sites)
    Platform team — full AWS, all environments
    Key principle: end users never touch infrastructure. Module
    installs always flow through the IT operator role. Architecture
    enforces this — end users have only HTTP access via ALB.

  Multi-tenancy implications (path, DB, secrets, NGINX, PHP-FPM,
  CFN stack, Make targets) — table comparing single vs multi.

  Git hosting strategy:
    CodeCommit deprecated July 2024 — maintenance mode only.
    GitLab CE still fully open source (MIT license) — viable
    self-hosted option for "complete sovereignty" customers (~$15-
    20/mo). Tooling should accept git URL as parameter, host-
    agnostic.

  Layered codebase model:
    worxco-drupal-base — shared base repo (Drupal core, standard
                         modules, base theme)
    worxco-drupal-site-<client> — per-site overlay (client modules,
                                  client theme, client config)
    Sites composer-require base@^1.0; auto-inherit security patches
    on minor versions, opt-in for major. Per-client isolation,
    independent deploy cycles, clean offboarding.

  Five design questions deferred to team discussion:
    Q1 — repo granularity (per-site / monorepo / multisite)
    Q2 — base evolution policy (auto / opt-in / hybrid)
    Q3 — per-site dev environments
    Q4 — cross-site DB isolation (shared RDS vs per-tenant RDS)
    Q5 — tenant offboarding workflow

  Constraints for current Phase C work to keep compatible:
    - keep <env> in paths (don't simplify to /var/www/drupal/)
    - parameterize site-slug as variable (default 'drupal') even
      when single-site
    - NGINX default catch-all (don't add hardcoded server blocks)
    - Make target naming should not imply "only one site"
    Build for one, name for many.

  Future phasing C → D → E → F → G → H, each phase validating the
  previous on real infrastructure.

This is planning, not implementation. Phase C continues unchanged.

Co-Authored-By: Claude Opus 4.6 (1M context) <noreply@anthropic.com>
```

**Files:**
```
A	docs/plans/multi-tenancy.md
```

---

### 11:15:20 - Response

Pushed (`ca6045c`).

## What's in `docs/plans/multi-tenancy.md`

A 320-line planning doc covering:

| Section | Purpose |
|---------|---------|
| **Two Deployment Models** | Managed hosting (worxco) vs self-managed (corporate IT) — same infrastructure, different operators |
| **Three Roles** | End users (content), IT operators (modules/code), platform team (infra) — and what each can/cannot touch |
| **What Multi-Tenancy Changes** | Comparison table: paths, DBs, secrets, NGINX, PHP-FPM, Make targets — single vs multi-tenant |
| **Git Hosting Strategy** | CodeCommit deprecation, GitLab CE option, host-agnostic tooling principle |
| **Layered Codebase** | `worxco-drupal-base` + per-site overlay model, with composer dependency |
| **Design Questions** | Five questions deferred to team discussion (repo granularity, base evolution, dev envs, DB isolation, offboarding) |
| **Constraints for Current Phase C** | What to NOT do that would block the future refactor |
| **Suggested Future Phasing** | C → D → E → F → G → H roadmap |

The big "build for one, name for many" principle gives current Phase C a useful guide: today's single-site code paths should treat the site identifier as a variable that happens to default to a single value, not a hardcoded literal.

## When you bring the team in

The doc is designed to be a starting point for team discussion, not an answer. The five design questions in particular need real conversation:

1. Repo granularity (per-site vs monorepo vs Drupal multisite)
2. Base evolution policy (auto / opt-in / hybrid)
3. Per-site dev environments
4. Cross-site DB isolation (shared RDS vs per-tenant)
5. Tenant offboarding workflow

Linking the team to `docs/plans/multi-tenancy.md` on GitHub gives them the context without you having to re-explain the architecture each time.

---

Now back to watching `deploy-allX` finish. Once it's done, we can run `make install-drupal ENV=sandbox` and validate the cloud install end-to-end. Then we'll start on **step 3 — `cf-app-drupal.yaml`** to formalize the Secrets Manager + SSM parameter management.

---

### 11:15:20 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
