# AI Prompt Log

**Date**: 2026-07-29 08:46:48
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 08:46:48 - User Prompt

```
fire the commit - include the PROMPT_LOGS - I have done the rm -rf for .RESTORE* in /var/www -- which, begs the question... What about drupal-private.BAK.20260718-031235Z?
```

---

### 08:47:06 - Git Commit

**Commit:** `b060d4b30c3c`

**Message:**
```
fix(migration): restore-codebase — identify Drupal root by type=project, not composer.json presence

Bug caught during second real run 2026-07-29:

    [ERROR] Multiple top-level dirs contain composer.json:
             zoning_info_platform filtered_entity_reference entity_bundle_manager
    [ERROR] Ambiguous — cannot pick a single Drupal root.

Which — right. Every composer package has a composer.json, including
the sibling drupal-custom-modules. Mere PRESENCE of composer.json is
not a Drupal-root distinguisher. The `type` field IS:

    zoning_info_platform      → type=project              (Drupal root)
    entity_bundle_manager     → type=drupal-custom-module (sibling)
    filtered_entity_reference → type=drupal-custom-module (sibling)

## Fix

Change the identify-Drupal-root heuristic to parse each top-level
composer.json's `type` field and pick the one where `type == "project"`.
That's the composer convention for a top-level Drupal installation
(drupal/recommended-project sets it, drupal/legacy-project sets it).

Also:
- Added `jq` to the Phase 1 required-tools list. If jq isn't on the
  deploy-host, script fails early with `Install: sudo apt install -y jq`
  instead of dying at Phase 4.
- Error messages now include a `Cleanup: sudo rm -rf $STAGING_DIR`
  hint. FSx staging leftovers accumulate at ~1 GB per aborted run
  otherwise — nothing catastrophic on a 64 GB FSx, but nice not to
  make the operator hunt for the path.
- Legacy-tarball path (composer.json at staging root) is unchanged.

## Verification

Kurt's staging dir from the failed run confirms the fix aims at the
right field:

    entity_bundle_manager     — type=drupal-custom-module
    filtered_entity_reference — type=drupal-custom-module
    zoning_info_platform      — type=project

So the new heuristic returns exactly one candidate. Correct.

## Cleanup before retry

The failed run left `/var/www/.RESTORE-STAGING.20260729_132246Z`
(~1 GB, 59882 files) on FSx. Kurt can either:
  sudo rm -rf /var/www/.RESTORE-STAGING.*
or leave it — new runs use a fresh STAMP so the dir won't collide.

## Files Modified

- migration/scripts/deploy-host/restore-codebase.sh —
  identify-Drupal-root heuristic uses jq to read composer.json's
  `type` field; jq added to Phase 1 preflight; error messages include
  cleanup hints

Tests: bash -n clean; verified jq output on the actual staging dir
matches the expected type distribution.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
```

**Files:**
```
M	migration/scripts/deploy-host/restore-codebase.sh
```

---

