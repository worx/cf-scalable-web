# AI Prompt Log

**Date**: 2026-07-13 17:09:10
**Week**: 29
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 17:09:10 - User Prompt

```
strangely.... the NextRun says none... 

-------------------------------------------------------------------------------------------------------
|                                             GetSchedule                                             |
+-------------------+---------------------------------------+----------+----------+-------------------+
|    Expression     |                 Name                  | NextRun  |  State   |        TZ         |
+-------------------+---------------------------------------+----------+----------+-------------------+
|  cron(0 2 * * ? *)|  cf-migration-jumpbox-nightly-backup  |  None    |  ENABLED |  America/Chicago  |
+-------------------+---------------------------------------+----------+----------+-------------------+
```

---

### 17:10:26 - Response

**Everything checks out. The scheduler is properly wired to the current jumpbox.** All three components agree:

| Source | Current Jumpbox ID |
|--------|-------------------|
| CFN stack output | `i-0616b051563227381` |
| Scheduler Input (what it sends to SSM) | `i-0616b051563227381` ✅ |
| IAM policy `Resource` scope | `.../instance/i-0616b051563227381` ✅ |

**Meaning:** when the scheduler fires tonight at 02:00 America/Chicago (07:00 UTC), it will:
1. Assume `cf-migration-jumpbox-scheduler-role`
2. Call `ssm:SendCommand` — permission scoped to your current instance ✓
3. Run the AWS-RunShellScript document — permission scoped to that document ✓
4. Push commands to instance `i-0616b051563227381` — which will download `backup-state.sh` and run it
5. Result lands at `s3://sandbox-migration-kv-worxco/config/jumpbox-latest.tar.gz` and a new timestamped archive

## The good news pattern here

The CFN update behavior we were worried about (`${Jumpbox}` reference re-resolving after instance replacement) actually did exactly the right thing. When the instance was replaced, CFN saw that `${Jumpbox}` in the scheduler's `Input` and in the IAM role's `Resource` was a dependency, and updated both to the new instance ID. That's CFN behaving well.

## Simple verification tomorrow morning

Just run this any time after 07:00 UTC (02:00 CDT) tomorrow:

```bash
cd migration
make list-jumpbox-backups
```

You should see a NEW timestamped archive with tomorrow's date. Example:
```
=== Timestamped archives (90-day retention) ===
2026-07-13 12:20:52   9.5 KiB  jumpbox-20260713-172051Z.tar.gz    ← your manual from earlier
2026-07-13 13:15:29  10.3 KiB  jumpbox-20260713-181528Z.tar.gz    ← another manual
2026-07-14 02:00:XX  10.X KiB  jumpbox-20260714-070000Z.tar.gz    ← the automated one
```

If the third one appears, the whole autoscheduled backup pipeline is confirmed end-to-end.

## Big day, real ending

You've deployed:
- Nightly EventBridge backups armed and pointing at the current instance
- UserData auto-restore proven twice (survived two instance replacements)
- S3 lifecycle rules protecting the latest pointer indefinitely
- Pre-destroy backup on `destroy-jumpbox`
- Non-fatal error handling learned the hard way ("Conditional" replacement really is conditional in both directions)

That's a real backup/recovery system in production. Enjoy the win — check `make list-jumpbox-backups` in the morning for the automated proof.

---

### 17:10:26 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
