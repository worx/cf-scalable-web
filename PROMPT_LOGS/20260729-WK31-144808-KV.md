# AI Prompt Log

**Date**: 2026-07-29 14:48:08
**Week**: 31
**AI System**: Claude Opus 4.6
**Project**: cf-scalable-drupal
**Session ID**: 8a226b59-faf4-46ce-8ca0-4d891822f86c

---

## Metadata

- Developer: KV
- User: kurtvanderwater
- Host: KLVMacBookPro16.local
- PWD: /Users/kurtvanderwater/Work/dev/cf-scalable-drupal
- Git Branch: main

---

## Prompt Log

### 14:48:08 - User Prompt

```
add both to Phase D backlog

once you have done that, let's do a review of the backlog for anything that would apply to our 'destroy-all'. Now 'might' be a good time to review/implement some of those items.
```

---

### 14:51:24 - File Modified (Write)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/phase-d-backlog.md`

---

### 14:51:27 - File Modified (Edit)

- `/Users/kurtvanderwater/Work/dev/cf-scalable-drupal/docs/memory/README.md`

---

### 14:52:15 - Response

Phase D backlog now captured at `docs/memory/phase-d-backlog.md` (index updated in README.md).

## destroy-all-related items — review

From the backlog, four items touch destroy-all:

| Item | What it fixes | Effort | Risk | Deploys with... |
|------|--------------|--------|------|-----------------|
| **A. Orchestrator check-before-delete** | False-✓ bug — real failures reported | Moderate (Python + ~13 Makefile targets) | Low | Nothing (pure orchestrator + Makefile) |
| **B. Lambda-backed purge-on-delete for versioned buckets** | Auto-fixes the "5535 delete markers block delete" problem | Larger (inline Lambda + IAM + custom resource) | **Medium** — custom resources can fail | deploy-all picks it up; tested on next destroy |
| **C. LifecycleConfiguration `NoncurrentVersionExpiration:1`** | Prevents version accumulation between destroys | Very small (~5 lines YAML) | Very low | deploy-all picks it up |
| **D. cf-app-drupal survives-destroy docs + `destroy-app-drupal` target** | Codifies intent; explicit nuclear option | Small | Very low | Nothing (docs + Makefile) |

## My recommendation for tonight

**Do A + C + D. Defer B.**

Rationale:

- **A** (orchestrator) is a pure Python/Makefile change. Zero impact on the CFN templates that deploy-all will use. Testing surface = just re-running orchestrator against a real state; low risk of complicating deploy-all.
- **C** (Lifecycle rule) is 5 lines of YAML that deploy-all will pick up cleanly. Even if it does nothing this rebuild cycle, it starts protecting the bucket from Day 1 of the new environment.
- **D** (cf-app-drupal docs + escape hatch) is pure documentation + a new Makefile target. No risk to anything running.
- **B** (Lambda purge custom resource) is a bigger change with real risk during deploy-all. Better done in a dedicated session where it can be tested carefully. Meanwhile, the tactical workaround is: `make clean-migration-baks` between the last sync-s3-media and any destroy-all. Not elegant, but not blocking.

## Interim workaround for the versioning problem

For future destroys until B lands, the pattern is:

```bash
# Pre-destroy: purge versioned content from the media bucket
# (I have the boto3-alternative bash script from tonight — could add
# a `make purge-media-versions` shortcut if wanted, ~15 lines)
```

If you want that shortcut added tonight, I can wire it — small.

## Order of operations if you agree

1. **A** (orchestrator) — ~15 min
2. **C** (lifecycle rule) — ~5 min
3. **D** (docs + destroy-app-drupal) — ~10 min
4. Optionally: `make purge-media-versions` shortcut — ~10 min
5. Then `make deploy-all`

Total add: ~30-45 min before firing deploy-all.

Or skip everything and just fire deploy-all now. Your call.

---

### 14:52:15 - Response Complete

Stop reason: completed

---


<sub>**License:** GPL-2.0-or-later | **Copyright:** 2026 The Worx Company | **Author:** Kurt Vanderwater <<kurt@worxco.net>></sub>
